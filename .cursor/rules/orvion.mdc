---
description: ORVION entry point
alwaysApply: true
---

Start at `README.md`; run `pwsh -NoProfile -File scripts/check_agent_continuity.ps1 -Boot`. Reading is unrestricted; only an active CR Write Scope authorizes mutation. `AGENTS.md §3–§4` owns conduct and fallback routing.
