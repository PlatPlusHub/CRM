# MASTER_INTEGRATION_CATALOG — External Integration Contracts

Class: **LIVING-AUTHORITATIVE** (evolve in place; one SSOT row per integration — `GOVERNANCE.md §2/§4`).
Purpose: the single registry of every external-platform integration — direction, transport, ORVION surface, status, and owning decision record. Seeded 2026-07-17 at its recorded trigger (Phase 8 landed; `future-backlog.md` Adopt-Later). Platforms are **consumers** of ORVION's verified outcomes, never dependencies (`PROJECT_CONTEXT.md §11`).

## 0. Supabase project topology (deployment targets — verify before every schema/data change)

Recorded here, not a new document: this is ORVION's own deployment-target registry, the closest existing fit to "external targets ORVION connects to" (`GOVERNANCE.md §6.8` — evolve an existing authoritative home rather than create a new one). Owner-provided ground truth, 2026-08-10.

**Account-identity labeling (added 2026-08-15, documentation-only — makes an already-implicit distinction explicit, no re-classification):** `platplustours@gmail.com` is the **tenant/customer account** — Platinum Plus Tours, ORVION's first production customer. `shehab.nim@gmail.com` is the **founder/developer account** used to build, test, and validate ORVION itself. This was already distinguishable from the differing `Organization` column values below (Platinum Plus Tours vs Orvion) but had no explicit label; the distinction matters because ORVION's product architecture must never assume the founder/developer account's identity or credentials as a product default when a future second tenant is onboarded.

| Project | Ref | Account | Organization | Role | Status |
|---|---|---|---|---|---|
| **ORVION** (primary) | `vrvtsxexkiiiivlkdxzp` | platplustours@gmail.com | Platinum Plus Tours (`zvlxdabpjuunyfigkhci`) — org name/id as owner-provided, not independently verifiable via MCP (see note) | **Primary** | ACTIVE_HEALTHY. **Reachable via the dedicated `supabase-primary` MCP server** — switched 2026-08-15 from local-stdio/PAT to the official Supabase **Remote MCP over OAuth** (`.mcp.json`; full detail `.workstation/manifest.md §4`). Still not reachable through the `claude_ai_Supabase` connector (`list_projects` omits it; `get_project` returns a permission error — that connector is scoped to the `shehab.nim@gmail.com` account only; this is a connector-scoping fact, not a project-reachability fact). **SPEC-120 deployment status: VERIFIED — full comparison 2026-08-10, see `MASTER_CERTIFICATION_STATUS.md`. SPEC-122 (`202607050000`) deployed and verified live 2026-08-15 — 89 migrations, `has_schema_privilege('orvion_integration','app','USAGE')` confirmed `true`; see §4.** **Primary's OAuth connection showed intermittent `Unauthorized` failures on data-access tools this same session (twice, resolved on a third disconnect+reauthenticate, no config change) — a future session must re-verify with a real data call before trusting it is reachable; do not assume based on this record alone.** |
| **orvion-core** (secondary / mirror) | `brplkqmbzffpxqgkkdzo` | shehab.nim@gmail.com | Orvion (`emjkklugmzgyuzuabell`) | **Secondary — mirrors primary** | ACTIVE_HEALTHY. SPEC-120 deployed and migration-version-reconciled 2026-08-10. Reachable via the `claude_ai_Supabase` connector. **89 migrations, SPEC-122 deployed 2026-08-15 — Primary and Secondary are synchronized (89/89, version-for-version and name-for-name identical, both `has_schema_privilege(...) = true`); see §4 and `MASTER_CERTIFICATION_STATUS.md`.** |
| orvion-core *(deleted)* | `hzyuczdlwalectfduehw` | platplustours@gmail.com | Platinum Plus Tours | — | **DELETED — intentionally. Never use, restore, link, or reference.** |
| ORVION *(created by mistake)* | `wgsmrjcuhjdksfpdbhre` | shehab.nim@gmail.com | Orvion (`emjkklugmzgyuzuabell`) | — | Created 2026-08-09; zero migrations, zero tables (re-confirmed 2026-08-10 via `list_migrations`/`list_tables`). Not an ORVION target. **Owner intends manual deletion.** No `delete_project`-class tool exists in any Supabase MCP server available to this repository as of 2026-08-10 — deletion is not currently possible via MCP regardless of authorization. **Scoped pre-authorization (owner-ratified 2026-08-10):** if a project-deletion tool ever becomes available to a future session, deletion of this exact ref (`wgsmrjcuhjdksfpdbhre`) only is pre-authorized without a further live owner confirmation, on condition the agent re-runs and re-confirms the same four checks recorded above (exact ref match; not one of the two authorized refs; zero migrations; zero tables) immediately beforehand. This pre-authorization does not extend to any other project, ref, or account, and does not relax the Primary/Secondary protection below. |

**Note on primary org/account attribution:** the `vrvtsxexkiiiivlkdxzp` → `platplustours@gmail.com` / "Platinum Plus Tours" mapping above is owner-provided ground truth, not independently confirmed by this repository — the `supabase-primary` MCP server exposes no `get_project`/`get_organization`/`list_organizations` tool, only project-scoped data/schema tools, so there is no MCP call available to cross-check which account or organization it authenticates as.

**n8n production target (USER-CONFIRMED 2026-08-15, resolves the former §3-item-3 open decision below):** the owner has directed that **Primary** (`vrvtsxexkiiiivlkdxzp`) is the future n8n production Postgres connection target; **Secondary** (`brplkqmbzffpxqgkkdzo`) is reserved for a possible future failover/backup n8n connection only. Both projects are kept permanently — this is not an either/or choice. If a Secondary connection is ever added, the two must never act as simultaneous active writers; the existing outbox idempotency/retry design (`claim_conversion_deliveries` / `record_conversion_delivery_result`) is the mechanism that must be preserved to keep that safe. No credential has been created against either project yet. The local Supabase CLI link (`supabase/.temp/linked-project.json`, gitignored) currently points at Secondary — this is a local dev-machine artifact only and must not be read as evidence of which project is production.

**Synchronization & agent-discipline rules (owner-ratified 2026-08-10; expanded 2026-08-10 into an explicit checklist — content unchanged, only made enumerable and testable). Each item is tagged with what actually enforces it — do not read a tag as stronger than it is:**

1. GitHub repository (`supabase/migrations/**`) is the schema/migration source of truth. *[AGENT DISCIPLINE — a repo instruction; nothing scans for direct out-of-band schema edits on either live project.]*
2. No undocumented direct schema change against either project. *[AGENT DISCIPLINE]*
3. Every ORVION migration is tracked in GitHub before being applied anywhere. *[AGENT DISCIPLINE]*
4. Every applicable migration is applied to **both** Primary and Secondary. *[AGENT DISCIPLINE — no automated deployment fan-out exists; each apply is a manual/agent-directed MCP call today.]*
5. A database change is not "complete" until both projects are verified, not assumed. *[AGENT DISCIPLINE]*
6. A new session must verify the current project refs (this table) before any database write. *[AGENT DISCIPLINE]*
7. A new session must verify migration synchronization before declaring a database task complete. *[AGENT DISCIPLINE]*
8. Never assume one Supabase project represents the whole ORVION system. *[AGENT DISCIPLINE]*
9. Never use `wgsmrjcuhjdksfpdbhre` or `hzyuczdlwalectfduehw` (or any ref other than the two Primary/Secondary rows above) as an ORVION deployment target. *[PARTIALLY ENFORCED — `scripts/check_repository_consistency.ps1` Check 8 fails if this file's Primary/Secondary ref values are altered or the retired/mistake rows are removed, but it cannot inspect live MCP calls an agent actually makes.]*
10. Never delete either authorized project (Primary or Secondary) without explicit, contemporaneous owner authorization. *[AGENT DISCIPLINE — no technical control prevents it; this is a hard behavioral rule.]*
11. If one project is unreachable, report the access gap; never claim synchronization anyway. *[AGENT DISCIPLINE]*
12. If Primary and Secondary differ, report the difference; never silently declare them synchronized. *[AGENT DISCIPLINE]*
13. GitHub must be updated whenever repository migrations or this governance is changed. *[AGENT DISCIPLINE, backstopped by normal PR/commit review]*
14. This consistency check must be re-runnable by any future session. *[ENFORCED — `pwsh -File scripts/check_repository_consistency.ps1` is deterministic, dependency-free, exit-code gated.]*
15. The guard must fail loudly / report INCOMPLETE, never silently pass, when only one project has been updated. *[PARTIALLY ENFORCED — Check 8 catches the *documented* contradiction (this file marking a project's deployment status as not confirmed while `MASTER_CERTIFICATION_STATUS.md` simultaneously claims CERTIFIED synchronized); it cannot detect an actual live drift an agent never wrote down, because it has no live DB credentials by design (dependency-free, CI-safe).]*

**Honest limit, stated once here rather than re-litigated per item above:** items 1–13 and part of 15 are *instructions a compliant agent follows*, not something this repository can force an arbitrary LLM session to obey — nothing in Git or CI can stop a future session from querying or writing to the wrong project. Only items 9 (partially), 14, and 15 (partially) have an actual automated check behind them. **Verify the exact project ref before every schema-changing or destructive operation against either** — never assume which project is the target.

## 1. Registry

| Integration | Direction | Transport | ORVION surface | Status | Decision |
|---|---|---|---|---|---|
| **Google Ads offline conversions** | Outbound | Google **Data Manager API** + Enhanced Conversions for Leads, orchestrated by **n8n** over the database outbox | `app.capture_attribution_click` → `app.map_outcomes_to_conversions` → `app.claim_conversion_deliveries` → `app.record_conversion_delivery_result` (migrations 049200/049300/049400); role `orvion_integration` | **ORVION-side COMPLETE**; n8n workflow pending owner-exclusive credentials (§3) | ADR-0023; analysis: `google-offline-conversion-transport-decision-2026-07-17.md` |
| Landing-page / GTM click webhook | Inbound | GTM/site webhook → n8n → `app.capture_attribution_click` | same capture RPC (explicit-tenant, integration-role-only) | ORVION-side complete; site/GTM wiring at first live campaign | ADR-0023 (capture side); SPEC-119 |
| Meta Conversions API | Outbound | Meta CAPI via n8n, **reusing the same outbox** (`platform_code = 'meta_ads'`) | identical claim/ack pair — no new ORVION surface expected | Planned (Phase 10) | ADR-0023 §channel-generic |
| WhatsApp Cloud API (company-owned conversations) | Bidirectional | Meta WhatsApp Cloud API via n8n | `conversations`/`conversation_messages` (built; `external_conversation_id` ready); ingestion/send RPCs to be designed | Planned (Phase 10; shape needs Meta-ecosystem Learn-Before-Designing) | future-backlog §Group-3 determinations |
| GA4 / GTM tagging | Client-side | Google tag; coexists with server-side truth via Google's unified enhanced-conversions setting | none (browser-side); ORVION remains authoritative | At first UI / live campaign | ADR-0023 §ecosystem |

Rules: one row per integration; a new integration enters only with its decision record; retire rows via strikethrough + pointer (ADR-supersede convention). n8n is the orchestration layer for all outbound/inbound flows (owner decision 2026-07-17); any orchestrator swap changes *credentials and workflows*, never the ORVION RPC surface.

**Tenant-scope note (added 2026-08-15, documentation-only, no architecture change):** the Google Ads row's workflow contract (§2) and validation state (§4) are currently wired to ORVION's first and only production tenant's specific account/conversion-action IDs — this is correct for the current single-tenant state, not a product-level design. The `integration_connections`-style per-tenant credential/config model that would generalize this to a second travel-agency tenant is already tracked as **CDD-7** (`reports\master\MASTER_GAP_REGISTER.md`, status DESIGN-READY, not yet implemented) and **DC-14** (tenant offboarding, depends on CDD-7 outbox) — correctly Earn-It-deferred until a second tenant needing different Google Ads credentials actually exists (`AGENTS.md §3` Fundamental-Domain-Structure test), not silently missing. A future session adding a second tenant's integration must design against CDD-7, not copy this section's single-account pattern.

## 2. Google delivery — n8n workflow contract (build-ready spec)

One workflow, schedule-triggered (e.g. every 15 min). All DB calls use the `orvion_integration` Postgres credential; the whole pipeline is idempotent, so a crashed run is safely re-run.

1. **Map** — `select app.map_outcomes_to_conversions(500);` (advances the event cursor; returns rows mapped).
2. **Claim** — `select * from app.claim_conversion_deliveries('google_ads', 50);` → if 0 rows, stop. Only consent-granted conversions are ever returned (in-DB gate); each row carries `delivery_id`, conversion facts, click IDs (`gclid`/`gbraid`/`wbraid`), consent signals, and raw `customer_phone`/`customer_email`.
3. **Hash at the edge** (Function node) — normalize then SHA-256 (lowercase hex): email → trim + lowercase; phone → E.164 (`+…`) before hashing. **Raw PII never leaves n8n**; ORVION stores truth, Google receives hashes + click IDs only.
4. **Send** (HTTP node, OAuth2 credential with the `datamanager` scope) — Data Manager API conversion-event ingestion: click ID(s) + hashed `userIdentifiers` + `conversion_event_type_code`-mapped action + value/currency (revenue — never profit, ADR-0023) + `conversion_at` + consent (`adUserData`/`adPersonalization` from the claimed row). *Fast-moving surface:* verify the exact endpoint/payload field names against current Google Data Manager docs at build time (Learn-Before-Designing; the API is <1 yr old).
5. **Ack per row** — `select app.record_conversion_delivery_result(:delivery_id, :success, :response_json, :error_text);` → `sent`/`failed` + canonical events. Failed rows become re-claimable automatically on later runs (retry ceiling 5, then parked for review via `reporting`/events).

Monitoring: `offline_conversion_deliveries` rows + `offline_conversion_sent/failed` events are the health signal; a staleness/failure report is a Tier-A view away if operations wants one (Earn-It: build on demand).

## 3. Owner-exclusive setup (remaining Phase-8 inputs)

1. **RESOLVED (2026-08-16/17).** Google Cloud OAuth **Web application** client for the **Data Manager API** (`datamanager` scope) was created in `orvion-data-manager` (owner-performed in Console — no API/CLI path exists for standard OAuth client creation, confirmed this session against current `support.google.com/cloud/answer/15549257`; `gcloud iam oauth-clients` is a different product, Workforce Identity Federation, and was correctly not used). Authorized redirect URIs registered: `https://oauth.n8n.cloud/oauth2/callback` and `https://plat.app.n8n.cloud/rest/oauth2-credential/callback` (both registered deliberately — n8n Cloud has a known redirect-URI-mismatch bug across its own instances; current n8n community reports, 2026-08-17). Configured in n8n as a generic **"OAuth2 API"** credential (not the built-in "Google OAuth2 API" credential, whose fixed scope list does not include `datamanager`) with `access_type=offline&prompt=consent` to guarantee refresh-token issuance. Owner completed the Google consent flow (Testing-mode "Google hasn't verified this app" warning expected and accepted). **USER-CONFIRMED** connected successfully; not independently re-verifiable by an agent (no browser/API access this session, by design — the OAuth secret never left the owner's browser/n8n). The two pre-existing Desktop clients ("Shehab Prv lab", "Work lab") were not touched.
   - **Google Cloud project:** "ORVION Data Manager" (`orvion-data-manager`, project number `603895119164`). Google Auth Platform remains **External / Testing** — refresh tokens issued to n8n expire after 7 days while in this status (VERIFIED against current sources this session: Google force-expires tokens at 7 days for any app that has not completed OAuth verification). **Decision (technical, not escalated):** proceed in Testing mode to build and validate the pipeline now — ORVION is pre-production with no real ad spend, so 7-day expiry doesn't block build/validation. Full Google OAuth verification (required to reach "In production" and remove the 7-day limit) is deferred to a pre-go-live gate, not a Phase-8 blocker. `datamanager.googleapis.com` confirmed enabled in the project (`gcloud services list`, read-only, this session).
2. **Google Ads account-side Enhanced Conversions for Leads setup** (verified against current Google Ads Help, 2026-08-10 — not previously documented here): enable "Turn on enhanced conversions" (Goals → Settings), agree to the Google Ads Data Processing Terms, and create the Conversion Action(s) that will receive ORVION's conversions. **RESOLVED (owner-decided 2026-08-16):** one Conversion Action per `offline_conversion_event_type` code — **one-to-one mapping**, five actions total. `qualified_lead` already has its action ("ORVION - Qualified Lead", id `7721902713`); four more are being created the same way (`qualified_phone_call`, `booking_created`, `payment_received`, `ticket_issued`). Rationale: richest signal for Smart Bidding and full-funnel reporting visibility, at the cost of more Google Ads console setup — judged worth it for a 5-stage funnel. **Primary/Secondary bid-optimization designation deferred** (recorded, not decided): all five actions start as Secondary (reporting-only, no bidding impact) since ORVION is pre-production with zero real conversion volume — promoting one to Primary is a future decision once real conversion data exists to judge which stage is the best bidding signal, not a Phase-8 blocker. **Supersedes the §4 caution below** ("do not create another conversion action") — that note was scoped to avoid side effects *during the validateOnly technical test*, which is now complete (§4); the owner has now explicitly authorized creating the four additional actions.
3. **RESOLVED (2026-08-17).** Login enabled on `orvion_integration` (`alter role orvion_integration with login password '…';`, run by the owner directly in the Primary SQL Editor — password never entered the repo, chat, or any agent tool call). **VERIFIED** independently this session (`select rolcanlogin from pg_roles where rolname = 'orvion_integration'` via the `supabase-primary` MCP, before: `false`, after: `true`). n8n Postgres credential created against Primary via the **Session Pooler** (`aws-0-eu-central-1.pooler.supabase.com:5432`, database `postgres`, user `orvion_integration.vrvtsxexkiiiivlkdxzp` — correctly the dedicated integration role, not the pooler's `postgres.<ref>` example user) — **USER-CONFIRMED** tested successfully in n8n.
4. **RESOLVED (2026-08-17).** n8n Cloud instance now exists: `plat.app.n8n.cloud`. Both required credentials (Google OAuth2 API generic credential, item 1; Postgres, item 3) are created and tested in n8n. **Next step:** build the §2 workflow.

## 4. Google Ads Data Manager — Phase-A validation state (handoff log)

Class note: this section is the durable handoff record for the in-progress Data Manager validation sub-step of Phase 8, so a new session/agent can resume without conversational context. Classification tags follow this session's convention: **VERIFIED** (independently confirmed from repo or current official docs this session), **USER-CONFIRMED** (owner-stated, not independently verifiable by an agent), **RECOMMENDATION** (engineering proposal, not yet owner-ratified), **OPEN DECISION** (requires owner approval), **NOT-CONFIRMED** (external claim not independently checked this session — must not be treated as fact; a different concept from this file's `§0` Supabase-deployment status convention). Last verified: 2026-08-15.

**Conversion Action mapping — RESOLVED 2026-08-17 (USER-CONFIRMED — owner-supplied Google Ads UI evidence; no Google Ads API/UI access exists in this environment to independently verify):** one-to-one, all five ORVION `offline_conversion_event_type` codes now have a dedicated Google Ads Conversion Action, all currently **Secondary** (reporting-only, no bid-optimization impact) pending real conversion volume:

| `offline_conversion_event_type` | Conversion Action name | Conversion Action ID (`productDestinationId`) |
|---|---|---|
| `qualified_phone_call` | ORVION - Qualified Phone Call | `7724520652` |
| `qualified_lead` | ORVION - Qualified Lead | `7721902713` |
| `booking_created` | ORVION - Booking Created | `7724276153` |
| `payment_received` | ORVION - Payment Received | `7724478225` |
| `ticket_issued` | ORVION - Ticket Issued | `7724479620` |

None have a connected data source ("Set up import") — deliberately, per the §3-item-1 caution; the Data Manager API ingestion path (used here) is separate from that UI wizard.

**Phase-8 execution checkpoint (2026-08-17, updated same day post-validation) — durable handoff, read this first on any new session:**
- **Done:** dedicated Web OAuth client created + configured in n8n (§3-1); `orvion_integration` login enabled + Postgres credential tested in n8n (§3-3); n8n Cloud instance live at `plat.app.n8n.cloud` (§3-4); all 5 Conversion Actions created and mapped (table above). **n8n workflow built**, 7 nodes wired exactly to the §2 contract: Schedule Trigger → `Map Outcomes` (Postgres, `select app.map_outcomes_to_conversions(500)`) → `Claim Deliveries` (Postgres, `select * from app.claim_conversion_deliveries('google_ads', 50)`) → `Hash At Edge` (Code node — SHA-256 email/phone normalization, `qualified_phone_call`/`qualified_lead`/`booking_created`/`payment_received`/`ticket_issued` → the five Conversion Action IDs above, consent-code mapping to Google's `CONSENT_GRANTED`/`CONSENT_DENIED`/`CONSENT_STATUS_UNSPECIFIED`, `validateOnly: true` hardcoded) → `Send To Data Manager API` (HTTP Request, OAuth2 credential, `POST https://datamanager.googleapis.com/v1/events:ingest`) → `Prepare Ack` (Code node) → `Ack Result` (Postgres, `select app.record_conversion_delivery_result($1,$2,$3,$4)`).
- **Manual non-destructive validation — USER-CONFIRMED (2026-08-17):** workflow executed once manually (not the schedule). `Map Outcomes` and `Claim Deliveries` executed, no errors; `mapped_count = 0`, **0 rows claimed** — consistent with the reaffirmed pre-production empty dataset, not a defect. Per current n8n behavior (VERIFIED this session against n8n community/docs: a node producing zero output items does not propagate execution to connected downstream nodes, and no "Always Output Data" override is set anywhere in this workflow), `Hash At Edge`, `Send To Data Manager API`, `Prepare Ack`, and `Ack Result` did not execute. **Google Data Manager API was not contacted this run** — nothing was sent, hashed, or claimed-and-abandoned. `validateOnly: true` remains hardcoded in `Hash At Edge`, unexercised. No test/fabricated conversion was created; no production data was touched.
- **Not done:** an actual send to the Data Manager API has never happened from n8n (the Phase-A `validateOnly` calls in this section were direct API tests, not through this workflow). That requires either synthetic-but-labeled test data flowing through `Claim Deliveries` (not yet authorized) or genuine ad-click traffic (does not exist yet).
- **Current blocker:** none.
- **Exact next step:** owner decision on how to exercise the `Hash At Edge` → `Send` → `Ack` path with `validateOnly: true` still on, without fabricating a real conversion — e.g., a clearly-labeled synthetic conversion inserted and rolled back in the same spirit as the 2026-08-15 internal pipeline validation (§ below), never committed. Not yet authorized; owner-gated.
- **Known constraint carried forward:** Google Auth Platform is Testing status → n8n's refresh token expires every 7 days until the app completes Google OAuth verification (deferred to a pre-go-live gate, §3-1).

**Google Ads account & conversion action (USER-CONFIRMED — owner-supplied Google Ads UI evidence, not independently checked by any agent this session; no Google Ads API/UI access exists in this environment):**
- Account: `591-238-3888` (`5912383888`).
- Conversion action: **"ORVION - Qualified Lead"**, id `7721902713` — this is the `productDestinationId` for the Data Manager `Destination` object.
- Confirmed in the Google Ads UI: Conversion source = "Website (Import from clicks)" (= Google's `UPLOAD_CLICKS` type — the type required for offline conversions / Enhanced Conversions for Leads, per current Google documentation, VERIFIED below). Action optimization = Qualified leads / **Secondary** action. Count = One conversion. Click-through window = 90 days. Engaged-view window = 3 days. Attribution = Data-driven. Current status = **Inactive / Misconfigured** — Google UI reports no associated data-source connection yet (expected: nothing has been connected).
- **Terminology collision warning:** Google's "Secondary action" here is a *conversion-action optimization role* inside Google Ads (Primary vs Secondary conversion actions for bidding). This is a completely different concept from ORVION's "Primary/Secondary Supabase project" topology in §0 above. Do not conflate the two — nothing about the Supabase Primary/Secondary decision implies or requires changing this conversion action's Google-side Primary/Secondary status, and it must not be changed as part of this validation.
- **Do not** create another conversion action. **Do not** change this action's Secondary→Primary status. **Do not** click "Set up import" / "Connect data source" — those steps are out of scope until the technical validation below is explicitly approved and completed.

**Google Cloud / ADC state (USER-CONFIRMED — not independently verified; no `gcloud`/network calls were made by any agent this or the prior session, to avoid side effects on a live Google account):**
- Google Cloud project: `orvion-data-manager`; Google account `platplustours@gmail.com`.
- ADC configured via a dedicated OAuth desktop client with scopes `adwords`, `cloud-platform`, `datamanager`; an earlier scope-insufficient error was resolved by recreating ADC with this client/scope set.
- A prior Data Manager API call returned `requestId: v-eb919025-60b9-42c8-8e6e-a219fdc477e5` (an opaque Google-side request-correlation id, not a credential — safe to retain). Its response body was not reviewed in this session; API reachability alone is **not** proof of successful validation (do not treat it as such).
- No OAuth client secret, access token, or refresh token is recorded anywhere in this repository, per the security rule below.

**transactionId strategy — RECOMMENDATION (not yet owner-ratified as an ADR; carried forward from the prior session's review, re-confirmed this session against current Google docs):**
- Use `offline_conversions.id` (already returned as `conversion_id` by `app.claim_conversion_deliveries`, migration `202607049200`) as the Data Manager `transactionId`. No schema change; no new column; not GCLID-based.
- **VERIFIED (current official Google Data Manager API docs, fetched 2026-08-15):** `transactionId` is schema-optional in general; Google's own note is "Required for events sent as an additional data source for tag conversions." Within one conversion action, Google Ads uses it to dedupe events from multiple sources (a tag + the API) — if it matches an existing tagged event the API value overrides the tag's; otherwise a new conversion is created. ORVION's current flow has no client-side tag firing this same conversion action yet, so this is not currently a hard Google requirement for ORVION — it is future-proofing against the point where GA4/GTM tagging goes live on the same account (Integration Catalog §1 GA4/GTM row: "At first UI / live campaign").
- Repository confirmed (grep, 2026-08-15): no `transaction_id` column exists anywhere in `supabase/migrations/**` or `_ORVION_CANONICAL/**`; none should be added to support this.

**Phase-A `validateOnly` test — EXECUTED 2026-08-15 (result recorded here, not fabricated in chat):**
Schema re-verified against three independent current Google sources immediately before sending (`Destination` reference page, `ProductAccount` type description, and the literal example body in the `send-events` devguide) — confirmed `operatingAccount`/`loginAccount` are `ProductAccount` objects `{accountType: "GOOGLE_ADS", accountId: "5912383888"}`; `Consent` object is `{adUserData, adPersonalization}` with enum `CONSENT_GRANTED`/`CONSENT_DENIED`/`CONSENT_STATUS_UNSPECIFIED`.
- **Request:** `POST https://datamanager.googleapis.com/v1/events:ingest`, `validateOnly: true`, synthetic `transactionId` (`ORVION-TEST-<uuid>`, not written to Supabase, no `offline_conversions` row created), synthetic `gclid` (`TEST-GCLID-ORVION-VALIDATEONLY-<random>`), no real customer PII, `currency: EGP`, `conversionValue: 1.00`, `eventSource: WEB`, `operatingAccount = loginAccount = {GOOGLE_ADS, 5912383888}`, `productDestinationId: 7721902713`.
- **Result: HTTP 200 OK.** Response body: `{"requestId": "v-54ea963b-8551-4609-b66f-1314e07f5268"}` — no error, no per-field or per-destination detail.
- **Google's own documentation (fetched same session) states diagnostics/`RetrieveRequestStatus` can only be retrieved for requests with `validate_only` NOT set to true** — a bare `{requestId}` 200 response is therefore the maximum signal `validateOnly=true` can ever produce for this API; no further query is available to get more certainty than what is recorded here without a non-`validateOnly` send (not authorized).
- **Classification (A/B/C/D, not conflated):**
  - (A) API authentication — **VERIFIED**: 200 OK, no auth error.
  - (B) Request/schema validation — **VERIFIED with reasonable inference**: no `400 INVALID_ARGUMENT` (the user's earlier empty-`events` test previously produced exactly that error shape for a malformed request); a structurally invalid request would be expected to fail the same way.
  - (C) Destination/conversion-action-level validation (that `7721902713` is specifically accepted as belonging to, and of the correct type for, account `5912383888`) — **NOT-CONFIRMED**: the response contains no field confirming this, and per Google's own docs no further diagnostic query exists for a `validateOnly` request. Absence of a rejection is consistent with correct pairing but is not the same as an explicit confirmation.
  - (D) Real conversion ingestion — did not happen; `validateOnly: true` was honored, by design and by result (no ingestion-only fields like a persisted event id were returned).
- Only after explicit further owner approval does a controlled real-GCLID (non-`validateOnly`) test become the next step — never a manufactured/fake GCLID, never without approval. **Superseded by the entry immediately below (2026-08-15): ORVION is still pre-production — no real GCLID exists, and none should be manufactured. The real-GCLID test is on hold until genuine ad-click traffic exists; do not attempt it before then.**

**Internal ORVION-side pipeline validation — EXECUTED 2026-08-15 (local dev database only, never Primary/Secondary):**
Ran against a freshly `supabase db reset` local stack (`supabase_db_ORVION`, all 88 migrations applied clean, existing `scripts/verify_database.sql` smoke-test passed first). The entire test — two synthetic tenants, a lead, an attribution click, and the full conversion pipeline — ran inside one transaction that ended in `ROLLBACK`; a post-run query confirmed **zero residue** (0 rows in every touched table). No external system was contacted.
- **Proven (executed, not just read from source):**
  - `app.record_offline_conversion` — blocked without tenant context (correct rejection message); allowed path creates exactly 1 row + exactly 1 `offline_conversion_created` event.
  - `app.map_outcomes_to_conversions` — `authenticated` role correctly denied EXECUTE (service-only surface); as `orvion_integration`, one `lead_qualified` event mapped to exactly 1 new conversion; an immediate re-call mapped 0 — the same event is never double-mapped (idempotent).
  - `app.claim_conversion_deliveries` — `authenticated` correctly denied; as `orvion_integration`, claimed the eligible consent-granted conversion(s); an immediate re-claim returned 0 — a pending delivery blocks re-claiming the same conversion (idempotent claiming, confirmed).
  - **transactionId source directly proven:** the `conversion_id` field `claim_conversion_deliveries` returns is byte-identical to `offline_conversions.id` — the transactionId recommendation (§ above) works today with zero code change; this is now an executed fact, not only a code-read inference.
  - Failure → retry → success proven end-to-end: `record_conversion_delivery_result(..., false, ...)` → status `pending → failed`; re-claiming retires that row to `retried` and opens a new `pending` row at `attempt_number = 2`; `record_conversion_delivery_result(..., true, ...)` on the retry → `pending → sent`, `offline_conversion_sent` event emitted exactly once.
  - CRM/business-state independence proven: the originating lead's `lead_status_code` was unchanged (`new`) after a delivery failure — external delivery outcome never touches CRM state.
  - Tenant isolation proven **at the RLS layer**, not just app-layer filtering: queried as `authenticated` (not the bypass-everything `postgres` role) with a real per-tenant JWT claim — tenant B's session saw **0** of tenant A's `offline_conversions` rows.
  - The known canon-vs-implementation event-emission gap (`offline_conversion_send_attempted` / `offline_conversion_retried` never emitted) is **dynamically reconfirmed**: after the proven retry above, both event-type counts were 0. No longer just a static-analysis finding.
- **Gap found during this test — FIXED and DEPLOYED to secondary (owner-approved 2026-08-15, "approve the schema usage grant fix" then "deploy SPEC-122 to Primary and Secondary"):** `orvion_integration` had function-level `EXECUTE` grants on all 4 of its RPCs but was never granted `USAGE` on the `app` schema itself — the first call as that role failed with `permission denied for schema app`. Schema `USAGE` is a prerequisite Postgres privilege independent of function-level grants; without it, the role could not call *any* of its granted functions, regardless of the function-level grants already in place. **Resolved by SPEC-122** (`supabase/migrations/202607050000_grant_orvion_integration_schema_usage.sql`, committed `6fdaa2c` and pushed) plus a permanent guard — `scripts/verify_database.sql` Check 10 — that fails `db reset` if any role holds function `EXECUTE` without schema `USAGE` in any of ORVION's own schemas (`app`/`public`/`reporting`; deliberately excludes Supabase's platform-internal schemas). Verified locally: `supabase test db` (8/8 pgTAP files green), smoke-test green, guard proven load-bearing by a negative-case test.
  - **Deployed to secondary (`brplkqmbzffpxqgkkdzo`) 2026-08-15**, verified live before (`has_schema_privilege('orvion_integration','app','USAGE')` confirmed `false`, matching the local finding) and after (`true`). MCP-assigned version (`20260815183535`) reconciled to `202607050000` — same technique as the 2026-08-10 precedent (`MASTER_CERTIFICATION_STATUS.md`). `list_migrations` re-confirms 89/89 version-for-version and name-for-name.
  - **SUPERSEDED by the entry immediately below (2026-08-15, later the same day) — left as the historical record of that session's blocker, not corrected in place:** ~~NOT deployed to primary (`vrvtsxexkiiiivlkdxzp`) — no `supabase-primary` MCP server was connected in the deploying session... The two projects are now known to differ by exactly one migration... OPEN — requires either a session with the `supabase-primary` MCP server connected, or the owner applying `202607050000` to primary directly.~~
  - **SUPERSEDED — see below:** ~~a `supabase-primary` entry was added to `.mcp.json` using local-stdio + `SUPABASE_ACCESS_TOKEN` (PAT). This makes the connection reproducible, not yet live...~~ That local-stdio/PAT approach was itself replaced the same day by the official Supabase **Remote MCP over OAuth** — see the entry below and `.workstation/manifest.md §4` for the current config.
  - **Deployed to primary (`vrvtsxexkiiiivlkdxzp`) 2026-08-15, owner-approved ("proceed with the next single step: deploy SPEC-122 to Primary"), closing the gap above.** Primary was first reconnected via the official Supabase Remote MCP/OAuth (superseding the local-stdio/PAT approach), authenticated as `platplustours@gmail.com`, and verified live with a real Management API call (`list_migrations`) before any write. Verified before (`has_schema_privilege('orvion_integration','app','USAGE')` confirmed `false`, matching Secondary's pre-deploy state) and after (`true`). MCP-assigned version reconciled to `202607050000` — same technique as Secondary. `list_migrations` re-confirms **89/89 migrations, version-for-version and name-for-name identical between Primary and Secondary — the two projects are synchronized again.** Both `has_schema_privilege(...) = true`, re-verified live on both sides in the same session. **Primary's OAuth connection was not fully reliable this session** — before this successful deployment, `list_migrations`/`list_tables` returned `Unauthorized` twice (once on first authentication, once again after a first disconnect+reauthenticate) while `get_project_url` kept succeeding; a *second* disconnect+reauthenticate cycle then worked with no config change, matching upstream report `supabase/supabase#38926`. **Treat as flaky, not fixed: a future session must independently re-verify with a real data call (e.g. `list_migrations`) before trusting Primary is reachable** — full detail `.workstation/manifest.md §4`. Re-certified in `MASTER_CERTIFICATION_STATUS.md` (CONDITIONAL → CERTIFIED, synchronized).
- **Minor, informational (not a defect):** `postgres` as reached via `docker exec ... psql -U postgres` is **not** a true Postgres superuser locally (`rolsuper = false`), only `rolbypassrls = true` — Supabase's own local-stack hardening. It already holds `authenticated`/`service_role` membership from platform provisioning but needed a temporary, transaction-scoped `GRANT orvion_integration TO postgres` (also rolled back) to exercise that role directly. Relevant for any future session writing local behavioral tests against integration-role-only RPCs.
- **Pre-production state reaffirmed:** Primary and Secondary production tables are empty of real business data; no real GCLID exists anywhere in the system; none should be manufactured. The real-GCLID test (previous entry above) stays on hold until genuine ad-click traffic exists.

**Sources (fetched 2026-08-15):** [Send events | Data Manager API](https://developers.google.com/data-manager/api/devguides/events/send-events) · [Method: events.ingest | Data Manager API](https://developers.google.com/data-manager/api/reference/rest/v1/events/ingest) · [Destination | Data Manager API](https://developers.google.com/data-manager/api/reference/rest/v1/Destination) · [Using Google Ads Data Manager with enhanced conversions for leads](https://support.google.com/google-ads/answer/15707550?hl=en) · [Diagnostics | Data Manager API](https://developers.google.com/data-manager/api/devguides/diagnostics)

**Known implementation gap found during this session's cross-check (not a topology/account conflict — a canon-vs-implementation gap, recorded here as it directly touches the delivery pipeline this section tracks; not fixed, per this task's read-only scope):** `_ORVION_CANONICAL/26_state_machines.md`'s offline-conversion delivery state machine requires 5 events — `offline_conversion_created`, `offline_conversion_send_attempted`, `offline_conversion_sent`, `offline_conversion_failed`, `offline_conversion_retried` — and all 5 are registered in the `event_type` catalog (migration `202607049100`). The implementation (migration `202607049200`) emits only 3: `created`, `sent`, `failed`. `offline_conversion_send_attempted` is never emitted, and the failed→retried transition inside `app.claim_conversion_deliveries` updates `delivery_status_code` directly without calling `app.record_event`, so `offline_conversion_retried` is also never emitted. Not blocking Phase-A validation; flagged for a future CR under the existing discovery-to-guard duty (`AGENTS.md §2`), not actioned here.
