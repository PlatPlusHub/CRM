# ORVION MASTER SURFACE DISPOSITION

Status: **Permanent cumulative assurance record — the SSOT for one fact and one only: what audit disposition each database surface has been given, by which session, and what remains.** Never recreate; evolve. One row per `public` table. It owns no findings (those are `MASTER_GAP_REGISTER.md`), no narrative (that is the named session report), no schema (that is the catalog and `MASTER_DOMAIN_CATALOG.md`), and no test inventory (that is `supabase/tests/**`). Every cell that is not the disposition itself is a **pointer**, deliberately, so nothing here can drift from a fact it restates. Cross-reference: `MASTER_EXECUTION_PLAN.md` Batch 6 exit criteria **EC-1**, which this file exists to satisfy.

Last updated: 2026-09-26 (**slice 21 — `marketing_campaigns` selected by Exposure 10, coverage 26; runner-up `passengers` at 10/70.** Coverage **21 → 22 of 77**. CAMP-3: an `owner` at `aal1` holding MANAGE_MARKETING_CAMPAIGN but lacking step-up was refused by the campaign RPC and wrote the same campaign through the table, because the RLS WITH CHECK charged permission and not step-up. SPEC-222 attaches the existing `app.guard_write_capability` with one added map arm. CAMP-4 (no event on direct creation or direct status moves) stays OPEN; ENTRY-1 applies to this table and stays separately owned.)

Previously: 2026-09-25 (**slice 20 — `payments` selected by Exposure 11, coverage 84; runner-up `marketing_campaigns` at 10/26.** Coverage **20 → 21 of 77**. PAY-2: an employee who could see a payment but held no RECORD_PAYMENT changed its direction, customer or currency by direct UPDATE, moving 100 EGP out of the customer's paid amount and into outstanding balance, because `app.guard_financial_capability` charged the capability on `amount` alone. SPEC-221 extends that existing guard's `payments` UPDATE list to the payment's economic identity; no trigger, grant or policy was added. PAY-3 (direct creation emits no payment event) and PAY-4 (a zero-value row coded `customer_refund` is accepted) remain OPEN, so this surface is AUDITED-OPEN / ADVERSARIAL. SPEC-221 is the evidence artifact. Prior entry follows.)

Previously: 2026-09-25 (**slice 19 — `conversations` selected by Exposure 11, coverage 66; `payments` runner-up at 11/84.** Coverage **19 → 20 of 77**. SPEC-220 closes CHAT-1 and this surface's ENTRY-1 instance: authorized direct INSERT could begin closed or name another user as owner, exposing the row through owner-based RLS, and emitted no `conversation_started` timeline event. A conversation-local guard derives actor and placement; a single AFTER INSERT emitter covers direct and RPC creation. The separate direct UPDATE lifecycle gap CHAT-2 remains OPEN, so this surface is AUDITED-OPEN / ADVERSARIAL. SPEC-220 is the evidence artifact. Prior entry follows.)

Previously: 2026-09-24 (**slice 18 — `customer_contact_methods` selected by Exposure 11, coverage 42; runner-up `conversations` at 11/66.** Coverage **18 → 19 of 77**. CM-3: an authorized direct INSERT created a contact with no `customer_contact_added` event, so the customer timeline omitted it. SPEC-218 gives both the direct and RPC doors one event via a contact-local AFTER INSERT trigger, with verified actor, payload and server event time. The existing capability, tenant, catalog, subscription, normalization, uniqueness, primary-per-channel and merge controls still hold; DELETE has no authenticated grant. `is_verified` and contact-row `created_at` accepted caller values, but no current function, view or policy consumes them as proof, so no new policy was invented. The row is AUDITED / ADVERSARIAL; SPEC-218 is the evidence artifact. Prior entry follows.)

Previously: 2026-09-24 (**slice 17 — `refunds` chosen by Exposure 11, coverage 34; runner-up `customer_contact_methods` at 11/42.** Coverage **17 → 18 of 77**. SPEC-217 closes RFD-1 (an employee without RECORD_REFUND changed a completed refund direction and shifted the computed customer balance by 25,000 EGP), RFD-2 (zero amount and caller-authored completion evidence), and the `refunds` instance of ENTRY-1 (born completed). A refund-only BEFORE guard preserves both customer and supplier paths, status validity, catalog, tenant and subscription controls. RFD-3 remains OPEN: legal authorized direct transitions still emit no refund event, so this row is AUDITED-OPEN. SPEC-217 is the evidence artifact; no session report was written. Prior entry follows.)

Previously: 2026-09-24 (**slice 16 — `documents`, whose table door could give a document a state its own RPCs never could.** Coverage **16 → 17 of 77**. Selected by `scripts/batch6_select_target.ps1` (Exposure 13, first; runner-up `refunds` at 11). `20260924140000` closed **DOC-4** (Medium — an `employee` refused a legal hold by UPDATE created a document born held in the owner's name, backdated and unrecorded, or carrying a forged release), **DOC-6** (Medium — a `finance_manager` without MANAGE_TENANT_SETTINGS retyped the pending subscription payment proof out of its class and replaced its file: PP-4's unmeasured half), **DOC-5** (Low — the version pointer could name another document's version or nothing) and **DOC-LC-3** (Medium — `archived/false`, its decided constraint), and with them the `documents` instances of ARCH-2 and ENTRY-1. One guard in `app.guard_quotation_integrity`'s shape plus one constraint. **INCIDENTAL DEFENSE ≠ INTENTIONAL CONTROL, once more:** the first retype of the proof was refused by `scope_isolation`'s WITH CHECK because the row left the actor's view; retyped to another financial type it succeeded. Recorded `PARTIAL`, not `AUDITED`: event parity for a door-created document and a caller-supplied `created_at` were probed but deliberately not classified, so they are named in the row's Next cell instead of being claimed. SPEC-216 is the evidence artifact; no session report was written. Prior entry follows.)

Previously: 2026-09-24 (**slice 15 — `suppliers`, whose table door created a payee and recorded nothing.** Coverage **15 → 16 of 77**. Selected by `scripts/batch6_select_target.ps1` (Exposure 13, first on the coverage tie-break over `documents`). `20260924130000` closed **SUP-5** (Medium — a `senior_employee` created a supplier by direct INSERT, backdated to 2019, and no event or column recorded who did it, while the same act through `app.create_supplier` was recorded). One AFTER INSERT trigger is now the single producer of `supplier_created`, USR-1's shape; `app.record_event` owns the actor and the time. **What held is recorded as deliberately as what did not:** tenant isolation, archive authority and attribution, MFA parity between the doors, and the name rules and internal-supplier columns, each classified in the row's Next cell. Recorded `AUDITED-OPEN` because ARCH-2's `suppliers` instance stays open with ARCH-2. SPEC-215 is the evidence artifact; no session report was written. Prior entry follows.)

Previously: 2026-09-24 (**slice 14 — `quotations`, whose table door could give a quotation a state or a commercial term its own lifecycle never could.** Coverage **14 → 15 of 77**. Selected by `scripts/batch6_select_target.ps1` (Exposure 14, first). `20260924120000` closed **QUO-5** (High — a user DENIED acceptance created a quotation born `accepted`, with no lines and no event, and a booking came from it: ENTRY-1's third instance), **QUO-6** (High — a SENT quotation's customer and an ACCEPTED one's currency were rewritten, and the booking followed the rewritten customer), **QUO-7** (Medium — the header total stood against its lines, QUO-1's other half) and **QUO-8** (Low — an empty draft sent directly). One guard, `app.guard_invoice_integrity`'s shape. Recorded `PARTIAL`, not `AUDITED`: four axes were probed but deliberately not classified, so they are named in the row's Next cell instead of being claimed. SPEC-214 is the evidence artifact; no session report was written. Prior entry follows.)

Previously: 2026-09-23 (**slice 13 — `offline_conversions`, whose cross-tenant idempotency key a signed-in session could write.** Coverage **13 → 14 of 77**. Selected by `scripts/batch6_select_target.ps1` (Exposure 14, first on the coverage tie-break). `20260923120000` closed **CONV-6** (High — a tenant owner writing `source_event_seq` made another tenant's conversion vanish silently and its CONV-1 deferral close as mapped). Recorded `PARTIAL`, not `AUDITED`: the provenance and tenant axes were swept, and two others were probed but deliberately not classified, so they are named in the row's Next cell instead of being claimed. SPEC-213 is the evidence artifact; no session report was written. Prior entry follows.)

Previously: 2026-09-20 (**slice 12 CERTIFIED — the audit stands, and the evidence pointer now names a contract that completed.** Coverage unchanged at **13 of 77**: no row is added or removed, because the audit itself was genuinely performed and recorded when the repair was deployed. What changed is the `Session` cell, which pointed at `SPEC-203` — a contract that deployed `20260920120000` to Primary, proved USR-1 and USR-2 with 43 assertions, and was then **Cancelled** because a mandatory verification regenerated a generated document its own frozen Write Scope excluded. A permanent assurance record whose evidence pointer names a contract stating it could not certify invites a needless re-audit, so the cell moves to the contract that did certify, and `SPEC-203` stays named inside the row as where the deployment and adversarial evidence lives. Nothing about the surface's assessment changed: disposition stays `AUDITED-OPEN` because **USR-4** is still open, assurance stays `ADVERSARIAL`, and the findings cell is untouched. Prior entry follows.)

Previously: 2026-09-20 (**slice 12 — `public.users`, the row that decides who exists in a tenant and whether they can act, and the only member of the canon-34 identity-and-access family with no emitter at all.** Coverage **12 → 13 of 77**. `20260920120000` closed **USR-1** (High — one actor created a membership, deactivated a colleague and unbound a third human by direct DML and the tenant’s event count stayed at 2 → 2, while the same `user_created` fact through `app.create_tenant_user` moved it 2 → 3: the RPC-only-producer shape `202607055100` had already removed from `assign_user_role`, left standing one table over) and **USR-2** (High — `app.authorize` is permission AND MFA, the four RLS policies call `app.has_permission`, which is permission only, so at `aal1` the RPC refused `42501` while the equivalent direct INSERT, a direct deactivation and a direct identity re-point all succeeded in the same transaction — the cheaper route was the destructive one). **IDENT-2 closed by the same emitter**, because the claim moves the column that emitter observes. **The repair is TWO triggers and the reason is measured, not stylistic:** `is_active` is an input to `app.current_user_id()` and therefore to permission resolution, so an authority check evaluated AFTER would refuse an administrator their own exit; authority is a precondition and runs BEFORE, the record is a fact and runs AFTER. **What HELD is recorded as deliberately as what did not:** tenant isolation on both insert and relocation, `users_enforce_identity_binding`, the session-less provisioning path, and the ordinary employee self-claim — the last pinned with an `employee`-role claimant rather than a CEO fixture that would have hidden the regression it exists to catch. **The slice’s headline is again a method finding: INCIDENTAL DEFENSE ≠ INTENTIONAL CONTROL.** BEFORE triggers fire ALPHABETICALLY, so `users_enforce_identity_binding` refuses the carve-out attack `23514` before `users_guard_membership_authority` is reached; counting those refusals would have measured the wrong control, exactly as slice 8 did on `leads`. Removing that incidental defence inside a savepoint and re-running the same attacks yields `42501` **from the authorization model itself** (`118_...` 11-15). `AUDITED-OPEN` because **USR-4** stays open by choice: `is_platform_user` is settable by a tenant administrator and is read by no function, policy or tracked consumer, so it is DEFER WITH TRIGGER — the trigger being the first reader — and deliberately not an owner decision. **USR-3 is deliberately NOT absorbed:** `user_permission_grants` and `user_branch_assignments` are different surfaces and their gap is step-up only, because the permission-grant door already emits. Prior entry follows.)

Previously: 2026-09-09 (**slice 11 — `customers`, the CRM identity anchor whose merge, credit authority and threshold paths were tested together.** Coverage **11 → 12 of 77**. `202607062000` closed **INVOICE-1** (High — an amount is a PAIR and `app.guard_financial_capability` named one half of it: an `employee` holding NO financial permission, visible to the row only because they own the booking it hangs off, ran `set currency_code = 'USD'` and `app.customer_balance` reported 50,000 **USD** where it had read 50,000 EGP, with `total_amount` never moving and no guard firing), **INVOICE-2** (High — `customer_id`, `booking_id`, `invoice_number` and `invoice_date` all writable at the table door by the same actor), **INVOICE-3** (`corrects_invoice_id` forgeable), **INVOICE-4** (an invoice could be BORN `paid`, or `voided` with no reason and no timestamp — LEAD-2's shape on a financial table), **INVOICE-5** (the total of a FULLY PAID invoice raised 50,000 → 90,000, leaving a gap `app.record_payment` can never close), **INVOICE-6** (archive attribution caller-supplied on **thirteen** tables under a migration header stating the opposite — IDENT-1's class) and **INVOICE-7** (one invoice renamed `INV-2026-abcd` stopped `app.create_invoice` for the entire tenant and year). **What HELD is recorded as deliberately as what did not:** the amount, the status machine (`202607060200`), archive authority, the tax-submission column, cross-tenant INSERT, duplicate numbering, replayed issuance, and concurrent numbering — the last proven with two genuinely overlapping sessions rather than by reading the advisory lock. **Two incidental defences were named rather than credited:** detaching the invoice from its booking failed on RLS *visibility*, not authority (re-parenting onto another booking the same actor owned succeeded), and `invoices_corrects_not_self_check` is a coherence CHECK, not an authority control. **68 assertion 7 is preserved, not overturned** — the financial guard stays column-scoped and `due_date`, which nothing reads, still updates; only the consequence SET moved, and only where a reader was measured. `AUDITED`: nothing on this surface is left open. Prior entry follows.)

Previously: 2026-09-08 (**slice 9 — `leads`, the door that let the attacking statement answer the question that authorized it.** Coverage **9 → 10 of 77**. `202607061900` closed **LEAD-1** (High — **BOOK-5's shape a THIRD time and the widest yet**: `app.guard_write_capability` decided a FULL bypass, `if v_relationship_ok then return new`, from `new.assigned_user_id` — a column the attacking statement supplies — so naming yourself in the SET list skipped every permission, `app.authorize` call and MFA check for every other column in the same statement), **LEAD-2** (a lead could be BORN `won`: the state machine is BEFORE UPDATE and has no entry arm, while `app.create_lead` hardcodes `'new'`), **LEAD-3** (`converted` with `customer_id` NULL, which `app.convert_lead` refuses) and **LEAD-4** (`assigned → contacted` asserted by hand with no interaction — the one edge NO RPC offers, and taking it removes the lead from `app.process_lead_sla`'s working set). **The slice's headline is a method finding: INCIDENTAL DEFENSE ≠ INTENTIONAL CONTROL.** Slice 8 recorded the seize-plus-transition here as a VERIFIED NON-DEFECT on a 23514 from `leads_owner_matches_assignee_chk`, measured with ONE column in the SET list; re-run with the PAIR that constraint couples, the constraint is satisfied and the refusal comes from `app.require_assignment_history`, a canon-04 timeline-completeness control. **With BOTH removed inside a transaction the attack SUCCEEDED**, so two unrelated mechanisms had been holding an open authorization model shut. Repaired by reading `old` and charging ASSIGN_LEAD/REASSIGN_LEAD to move an assignment, both copied verbatim from the two RPCs that do it — then re-measured with the incidental defences STILL removed and refused **42501 by the authorization model itself** (`109_...` 15-18). `AUDITED`, not `AUDITED-OPEN`: nothing on this surface is left open, though **LI-3** remains open on `lead_interactions` and its evidence was corrected here — it named `last_contact_at` as the SLA's driver and NO function, view or policy reads that column at all. Prior entry follows.)

Previously: 2026-09-08 (**slice 8 — `lead_interactions`, the contact log that could be moved onto another lead and rewritten.** Coverage **8 → 9 of 77**. The inherited note (`202607056100` §3) said the RPC "authorizes nothing", so direct DML could not bypass it. **Both halves were stale, in opposite directions.** The AUTHORITY half had since been fixed by a trigger nobody re-read: `guard_lead_interaction_authority` charges the same assigned-handler-or-ASSIGN_LEAD-plus-MFA the RPC does, and a non-handler `employee` at `aal1` was refused **42501** on both doors — asserted rather than assumed (2-6). The half that was never true is what the RPC MAINTAINS. `202607061800` closed **LI-1** (High — **BOOK-5's shape on a second table**: the guard asks who handles `new.lead_id`, a column the attacking statement supplies, and never asks about `old.lead_id`, so a handler re-parented a colleague's private interaction onto their own lead, still attributed to the colleague) and **LI-2** (Medium — the log was rewritable: `interaction_at` backdated to 2001-01-01 and `summary` replaced). **One revoke closes both**, and it is not a new mechanism: NO function in the database updates this table, so the UPDATE grant had no sanctioned writer, which is `202607056100`'s own stated preference and the rule `75_...` assertion 25 now makes executable. INSERT is KEPT because the RPC is SECURITY INVOKER — the `trusted_devices` lesson, not the `otp_challenges` one — and assertions 9-10 CALL it to prove so. **A sibling instance of BOOK-5's shape was swept and cleared:** `enforce_status_transition` authorizes a `leads` transition on `to_jsonb(new) ->> 'assigned_user_id'`, and seizing plus transitioning in one statement is refused **23514** by `leads_owner_matches_assignee_chk` — a VERIFIED NON-DEFECT, pinned (11-12) because the defence is incidental rather than deliberate. `AUDITED-OPEN` because **LI-3** stays open by choice: a direct-DML qualifying interaction does not set `last_contact_at`, does not walk `assigned → contacted` and emits no event, and assertion 14 is written to FAIL when that is repaired. Prior entry follows.)

Previously: 2026-09-08 (**slice 7 — `trusted_devices`, the device record its own subject could rewrite.** Coverage **7 → 8 of 77**. Derived, not ranked: it was the last actionable member of PAX-4's corrected five-table set, and the only canon-34 table whose write grant is LOAD-BEARING — `app.record_trusted_device` is SECURITY INVOKER, so the inherited BOOK-3 question was whether direct DML bypasses what the RPC charges. **It did.** `202607061700` closed **TD-1** (the seeded `trusted_device_status` vocabulary was enforced by nothing; `SUPER_TRUSTED_FOREVER` stored verbatim at `aal1`) and **TD-2** (`verified_at`/`first_seen_at`/`created_at` rewritable by the subject, defeating the RPC's own first-verification-wins rule). **The grant was KEPT and the repair is two doors** — the deliberate opposite of OTP-1's revoke one migration earlier, because copying it would have broken the SECURITY INVOKER path; assertions 11-13 CALL all three RPCs rather than inspecting them. **The boundaries that held are asserted, not assumed:** a device cannot be planted on another human, another's row is invisible, a row cannot be stolen by moving `auth_user_id` — the last verified against ground truth READ AS THE OWNER, because that refusal is a silent no-op. **Why this is AUDITED-OPEN rather than AUDITED:** `trusted_devices` is a RECORD, not a GATE, and that single condition is what keeps TD-2's residue and TD-3's non-durable revocation at integrity instead of privilege. Assertion 15 names the table's entire mention set by membership, so wiring device trust into an authorization path fails there first. Prior entry follows.)

Previously: 2026-09-07 (**slice 6 — `otp_challenges`, the challenge its own subject could answer.** Coverage **6 → 7 of 77**. The slice was **not selector-chosen and the selector had stopped discriminating** — every remaining candidate scores negative — so the target was taken on a stated criterion instead: the only `NOT-RECORDED` surface carrying both PII and an unguarded write path. **It refuted the premise it was chosen under.** The manifest predicted "correctly unguarded — it is pre-authentication"; measured, `anon` holds no grant and matches no policy on this table, so an unauthenticated caller could never reach it, and canon 34 § 7 makes OTP a *second factor* presented at `aal1`. The surface is INTRA-authentication, and that is exactly why it was reachable by the one actor it exists to interrogate. `202607061600` closed **OTP-1** (High, latent): `authenticated` held INSERT and UPDATE with RLS scoped to `auth.uid()` and **no trigger at all**, so the subject could self-verify an expired challenge, zero its own attempt counter, extend its own expiry, redirect its delivery address and forge a pre-verified row — every one reproduced at `aal1` before a line was written. **The repair is a REVOKE, not a capability trigger**: canon 34 puts these tables outside tenant RBAC, so the right boundary is `202607056100`'s existing one, and the blast radius is provably nil because ZERO functions reference the table. `AUDITED-OPEN` because **OTP-2** — MFA self-disable at `aal1` on `totp_enrollments` — is a security-policy decision canon does not answer, and the two tables are deliberately **not** treated as one family: `trusted_devices` keeps its grant because `app.record_trusted_device` is SECURITY INVOKER and depends on it. Also this session, and not the target: **PAX-4 CORRECTED** — 15 of the 20 tables it counted as "no capability trigger" carry the check in an RLS policy instead, so the uncovered set is **five, not twenty**; the row measured enforcement sites of one kind and was read as governance. Two class guards were narrowed by the repair and **re-expressed as membership rather than counts** (`67_...` assertion 7, `83_...` assertion 22-class), because a bare count cannot see one table leaving as another joins.)

Previously: 2026-09-07 (**slice 5 — `booking_items`, the booked service that had no door of its own.** Coverage **5 → 6 of 77**. This slice was **NOT selector-chosen**: slice 4 had reproduced a High finding on it (**BOOK-3**) before its sweep began, and a reproduced defect outranks a ranking. The sweep then found four more, and the ranking's own blindness is why: the selector SUBTRACTS test coverage, and `booking_items` is one of the best-covered tables in the repository. **BOOK-5 is the finding worth carrying forward** — a guard that charged the correct permission unconditionally on every path was still defeated, because it asked "is this item mine?" of the post-image the attacking statement had just supplied. Nothing textual can measure that; only a discriminating pair found it. Also this session, and NOT part of the target: **GOV-19** (a register row that contradicted itself for fourteen days) and **GOV-16** (**Check 25**), which measured that seven registered decisions were invisible on the manifest's boot line. `MASTER_GAP_REGISTER.md` owns all nine.)

Previously: 2026-09-07 (**slice 4 — `booking_item_passengers`, and the ceiling that had been crediting it.** Coverage **4 → 5 of 77**. The slice was chosen by `scripts/batch6_select_target.ps1` (score -2; exposure 10, coverage 12 — the highest-ranked `NOT-RECORDED` surface) and closed **PAX-1**, **PAX-2** and **PAX-3** via `202607061400`. The disposition is `AUDITED-OPEN` rather than `AUDITED` because two findings remain and **neither is engineering**: **PAX-5** is an owner decision (when does a manifest become immutable) and **PAX-6** a canon one (no event vocabulary exists for a manifest change, so the swap is now authorized but still not audited). **The row that matters most is not this one:** `booking_items` gains a `Next` note carrying **BOOK-3**, PAX-1's defect on the parent table, found by root-causing why the SEC-1 ceiling had rated this surface protected — it CREDITED a conditional guard (**MEAS-2**). `MASTER_GAP_REGISTER.md` owns all eight.)

Previously: 2026-09-06 (**slice 3 — `company_assets`, and a residue found in a row that already read AUDITED.** Coverage **3 → 4 of 77**. The slice was chosen by `scripts/batch6_select_target.ps1` and closed **CA-1**, **CA-2** and — by attacking its own draft repair — **MONEY-1**, a class defect in every money CHECK in the repository. It also found that slice 1 left `campaign_daily_metrics` able to record spend denominated in nothing (**CDM-3**); that row keeps its `AUDITED` disposition and gains the finding, because a disposition records what was assessed and the currency question was not asked. `MASTER_GAP_REGISTER.md` owns all five.)

Previously: 2026-09-05 (**created — EC-1.** Batch 6's exit criteria named a per-surface disposition record as the first thing that had to exist, because without one "how much has been audited?" is unanswerable: all 77 tables are named somewhere in `reports/**`, so *mention* is saturated and proves nothing, and 75 of 77 are named in some pgTAP file, which is a floor and not coverage. Seeded with all **77** surfaces and the first slice's **2**.)

---

## How to read this file

**`NOT-RECORDED` is a statement about THIS RECORD, not about the surface.** It means no Batch 6 disposition has been recorded for that table. Many of these surfaces have been inspected, repaired and guarded by closed packages in `reports/history/` — `notification_deliveries` on 2026-09-05, `subscription_payment_proofs` on 2026-08-27, `lead_interactions` on 2026-08-28 — and none of that work is erased by a `NOT-RECORDED` row. What the row says is that the surface has **not been assessed against Batch 6's exit criteria**, which is a different and stricter question than "has anyone ever looked at it". Converting a row requires the assessment, not a memory of the earlier work.

**Disposition** — one of five, and the vocabulary is closed so `Check 22` can validate it:

| Value | Means |
|---|---|
| `NOT-RECORDED` | no Batch 6 disposition recorded (see above — not a claim that nothing was ever done) |
| `AUDITED` | swept against the EC checklist; nothing open for this surface |
| `AUDITED-OPEN` | swept; one or more findings remain open, each with a row in the gap register |
| `PARTIAL` | some axes swept and the unswept ones named in `Next` |
| `EXEMPT` | deliberately out of Batch 6 scope, with the reason in `Next` |

**Assurance** — one of three, and it is **not** a synonym for "has tests" (`MASTER_EXECUTION_PLAN.md` §10):

| Value | Means |
|---|---|
| `—` | nothing recorded |
| `TESTED` | functional coverage exists and passes |
| `ADVERSARIAL` | specific attempts were made to violate this surface's assumptions and it resisted them — negative controls, not only positive ones |

There is deliberately **no `EXHAUSTIVE` value.** Exhaustive adversarial audit is a claim about the *programme*, not about one row, and it requires every row to hold at `ADVERSARIAL` at once. Making it a per-surface label would let the strongest claim in ORVION be asserted one table at a time without anyone ever checking the whole.

**Session** — the immutable evidence artifact that OWNS the recorded audit evidence: normally the session report that performed the work, and a terminal Change Request where the repository deliberately wrote no report, as `SPEC-160` already did. `Check 22` requires the file to exist and resolves it through a repository-wide basename index, so either kind of artifact resolves identically. **Findings** — ids only; `Check 22` requires each to resolve in `MASTER_GAP_REGISTER.md`, which owns their status. **Next** — the exact remaining action for this surface, or `—`.

---

## Coverage

**22 of 77 recorded · 7 `AUDITED` · 12 `AUDITED-OPEN` · 3 `PARTIAL` · 0 `EXEMPT` · 55 `NOT-RECORDED`.**

**All 22 recorded surfaces stand at `ADVERSARIAL`, and Check 24 (ADV-1) is what makes that word cost something**: a surface may not carry it unless a pgTAP file names it, declares which attack classes it aimed at (`-- ATTACK-CLASSES:`, closed vocabulary), and carries at least one negative assertion. A file of positive controls earns `TESTED` and no more.

This count is the honest one and it is meant to be uncomfortable. It replaces two numbers that read better and mean less: "all 77 tables appear in the reports" (true, and worthless — mention is not audit) and "75 of 77 tables appear in a pgTAP file" (true, and a floor — a table named once in an unrelated fixture is not a swept surface).

| Surface | Disposition | Assurance | Session | Findings | Next |
|---|---|---|---|---|---|
| `approval_requests` | NOT-RECORDED | — | — | — | — |
| `attribution_clicks` | NOT-RECORDED | — | — | — | — |
| `booking_item_passengers` | **AUDITED-OPEN** | ADVERSARIAL | `session-2026-09-07-batch6-slice4-booking-item-passengers` | PAX-1, PAX-2, PAX-3, PAX-5, PAX-6 | PAX-5 (owner: manifest immutability after issuance) and PAX-6 (canon: no event vocabulary for a manifest change) — both decisions, neither engineering |
| `booking_items` | **AUDITED-OPEN** | ADVERSARIAL | session-2026-09-07-batch6-slice5-booking-items | BOOK-8, BOOK-9 | `105_booking_item_service_door_test.sql` — **44 assertions, all ten attack classes, none N/A.** `202607061500` closed **BOOK-3** (the bare booked service, and its whole UPDATE half: service type, currency, ownership and re-parenting were all free), **BOOK-4** (a cross-tenant existence-and-state oracle, PAX-3 one table up), **BOOK-5** (the canon-28 scope check read the attacker's own post-image), **BOOK-6** (the finance execution gate was clearable) and **BOOK-7** (a priced item could be redenominated). `AUDITED-OPEN` rather than `AUDITED` because two remain and **neither is engineering**: **BOOK-8** is a business decision (may a finance-approval requirement be withdrawn) and **BOOK-9** a canon one (booking items have no reassignment permission where leads have REASSIGN_LEAD) |
| `bookings` | NOT-RECORDED | — | — | — | — |
| `branch_business_hours` | NOT-RECORDED | — | — | — | — |
| `branches` | NOT-RECORDED | — | — | — | — |
| `campaign_daily_metrics` | **AUDITED** | ADVERSARIAL | `session-2026-09-05-batch6-evidence-foundation` | CDM-1, CDM-2, CDM-3 | — |
| `catalog_types` | NOT-RECORDED | — | — | — | — |
| `catalog_values` | NOT-RECORDED | — | — | — | — |
| `chart_of_accounts` | NOT-RECORDED | — | — | — | — |
| `company_assets` | **AUDITED** | ADVERSARIAL | `session-2026-09-06-batch6-slice3-company-assets` | CA-1, CA-2, MONEY-1 | — |
| `complaints` | NOT-RECORDED | — | — | — | — |
| `conversation_messages` | NOT-RECORDED | — | — | — | — |
| `conversations` | **AUDITED-OPEN** | ADVERSARIAL | `SPEC-220-conversation-creation-parity` | CHAT-1, CHAT-2, ENTRY-1 | `125_conversation_creation_parity_test.sql` proves a real SEND_MESSAGE actor's direct and RPC open starts, actual owner/placement despite false owner input, one event and timeline entry per row, refused terminal/preclosed entry, capability, catalog and replay refusals, and valid sessionless creation with null actor. A colleague in another branch cannot read the attempted forged-owner row. SPEC-220 closes CHAT-1 and this table's ENTRY-1 instance; **CHAT-2 remains OPEN:** legal direct UPDATE reopen can lack event/reason and retain `closed_at`. No authenticated DELETE grant; CONV-3 inbound integration remains deferred. |
| `countries` | NOT-RECORDED | — | — | — | — |
| `currencies` | NOT-RECORDED | — | — | — | — |
| `customer_contact_methods` | AUDITED | ADVERSARIAL | SPEC-218-customer-contact-event-parity | CM-3 | — |
| `customer_identity_merges` | NOT-RECORDED | — | — | — | — |
| `customer_identity_signals` | NOT-RECORDED | — | — | — | — |
| `customer_notes` | NOT-RECORDED | — | — | — | — |
| `customers` | AUDITED | ADVERSARIAL | `session-2026-09-09-slice11-closure` | CUST-7, CUST-8, CUST-9, CUST-10, CUST-11, CUST-12 | — |
| `departments` | NOT-RECORDED | — | — | — | — |
| `document_links` | NOT-RECORDED | — | — | — | — |
| `document_retention_policies` | NOT-RECORDED | — | — | — | — |
| `document_storage_findings` | NOT-RECORDED | — | — | — | — |
| `document_versions` | NOT-RECORDED | — | — | — | — |
| `documents` | **PARTIAL** | ADVERSARIAL | `SPEC-216-document-door-parity` | DOC-4, DOC-5, DOC-6, DOC-LC-3 | The table door's entry state, type, version pointer and archive representations were swept and **DOC-4**, **DOC-5**, **DOC-6** and **DOC-LC-3** closed: `122_document_door_parity_test.sql` pins them with 25 assertions — an `employee` holding neither ARCHIVE_DOCUMENT nor MANAGE_TENANT_SETTINGS, a `finance_manager` holding no MANAGE_TENANT_SETTINGS, and a pointer attack aimed at a version the caller CAN see, so no permission or visibility is the refuser — and `69_…` 18-19 now assert DOC-LC-3's refusal. The same guard closed the `documents` instances of **ARCH-2** (a document born archived) and **ENTRY-1** (born `archived` or `superseded`). **Swept and classified as non-defects, not registered:** tenant relocation (refused by `scope_isolation`); `created_by` (derived and immutable, `61_…`); confidentiality and title (editable by an actor who already holds the file, so no escalation; a `payment_proof` stays under MANAGE_TENANT_SETTINGS for every edit); both doors agreeing at `aal1`; no DELETE grant; the subscription gate on every door. **`PARTIAL` because two axes were probed and deliberately not classified:** (1) event parity for a document created at the door — it records no `document_uploaded`, while its creator is server-derived and it carries no file until a recorded version is added (`quotations` names the same axis); (2) `created_at`, writable at the door and read only by `app.financial_documents`' listing, a question that stands on every table with such a column |
| `events` | NOT-RECORDED | — | — | — | — |
| `exchange_rate_adjustments` | **AUDITED** | ADVERSARIAL | `session-2026-09-05-batch6-evidence-foundation` | ERA-1 | — |
| `exchange_rates` | NOT-RECORDED | — | — | — | — |
| `feature_entitlements` | NOT-RECORDED | — | — | — | — |
| `financial_accounts` | **AUDITED-OPEN** | ADVERSARIAL | `session-2026-09-05-batch6-adversarial-program` | FA-1, FA-2 | FA-2 is an owner question: may a bank account receive a payment in another currency? |
| `holidays` | NOT-RECORDED | — | — | — | — |
| `integration_cursors` | NOT-RECORDED | — | — | — | — |
| `internal_supplier_links` | NOT-RECORDED | — | — | — | — |
| `invoices` | **AUDITED** | ADVERSARIAL | `session-2026-09-08-batch6-slice10-invoices` | INVOICE-1, INVOICE-2, INVOICE-3, INVOICE-4, INVOICE-5, INVOICE-6, INVOICE-7 | — |
| `journal_entries` | NOT-RECORDED | — | — | — | — |
| `journal_entry_lines` | NOT-RECORDED | — | — | — | — |
| `languages` | NOT-RECORDED | — | — | — | — |
| `lead_assignments` | NOT-RECORDED | — | — | — | — |
| `lead_interactions` | AUDITED-OPEN | ADVERSARIAL | `session-2026-09-08-batch6-slice8-lead-interactions` | LI-1, LI-2 | LI-3 open by choice — the door enforces the RPC's authority and none of its derived state; `108_...` assertion 14 fails when that is repaired |
| `leads` | **AUDITED** | ADVERSARIAL | `session-2026-09-08-batch6-slice9-leads` | LEAD-1, LEAD-2, LEAD-3, LEAD-4 | `109_lead_authority_and_lifecycle_test.sql` — 20 assertions, eight attack classes, two declared `N/A` with reasons. `202607061900` closed **LEAD-1** (High — BOOK-5's shape a third time, and the widest: `guard_write_capability` decided a FULL bypass from `new.assigned_user_id`, a column the attacker supplies), **LEAD-2** (a lead could be born `won`), **LEAD-3** (`converted` with no customer) and **LEAD-4** (`assigned → contacted` by hand, which removes the lead from `process_lead_sla`'s working set). `AUDITED` rather than `AUDITED-OPEN`: nothing on this surface is left open. The slice's own headline is a method finding — **two INCIDENTAL DEFENCES had been standing in for the authorization model**, and assertions 15-18 remove both before asking whether the guard holds |
| `marketing_campaigns` | **AUDITED-OPEN** | ADVERSARIAL | `SPEC-222-marketing-campaign-step-up-parity` | CAMP-3, CAMP-4 | `127_marketing_campaign_step_up_parity_test.sql` proves an `aal1` owner who holds MANAGE_MARKETING_CAMPAIGN is refused step-up on a direct INSERT and a non-status UPDATE with the campaign unchanged, a non-holder is refused by the guard's own message, `aal2` direct and RPC paths, status authority and the platform path are unchanged, and an installed/restored mutation of the attachment lets the same writes land; it pins CAMP-4 and ENTRY-1 as still OPEN. |
| `nationalities` | NOT-RECORDED | — | — | — | — |
| `notification_deliveries` | NOT-RECORDED | — | — | — | — |
| `notifications` | NOT-RECORDED | — | — | — | — |
| `offline_conversion_deliveries` | NOT-RECORDED | — | — | — | — |
| `offline_conversions` | **PARTIAL** | ADVERSARIAL | `SPEC-213-conversion-provenance-is-platform-written` | CONV-6 | Provenance and tenant isolation were swept and **CONV-6** closed: `source_event_seq` is platform-written, and `119_conversion_provenance_is_platform_written_test.sql` pins it with 16 assertions and an in-file mutation control. Two axes remain unswept by choice and are named here rather than registered, because neither was classified in this slice: signed-in writes by an authorised MANAGE_MARKETING_CAMPAIGN holder to the identity snapshot, value and time after creation (`64_...` pins value updates as intended), and creation-event parity for direct-DML rows |
| `otp_challenges` | **AUDITED-OPEN** | ADVERSARIAL | `session-2026-09-07-batch6-slice6-otp-challenges` | OTP-1, OTP-2, PAX-4 | `106_otp_challenge_subject_authority_test.sql` — 16 assertions, 3 classes declared `N/A` with reasons. `202607061600` closed **OTP-1** (the subject of a challenge could self-verify it, reset its own attempt counter, extend its expiry, redirect its delivery address and forge a pre-verified one — all reproduced at `aal1`). **The slice refuted its own premise:** the manifest predicted "correctly unguarded — pre-authentication", and `anon` holds no grant and matches no policy here, so the surface is INTRA-authentication (canon 34 § 7 makes OTP a second factor). `AUDITED-OPEN` because **OTP-2** is a security-policy decision (MFA self-disable at `aal1` on `totp_enrollments`), not engineering. **PAX-4 is listed because this slice corrected it**, not because it belongs to this table: 15 of its 20 "ungoverned" tables carry the capability check in RLS, so the uncovered set is five |
| `passengers` | NOT-RECORDED | — | — | — | — |
| `payment_allocations` | NOT-RECORDED | — | — | — | — |
| `payments` | **AUDITED-OPEN** | ADVERSARIAL | `SPEC-221-payment-write-capability-parity` | PAY-2, PAY-3, PAY-4 | `126_payment_write_capability_parity_test.sql` proves an employee who sees the payment but lacks RECORD_PAYMENT is refused by the guard's own message on direction, customer, supplier, booking, item, account, currency, method, paid time and rate, with both customers' balances unchanged; same-value assignments, reference edits, the owner's amount correction and a session-less guarded change still land. An installed amount-only mutant lets the same employee's direction, customer and currency rewrites persist and move real balances; the original function is restored byte-identically. **PAY-3 and PAY-4 remain OPEN.** |
| `permissions` | NOT-RECORDED | — | — | — | — |
| `quotation_items` | NOT-RECORDED | — | — | — | — |
| `quotations` | **PARTIAL** | ADVERSARIAL | `SPEC-214-quotation-door-parity` | QUO-5, QUO-6, QUO-7, QUO-8 | The table door's lifecycle state, commercial terms, authority contrast and tenant boundary were swept and **QUO-5** to **QUO-8** closed: `120_quotation_door_parity_test.sql` pins them with 30 assertions, an `employee` carrying DENY on SEND_QUOTATION and ACCEPT_QUOTATION so no permission can be the refuser, and an in-file mutation control. Four axes remain unclassified by choice and are named here rather than registered: (1) event parity for direct-DML creation and transitions (canon 26's Required Events); (2) reassignment of `owner_user_id`, `lead_id` and `quotation_number` by any CREATE_QUOTATION holder who can see the row, which no RPC offers and canon does not decide (BOOK-9's question on another surface); (3) the `quotations` instance of **ARCH-2** (a row born archived), reproduced in this slice and owned by ARCH-2's row; (4) direct writes to `sent_at`, `accepted_at` and `rejected_at`, probed and found to have no reader |
| `receipts` | NOT-RECORDED | — | — | — | — |
| `refunds` | **AUDITED-OPEN** | ADVERSARIAL | `SPEC-217-refund-door-integrity` | RFD-1, RFD-2, RFD-3 | `123_refund_door_integrity_test.sql` proves real-row visibility, absent RECORD_REFUND, unchanged balance after refused direction and currency changes, the RPC's three-event lifecycle, allowed direct customer and supplier paths, the requested positive entry rule, server-derived completion, MFA and tenant boundaries. SPEC-217 closes **RFD-1**, **RFD-2** and the `refunds` instance of **ENTRY-1** with one refund-local guard; catalog, subscription, status-transition and creator-attribution controls remain. **RFD-3 remains OPEN:** a legal authorized direct status progression still emits no refund event; changing event authority while retaining RPC `p_reason` is a separate repair. `authenticated` has no DELETE grant. |
| `role_permissions` | NOT-RECORDED | — | — | — | — |
| `roles` | NOT-RECORDED | — | — | — | — |
| `scheduled_job_findings` | NOT-RECORDED | — | — | — | — |
| `security_events` | NOT-RECORDED | — | — | — | — |
| `service_requests` | NOT-RECORDED | — | — | — | — |
| `subscription_payment_proofs` | NOT-RECORDED | — | — | — | — |
| `subscription_plans` | NOT-RECORDED | — | — | — | — |
| `subscriptions` | NOT-RECORDED | — | — | — | — |
| `suppliers` | **AUDITED-OPEN** | ADVERSARIAL | `SPEC-215-supplier-creation-is-recorded` | SUP-5, ARCH-2 | `121_supplier_creation_is_recorded_test.sql` — 21 assertions and an in-file mutation control. `20260924130000` closed **SUP-5** (Medium — a supplier created at the table door recorded nothing, while the RPC recorded the same act; the table has no creator column, so the event is the only attribution). `AUDITED-OPEN` because **ARCH-2**'s `suppliers` instance — a row born archived by a user without ARCHIVE_RECORD — was reproduced in this slice and stays with ARCH-2's row. **Swept and classified as non-defects, not registered:** tenant relocation and planting (refused by RLS); archive authority and attribution on UPDATE (`app.enforce_archive_authority`); both doors agreeing at `aal1`; a blank name and a case-variant duplicate at the table door (a name is freely editable there and the RPC itself admits a near-variant, so neither changes what an ASSIGN_SUPPLIER holder can do); and `is_internal` / `internal_branch_id` / `internal_department_id`, which nothing reads. Whether an archived supplier may still be assigned new work is a rule no code or canon states, and belongs to `booking_items`' supplier assignment rather than to this table |
| `tasks` | NOT-RECORDED | — | — | — | — |
| `tenant_license_activations` | NOT-RECORDED | — | — | — | — |
| `tenants` | NOT-RECORDED | — | — | — | — |
| `totp_enrollments` | NOT-RECORDED | — | — | — | — |
| `trusted_devices` | AUDITED-OPEN | ADVERSARIAL | `session-2026-09-08-batch6-slice7-trusted-devices` | TD-1, TD-2 | TD-3 resolved by derivation; the surface stays OPEN only because `107_...` assertion 15 pins the condition (record-not-gate) that bounds it |
| `usage_counters` | NOT-RECORDED | — | — | — | — |
| `user_branch_assignments` | NOT-RECORDED | — | — | — | — |
| `user_permission_grants` | NOT-RECORDED | — | — | — | — |
| `user_role_assignments` | NOT-RECORDED | — | — | — | — |
| `users` | **AUDITED-OPEN** | ADVERSARIAL | `SPEC-206-slice-12-closure` | USR-1, USR-2, USR-4 | Deployment and adversarial evidence lives in the terminal `SPEC-203-membership-authority-and-audit`, which deployed the repair and was then Cancelled without certifying it; `SPEC-206-slice-12-closure` is the contract that certified this surface. `118_membership_authority_and_audit_test.sql` — **43 assertions, eight attack classes, two declared `N/A` with reasons.** `20260920120000` closed **USR-1** (High — the only canon-34 identity-and-access table with NO emitter at all: three membership mutations by direct DML left the tenant's event count at 2 → 2, while the same `user_created` fact through the RPC moved it 2 → 3) and **USR-2** (High — `app.authorize` is permission AND MFA, the four RLS policies call `app.has_permission`, which is permission only, so at `aal1` the RPC refused 42501 while the equivalent direct INSERT, deactivation and identity re-point all succeeded). **IDENT-2 closed by the same emitter**, because the claim moves the column that emitter observes. **What HELD is recorded as deliberately as what did not:** tenant isolation on both insert and relocation, `users_enforce_identity_binding`, and the ordinary employee self-claim, which is pinned by an `employee`-role claimant rather than a CEO fixture that would have hidden the regression. **The slice's method finding: INCIDENTAL DEFENSE ≠ INTENTIONAL CONTROL, a second time.** BEFORE triggers fire alphabetically, so `users_enforce_identity_binding` refuses the carve-out attack `23514` before the authority check is ever reached; reading those refusals as proof the guard holds would have measured the wrong control, so the incidental defence is REMOVED inside a savepoint and the same attacks re-measured as `42501` from the authorization model itself. `AUDITED-OPEN` because **USR-4** remains open — `is_platform_user` is settable and read by nothing — classified **DEFER WITH TRIGGER**, whose exact trigger is the first time any policy, function or application path reads that column. No owner decision is owed for it: nothing reads the column, so no business or compliance question has been reached. **USR-3 is NOT on this row** — `user_permission_grants` and `user_branch_assignments` are separate surfaces, and their gap is step-up only, not audit |

---

## The surface set is not maintained by hand

`Check 22` derives the expected set from `supabase/migrations/**` — every `create table` less every `drop table` — and fails if this file names a surface that does not exist or omits one that does. So a migration that adds a table makes the build red until the table has a row here, which is the only mechanism that keeps a coverage record honest as the schema grows.

**Its ceiling, stated because a registry that oversells a guard is the failure this repository keeps finding (MEAS-1).** It parses DDL text. A table created by dynamic SQL, or renamed by `ALTER TABLE ... RENAME`, would fool it. That is bounded rather than open: the derivation is cross-checked from the other side by `scripts/verify_database.sql`'s table-count assertion and by `check_database_parity.ps1`'s structural surface, both of which read the live catalog. Today the derivation returns exactly the 77 tables the catalog holds.

End of document.
