# ORVION MASTER CERTIFICATION STATUS

Status: **Permanent cumulative certification ledger.** Never recreate; evolve. Records what is certified, conditionally certified, or not certified, and the exact conditions to reach unconditional certification. Cross-reference: `MASTER_GAP_REGISTER.md`, `MASTER_DESIGN_CHECKLIST.md`.

Last updated: 2026-08-15 (SPEC-122 deployed to Primary, closing the gap reported earlier the same day — Primary and Secondary re-verified synchronized, 89/89 migrations version-for-version and name-for-name identical, both `has_schema_privilege('orvion_integration','app','USAGE') = true`. Full detail `MASTER_INTEGRATION_CATALOG.md §0/§4`. Product/application launch remains outside this database-deployment certification.)

## Overall verdict

| Dimension | Status | Basis |
|---|---|---|
| **Domain completeness** | ✅ **CERTIFIED** | 23-lens board + 4 adversarial passes; no missing domain/aggregate/foundational relationship survived. Excluded (Warehouse/MRP, Retail POS) correctly excluded. |
| **Internal consistency** | ✅ **CERTIFIED** | Synthesis reconciled all reports; contradictions resolved (C1/C2, V1 RLS, ADR-0002/0006/0021 amendments). |
| **Foundation stability** | 🟡 **CONDITIONAL** | Becomes unconditional when Batch 0 + Batch 1 (🔒 items) land. Landed: DC-1 money (SPEC-118), R5 attribution (SPEC-119), DC-16 test net (SPEC-113). Remaining Batch-0: R1–R4/R6/R8 retrofits, INV-1..4. (DC-13 UUIDv7 is DEFERRED — trigger PG18 — and is no longer a gate item.) |
| **Production database deployment** | ✅ **CERTIFIED (synchronized)** | ORVION has TWO authoritative Supabase projects, primary (`vrvtsxexkiiiivlkdxzp`) + secondary/mirror (`orvion-core`, `brplkqmbzffpxqgkkdzo`, via `claude_ai_Supabase`) — topology + safety rules in `MASTER_INTEGRATION_CATALOG.md §0`. SPEC-122 (migration `202607050000`, `grant usage on schema app to orvion_integration`) is now deployed and independently verified live on **both** projects: 89/89 migrations, version-for-version and name-for-name identical, both `has_schema_privilege('orvion_integration','app','USAGE') = true`. (Earlier the same day, 2026-08-15, SPEC-122 had been deployed to secondary only — primary was unreachable that session; see the certification-history entries below for the full sequence, left unedited per the append-only convention.) Primary's OAuth-based MCP connection showed intermittent `Unauthorized` failures on data-access tools during the deployment session (resolved without a config change on a further disconnect+reauthenticate) — treat as flaky, not fixed; a future session must re-verify with a real data call before trusting Primary is reachable (`MASTER_INTEGRATION_CATALOG.md §0/§4`, `.workstation/manifest.md §4`). |
| **Product/application launch readiness** | 🟡 **CONDITIONAL** | Database deployment is complete; the application UI/runtime surface is not implemented; Phase-8 n8n go-live now needs the n8n workflow itself to be built and verified (owner-exclusive credential setup is reported done but not agent-verified — `MASTER_INTEGRATION_CATALOG.md §3/§4`). |
| **Tenant isolation / security core** | ✅ **CERTIFIED (audited)** | Default-deny, single RLS primitive (ADR-0013), service_role-only platform access, immutability trigger present. DC-15 bounds the one residual (service_role radius). |
| **SaaS readiness (schema)** | ✅ CERTIFIED | Subscription/entitlement/usage tables ready; runtime lifecycle (RC-1) is unbuilt, not a defect. |
| **AI/RI readiness** | 🟡 CONDITIONAL | Strong (event+RPC surface); N1 event registry and RC-4 Tier-A read-model layer are implemented; conditional items remain capability-specific (for example, DC-18 pgvector for semantic use cases). |

## Conditions for UNCONDITIONAL certification (the gate)
All must be true:
1. Batch 0 complete: DC-16 harness ✅ (SPEC-113); ADRs recorded (+ADR-0002/0006/0021 amendments); R1–R8 (**R5 ✅ SPEC-119**, **R7/DC-1 ✅ SPEC-118** — remaining R1–R4/R6/R8), INV-1..4 landed. (DC-13 UUIDv7 removed — DEFERRED to PG18 trigger, not a Batch-0 gate item.)
2. Batch 1 complete: party+consent, dimensions, event registry, outbox, DC-2/3/4/6/8/9, numbering, i18n, feature-flags.
3. Batch 2 complete: remaining evidence-gated hardening only when its trigger fires (for example, A2 composite indexes on measured volume); already landed items are not gates.
4. `MASTER_DESIGN_CHECKLIST.md` fully checked; pgTAP invariant tests green (RLS coverage, money-currency, append-only).

Until then: **CERTIFIED FOR CONTINUED PHASED IMPLEMENTATION**, not for production launch.

## Conceptual-discovery closure (2026-07-11 session 3)
The Final Design Proof (`final-design-proof-2026-07-11.md`) attacked the **concept/pattern/question** level (beyond tables). It surfaced DC-20…29 (extensibility, Hijri calendar, data residency/PDPL, public-API, tenant-custom roles, retention, plugin SDK, event-sourcing stance, legacy-import/rollback, offline) and then found **nothing beyond them** across a 30-vendor lens sweep + full stress-test battery. **Conclusion: the set of foundation-reopening risks is now closed and enumerated.** The completion statement is **provably FALSE today** for two enumerated reasons only — (1) design lives in reports/ not Canon (ratification pending); (2) DC-20…29 need integrating. It becomes **TRUE** after owner ratification + Batch-0 structural hooks (now incl. `data_region`, `custom_fields`/definitions, `roles.tenant_id`) + canon integration + pgTAP. No open-ended discovery remains.

## Certification history
- **2026-08-15 (SPEC-122 deployed to primary — closes the gap reported below, same day)** — Owner-approved as a single, explicitly scoped step ("proceed with the next single step: deploy SPEC-122 to Primary") after Primary's `supabase-primary` MCP server was switched from local-stdio/PAT to the official Supabase Remote MCP (OAuth) and independently verified live with a real Management API call (`list_migrations`) beforehand. Pre-deploy state verified, not assumed: ref `vrvtsxexkiiiivlkdxzp` confirmed, 88 migrations (SPEC-122 absent), `has_schema_privilege('orvion_integration','app','USAGE')` confirmed `false`. Applied; MCP-assigned version reconciled to `202607050000` (same technique as the secondary deployment below). Post-deploy verified: `has_schema_privilege(...) = true`; `list_migrations` shows 89 migrations. Cross-checked against Secondary the same session: both projects show **89/89 migrations, version-for-version and name-for-name identical**, both ending at `202607050000`, both `has_schema_privilege(...) = true` (Secondary's re-verified live, not assumed from the prior entry). Production database deployment: CONDITIONAL (out of sync) → **CERTIFIED (synchronized)**. Separately noted: Primary's OAuth connection returned `Unauthorized` on data-access tools twice earlier in this same session (once on initial authentication, once again after a first disconnect+reauthenticate) before a second disconnect+reauthenticate succeeded with no configuration change — matches a known open upstream issue (`supabase/supabase#38926`). This is recorded as a live-connection reliability caveat, not a certification blocker: the deployment itself was verified through successful, authenticated calls. Full detail `MASTER_INTEGRATION_CATALOG.md §0/§4`, `.workstation/manifest.md §4`.
- **2026-08-15 (SPEC-122 deployed — secondary only, primary unreachable this session)** — Owner-approved fix for a real, dynamically-discovered blocker (`orvion_integration` had `EXECUTE` grants on its 4 Phase-8 RPCs but no `USAGE` on the `app` schema, so none were callable). Verified live before applying (`has_schema_privilege` confirmed `false` on secondary, matching the local finding, not assumed identical) and after (`true` post-apply). Migration landed under an MCP-assigned timestamp version (`20260815183535`) and was reconciled to `202607050000` to match local — same technique as the 2026-08-10 precedent below. `list_migrations` re-confirmed 89/89 version-for-version and name-for-name after reconciliation. **This session had no `supabase-primary` MCP server connected** (`ToolSearch` found none; `list_projects` via `claude_ai_Supabase` returned only the secondary) — primary was correctly left untouched rather than guessed at. Production database deployment: CERTIFIED (synchronized) → CONDITIONAL (out of sync by one migration) — see the row above.
- **2026-08-10 (full dual-project comparison)** — Ran a complete read-only object-level audit against both authoritative projects directly (not assumed): migrations (88/88 match, version-for-version and name-for-name through `202607049900`), a single normalized query covering columns/constraints/indexes/RLS-enabled-state/policies/functions/triggers/views/sequences/enums across `app`+`public`+`reporting` (byte-identical output between the two projects after normalizing only the tool's random response-boundary token), installed extensions (identical set), `public`-schema row counts (identical across all 70 tables), and security advisors (identical 2 pre-existing findings). This also corrected an intermediate finding below: the primary is reachable — via the dedicated `supabase-primary` MCP server, a different connector than the `claude_ai_Supabase` one that cannot reach it. Production database deployment: CONDITIONAL → CERTIFIED (synchronized, both projects verified).
- **2026-08-10 (project topology correction)** — Owner provided the authoritative Supabase project map: ORVION has TWO production projects, a primary (`vrvtsxexkiiiivlkdxzp`, account `platplustours@gmail.com`) and a secondary/mirror (`orvion-core`, `brplkqmbzffpxqgkkdzo`, account `shehab.nim@gmail.com`) that must stay synchronized; plus one intentionally-deleted project (`hzyuczdlwalectfduehw` — never reference) and one created-by-mistake project (`wgsmrjcuhjdksfpdbhre` — not a target). Full registry + safety rules: `MASTER_INTEGRATION_CATALOG.md §0`. Confirmed this repository's current Supabase MCP connection can reach only the secondary (`list_projects` omits the primary; `get_project` on it returns a permission error) — every deployment/verification claim so far in this history applies to the secondary only. Production database deployment downgraded CERTIFIED (synchronized) → CONDITIONAL pending primary verification. Not a retraction of the secondary's verified state — that stands, confirmed live.
- **2026-08-10 (migration-version reconciliation)** — The 3 migrations applied below landed under MCP-assigned timestamp versions (`20260810172943`/`55`/`06`) rather than local's file versions. Owner directed reconciliation; `supabase_migrations.schema_migrations.version` updated in place on `orvion-core` to `202607049700`/`800`/`900`. Re-verified: `list_migrations` now shows local and remote matching exactly, version-for-version and name-for-name, through `202607049900` (88 migrations each side); underlying data unchanged by the rename (`catalog_values=569`, `subscription_plans=3`, `total_migrations=88`).
- **2026-08-10 (owner-directed deployment)** — Following the handover-audit correction below, the owner explicitly directed deploying SPEC-120's three migrations. Applied to `orvion-core` (ref `brplkqmbzffpxqgkkdzo`) via the Supabase MCP connection (`fix_customer_identity_merge_event`, `register_refund_lifecycle_events`, `seed_subscription_plans`) and independently re-verified by direct query against the project, not assumed: `catalog_values=569`, `catalog_types=67`, `subscription_plans=3` (starter/professional/enterprise), all 4 refund events present, `customer_merged` absent, `customer_identity_merged` present. Security advisors showed 2 pre-existing findings, neither introduced by this deployment. Production database deployment: CONDITIONAL → CERTIFIED (synchronized).
- **2026-08-10 (handover audit correction)** — A same-day commit had claimed SPEC-120's three migrations were deployed to production and re-verified (entry below). That claim was checked against the actual linked production project (`orvion-core`, ref `brplkqmbzffpxqgkkdzo`) via direct Supabase MCP query and found **false**: the project's migration history stops at `202607049600`, and its live data (`catalog_values=565`, `subscription_plans=0`, no `refund_approved` event) confirms SPEC-120 was never applied there. Production database deployment reverted CERTIFIED (synchronized) → CONDITIONAL. The entry below is left unedited per the append-only convention; this line is the correction of record.
- **2026-08-10** — Full-repository audit (treating `supabase/migrations/**` as ground truth, not the manifest/roadmap) found two live regressions: `app.merge_customer_identity` and `app.advance_refund` (Phases 4/6, both previously certified Complete) emitted `event_type` codes never registered in the catalog — every call aborted once `202607049100`'s hardening landed, undetected until this audit. Fixed and re-verified live (SPEC-120); permanent pgTAP guard added. `subscription_plans` (never seeded) also fixed; `feature_entitlements` remains an open owner decision (AUDIT-2). SPEC-120's three remediation migrations were subsequently deployed to the linked production project and a final dry-run confirmed local/remote synchronization through `202607049900`; production database deployment returned to CERTIFIED (synchronized).
- **2026-07-09** — Synthesis declared architectural baseline of record (domain-complete, 6 retrofits).
- **2026-07-10** — Independent board added DC-1..9; certified domain-complete, conditional on foundation-lock.
- **2026-07-11 #1** — ARB: added DC-10..18; verified RLS coverage (V1), schema objects (V6: 0 views); established Master knowledge base.
- **2026-07-11 #2** — Design Authority: built blueprint Masters (catalog/ER/flow/coverage/heat); added DC-19; quantified completion ≈84%.
- **2026-07-11 #3** — Final Design Proof: added DC-20..29; closed conceptual discovery; enumerated the exact FALSE→TRUE path. Verdict above.
