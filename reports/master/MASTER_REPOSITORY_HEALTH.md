# ORVION MASTER REPOSITORY HEALTH

Status: **Permanent Living SSOT for repository/governance health.** Never recreate; evolve. Governance effectiveness is measured, not asserted (`GOVERNANCE.md §17`). Regenerate each review; target = auto-generated by the governance-lint automation (`GOVERNANCE.md §11`). A regressing indicator is a **governance defect to fix**, not tolerate.

Last measured: 2026-09-02 (**Check 18 added — `manifest.md`'s `Active Change Request` must name a real, still-OPEN Change Request (COLD-3).** The repository was CLEAN while that pointer named `SPEC-125`, whose own Status is `[x] Complete`, and equally CLEAN while it named a CR file that has never existed. §5 action 1's last-but-one remaining item is therefore closed, and the `future-backlog.md` process-safeguard row is struck. Prior entry follows.)

Previously: 2026-09-02 (**The enforced-invariant registry §2b called itself the single discoverable list of every automatic check and stopped at Docs-12 of 17 — Docs-13…17 added, and Docs-7 corrected to state what Check 7 now measures and what it deliberately does not (COLD-2).** Three restated counts that had gone stale under the 2026-08-29 rule below were removed rather than refreshed by the same reasoning: the pgTAP file count read 68 against a suite of 89, the historical-report count 78 against 106, and the guard was described as 11 checks. Prior entry follows.)

Previously: 2026-08-29 (**GOV-5 — this document no longer restates live database or git state.** It had carried "Primary — 90 migrations" and "worktree and GitHub `main` match at `c5590c4`" since 2026-08-20, while Primary stood at 160 and HEAD had moved seventy migrations on; its §2 schema counts were a 2026-08-10 snapshot carried forward unchanged for three weeks. Per `GOVERNANCE.md §2` live state has exactly one home, so the duplicates are removed rather than refreshed — refreshing them would only reset the same trap.)

**Where live state actually lives:** migrations / fingerprint / tables / functions / policies / cron → `_ORVION_CANONICAL/manifest.md`, proven by `scripts/check_database_parity.ps1` (ledger **and** full function surface, both sides read live) and `scripts/verify_database.sql`. Git HEAD → `git log`. Deployment topology → `MASTER_CERTIFICATION_STATUS.md` + `MASTER_INTEGRATION_CATALOG.md §0`.

**What this document owns and measures:** the governance layer — authority structure, document population, enforced invariants, and drift indicators. Values marked ⚙ are computable today (grep/script); values marked 👁 require the link-checker/governance-lint tooling to be exact (currently best-effort).

## 1. Composite scores (0–100)
| Score | Value | Basis |
|---|---|---|
| **Repository Health** | **94** | one conduct authority (AGENTS) + 2 tombstones, GOVERNANCE pointer at every entry point, reports subfoldered, 0 dead governance; −6: automation (governance-lint/link-check/Active-CR lint) not yet enforcing every drift rule |
| Governance Coverage | 97 | GOVERNANCE.md + reports/README + lifecycles + SSOT matrix + one-authority rule (§6.8); duplicate conduct docs retired |
| Knowledge Coverage (design) | 82 | per `MASTER_COVERAGE_SCORE.md` (weighted design completeness) |
| Documentation Coverage | 90 | every layer documented + indexed; −10: some canon still `Draft` status |
| Automation Coverage | 84 | repository-index + ai-map + **API contract** auto-generated; the **whole** pgTAP suite (file count: `manifest.md`, guarded by Check 15) and the self-gating smoke-test run in `migration-ci.yml`; the consistency guard runs in `repository-consistency.yml` — its checks are enumerated in §2b below rather than counted here, because a count restated beside the list it summarises is one more thing to go stale (**GOV-5**; it read "11-check" while the guard carried 17). −16: the three **Live-\*** parity invariants below cannot run in CI (no CI job can reach Primary), the six end-to-end HTTP suites are session-run, and `Active CR` lint + full link-check are still pending |
| Review Coverage | 98 | review sessions + validation pipeline; every finding traced |
| Repository Maturity | 88 | production database deployed; Supabase-native backend/RPC layer implemented; no app UI/runtime surface yet |

## 2. Structural indicators (⚙ measurable now)
| Indicator | Value | Note |
|---|---|---|
| Schema: tables / functions / policies / triggers / views / cron | **see `manifest.md`** | **Removed from this file 2026-08-29 (GOV-5).** These were a 2026-08-10 snapshot carried forward unchanged and every one of them had drifted. Live truth: `manifest.md`, guarded by `check_database_parity.ps1` + `verify_database.sql` — both of which compare against the database rather than against a number typed here |
| app RPCs / catalog types / permissions / roles | **see `manifest.md`** + `MASTER_API_CONTRACT.md` | Same reason. The API contract is *generated* from `pg_catalog`, so the endpoint surface cannot drift without `check_database_parity.ps1` Check L3 failing |
| Canon docs | 37 ⚙ | `_ORVION_CANONICAL/**` incl. `manifest.md` (deprecated tombstones retired 2026-07-17; git history preserves them) |
| ADRs (ratified) | 23 ⚙ | ADR-0001…0023, no gaps |
| Master documents (Living) | 15 ⚙ | 13 findings/blueprint/integration + this health doc + `MASTER_API_CONTRACT.md` (auto-generated — `GOVERNANCE.md §2/§5`, registered 2026-08-29 as GOV-2) |
| Evidence reports (Living) | 5 ⚙ | VALIDATED/PENDING/REJECTED/INDUSTRY_REFERENCES/PROOF_LOG |
| Historical-immutable reports | **see `ls reports/history`** | dated review + phase + process + session reports. **The number is removed rather than refreshed (2026-09-02, GOV-5's own remedy):** it had read 34, was corrected to 78 on 2026-08-29, and was 106 by 2026-09-02 — a count that must be hand-maintained on every session report is a stale number waiting to happen, which is the identical reasoning `reports/README.md` recorded when it dropped its own file counts |
| Tracked findings | 141 register rows / 109 detail blocks ⚙ | SSOT: `MASTER_GAP_REGISTER.md`. Every id the manifest raises is proven to resolve here by Check 11 |
| pgTAP suite | **see `manifest.md`** ⚙ | `supabase/tests/**`. File count, assertion total and HTTP-suite totals all live in `manifest.md`, where Check 15 compares the first two against the test files themselves. The literal "68 files" was removed 2026-09-02 (**GOV-5**): it had been carried here since 2026-08-29 while the suite grew to 89 — a hand-maintained number drifting below the artifact it summarises, in the one document whose job is to measure rather than assert |
| Memory files | 0 | external `.claude` memory is a cache only and is currently empty — nothing operational has ever depended on it (`AGENTS.md §6`) |

## 2b. Enforced repository invariants (the fitness functions — what the repo validates automatically)
Single discoverable list of every automatic check. If one fails, CI is red and the merge is blocked (schema) or the consistency job fails (docs). This is how the repository stays self-validating without human supervision.

| # | Invariant | Mechanism | Gate |
|---|---|---|---|
| Schema-1 | RLS enabled + policy on every tenant table | `supabase/tests/01_rls_coverage_test.sql` (pgTAP) | `migration-ci.yml` |
| Schema-2 | Append-only audit tables carry the immutability trigger | `02_append_only_audit_test.sql` | `migration-ci.yml` |
| Schema-3 | Every money column scale ≥ currency precision | `03_money_currency_precision_test.sql` | `migration-ci.yml` |
| Schema-4 | Every `tenant_id` column has an index | `04_tenant_id_index_coverage_test.sql` | `migration-ci.yml` |
| Schema-5 | Every `app` function pins `search_path` | `05_function_search_path_test.sql` | `migration-ci.yml` |
| Schema-6 | RLS predicates wrap `current_tenant_id()` (InitPlan) | `06_rls_initplan_wrapping_test.sql` | `migration-ci.yml` |
| Schema-7 | Foundation counts + read-scope model + transition/assignment triggers — the expected values live **in the script's own assertions**, deliberately not restated here (GOV-5) | `scripts/verify_database.sql` (self-arming `ON_ERROR_STOP`) | `migration-ci.yml` |
| Schema-8 | The **whole** pgTAP suite runs on a clean `db reset`, not a chosen subset | `supabase test db` over `supabase/tests/**` (every file; count in `manifest.md`, guarded by Check 15) | `migration-ci.yml` |
| Docs-1 | No broken document references in Living docs, **and (2026-09-04, GOV-13) no reference to a pgTAP `NN_*_test.sql` that does not exist** — in Living docs and in `scripts/*.ps1`. Three such references survived the `d02b702` merge's deletion of the files they named, one of them a register row citing a deleted test as a finding's proof; nothing resolved a test reference until this widening | `check_repository_consistency.ps1` Check 1 | `repository-consistency.yml` |
| Docs-2 | No finding-status contradiction **within** a Master, **and (2026-08-29, AUD-04) none ACROSS Masters** — the cross-file pass that makes §3's published "0" a measurement instead of an assertion. **REPAIRED 2026-09-04 (GOV-11):** the detail-block half of this check had matched **0 of 96** blocks since it was written, because it required a bullet beginning `- **Status:**` and the register states its verdict inline on a combined `- **Category:** … · **Status:** …` line; the cross-Master pass never read detail blocks at all. 46 findings — including RECOVER-1 (Critical, CLOSED), SUP-2, SUP-3, SUP-4a, RBAC-5, RBAC-6 — had statuses no contradiction search could see. Both halves fixed and mutation-proven in three directions | Check 2 | `repository-consistency.yml` |
| Docs-3 | Boot-chain router integrity + AI-pointer files stay thin (≤25 lines) & routed (anti-duplicate-authority) | Check 3 | `repository-consistency.yml` |
| Docs-4 | Every report declares its document class | Check 4 | `repository-consistency.yml` |
| Docs-5 | `manifest.md` stays lean (≤70 lines) — current-state only, no changelog creep | Check 5 | `repository-consistency.yml` |
| Docs-6 | Roadmap ↔ manifest phase agreement (which phase is current) | Check 6 | `repository-consistency.yml` |
| Docs-7 | `ai-map.json` stays fresh vs. manifest — **every live-state field `generate-ai-map.ps1` extracts is compared, which is a claim about the generator's contract rather than about which drifts have been noticed**: current phase (by number), `Last Completed` (by value — the comparison was gated on a literal-SPEC-id shape and inert for ~40 commits until `4b67d3f`), the whole multi-line `Next capability:` block by value with its steps (2026-08-17), and **`Active Change Request` by value (2026-09-02 — found independently and identically as `GOV-10` and `COLD-2`; see either register row)** — the field `AGENTS.md §4` *branches* on and the whole agent handoff. **This row is dated deliberately, because it twice claimed more than the check measured:** `last_completed` was listed here throughout the ~40 commits its comparison was inert, and `active_change_request` was compared by nothing at all until 2026-09-02, coverage having been added field-by-field on the day each field's own drift shipped. **Catches divergence from either side; does NOT detect a missed clear-on-Complete** (a manifest naming a finished SPEC with ai-map regenerated to agree is CLEAN here) — that is a different invariant and is now **Docs-18** below. **Measures disagreement, not presence:** silent if the manifest loses a field entirely | Check 7 | `repository-consistency.yml` |
| Docs-8 | Supabase dual-project topology registry (`MASTER_INTEGRATION_CATALOG.md §0`) retains both authorized project refs, retains disqualifying wording for non-target refs, and doesn't contradict `MASTER_CERTIFICATION_STATUS.md`'s stated per-project certification status (doc-vs-doc staleness only — **never a parity check between the two projects**) | Check 8 | `repository-consistency.yml` |
| Docs-9 | `manifest.md`'s migration count / latest migration / ledger fingerprint match the actual files in `supabase/migrations` | Check 9 | `repository-consistency.yml` |
| Docs-10 | `reports/README.md`'s **Latest session report** pointer is CURRENT, not merely resolvable — it must name the same file as the manifest's `Narrative:` field (**GOV-1**) | Check 10 | `repository-consistency.yml` |
| Docs-11 | Every open-decision ID `manifest.md` raises resolves to a row in `MASTER_GAP_REGISTER.md`, the declared findings SSOT (**GOV-3**) | Check 11 | `repository-consistency.yml` |
| Docs-12 | **No current-state evidence is dated in the FUTURE**, and the system clock agrees with the newest commit (**AUD-01**). No exemption list — a document describing a past future-dating incident must describe the date, not quote it | Check 12 | `repository-consistency.yml` |
| Docs-13 | No `reports/master` table row is escaped out of its own table — a leading `\|` renders one column left AND makes the row invisible to Check 2's status comparison (**REG-1**) | Check 13 | `repository-consistency.yml` |
| Docs-14 | No ID on `manifest.md`'s open-decision line is already marked decided in the register. Check 11 proves the ID *resolves*; this asks whether what it resolves to is still **open** (**OWNER-1**). The resolved set is derived from the register's own status cells, never from a list maintained in the script | Check 14 | `repository-consistency.yml` |
| Docs-15 | `manifest.md`'s suite figures match the test files (each carries a literal `select plan(N)`) and its client-RPC count matches the **generated** API contract (**META-1**). The HTTP-assertion total and the table count are deliberately **not** mechanised — reasons recorded in the script, not left as silence | Check 15 | `repository-consistency.yml` |
| Docs-16 | No canonical document names a settled finding as a **current** owner decision, judged against `manifest.md`'s own open-decision line — five structural gates, and no closed-ID list is maintained anywhere (the cold-start contradiction, 2026-09-01) | Check 16 | `repository-consistency.yml` |
| Docs-17 | No canonical document restates the RPC-endpoint count that `MASTER_API_CONTRACT.md` generates — the fix that holds is deletion, not refreshment (**REG-2**) | Check 17 | `repository-consistency.yml` |
| Docs-18 | `manifest.md`'s `Active Change Request` names a **real, still-OPEN** Change Request, judged against that CR's own `## Status` section — the authority `changes/TEMPLATE.md` defines and `CR_LIFECYCLE.md` governs (**COLD-3**). Fails on a terminal CR (`Complete`/`Cancelled`, per `CR_LIFECYCLE.md §4`), a CR file that does not exist, or a Status section that is missing, ambiguous, or outside the template's vocabulary. **Status is read, never copied** — no CR status enters the manifest or `ai-map.json`. Distinct from Docs-7: that one asks whether the manifest and ai-map *agree*, this one asks whether what they agree on is *true*; both are independently testable and neither substitutes for the other | Check 18 | `repository-consistency.yml` |

| Docs-19 | **The repository CARRIES attributable, current evidence that Primary's migration ledger is this repository's migration ledger** — the whole `version_name` set compared as a set, `migration_count` and `ledger_fingerprint` **recomputed** from the recorded ledger so a hand-edited number carries no authority, and `repository_head` required to be an ancestor of HEAD. **FAILS CLOSED**: absent, unparseable, incomplete, stale or disagreeing evidence is an issue, never a skip (**RECOVER-1**) | Check 19 → `check_primary_ledger.ps1` | `repository-consistency.yml` |

*(Docs-13…17 were added to this table on 2026-09-02. The five checks had been live in the script since 2026-08-30/09-01 while this table — which calls itself the single discoverable list of every automatic check — stopped at Docs-12. A registry that claims completeness and is not complete is the same "documentation stronger than the measurement" class the checks below it exist to catch, so it is corrected here rather than noted.)*

*(Docs-18 was added 2026-09-02 with COLD-3's check. Docs-19 was written on 2026-09-03 as Docs-18 by a session that had not fetched, and renumbered during the 2026-09-03 reconciliation when both numbers turned out to have been claimed independently — the same divergence that produced GOV-10 and COLD-2 as one finding under two ids.)*

**Docs-19's evidence class, stated because a guard must not be quoted for more than it measures (MEAS-1).** Check 19 verifies a **RECORDED** Primary reading; it opens no database. It cannot prove the recorded ledger was read from Primary rather than generated from the repository — that is **GUARD-1**'s class and no repository-local mechanism closes it. What it does close is the state RECOVER-1 actually occupied: *no Primary reading for the current migration set, and every guard green*. That is now a hard CI failure. Its mutation suite (`test_primary_ledger_guard.ps1`, 13 assertions) proves it flags an extra migration on either side, a same-count/different-identity swap, a stale or foreign HEAD binding, missing evidence, a hand-edited count, a hand-edited fingerprint, malformed JSON and a missing field — and, in both directions, that an untouched sandbox still passes.

**Guards that exist but are NOT CI-gated** — stated because this table is meant to be the single discoverable list, and a reader who assumes "listed ⇒ enforced by CI" would be wrong about these:

| # | Invariant | Mechanism | Gate |
|---|---|---|---|
| Live-1 | Local ledger = the migration files; **Primary's ledger** = the same, when its fingerprint is supplied from a live read. **(2026-08-29, AUD-05) the script no longer prints CLEAN / exit 0 when Primary was never contacted** — three distinct outcomes now: exit 0 CLEAN (measured), exit 1 DRIFT (measured and disagreed), exit 2 UNPROVEN (not measured) | `check_database_parity.ps1` L1/P1 | **manual / session gate** (`AGENTS.md §4` Stage B step 8b) — no CI job can reach Primary |
| Live-2 | The **full** `app`+`public` function surface is byte-identical local↔Primary under comment-stripped normalization (**PAR-1**; the pattern must use `chr(10)` — **PAR-1a**) | `check_database_parity.ps1` L2/P2 | manual / session gate |
| Live-3 | `MASTER_API_CONTRACT.md` still matches the live surface — regenerated and diffed, so a hand edit fails | `check_database_parity.ps1` L3 | manual / session gate |

Adding a new invariant is the standard permanent-guard response (GOVERNANCE §18 discovery-to-guard) — extend the pgTAP suite or the consistency script, never a one-off manual check.

## 3. Drift indicators (👁 target-automated)
| Indicator | Value | Method / status |
|---|---|---|
| Duplicate findings (same fact, 2 authoritative homes) | 0 | resolved: MASTER_ARCHITECTURE_DECISIONS = overlay; MASTER_EXECUTION_PLAN = batches-not-phases |
| Conflicting finding status across Masters | 0 ⚙ | **MEASURED since 2026-08-29 (AUD-04), not asserted.** This row published "0 across Masters" while Check 2 only ever compared a file against ITSELF — a stronger claim than anything measured it. Check 2 now carries a cross-file pass over every `reports/master/*.md`; the current run measures agreement over 24 open ids. Register-vs-`VALIDATED_ARCHITECTURE_DECISIONS.md` (outside `master/`) is still 👁 |
| Duplicate ADRs | 0 | append-only log |
| Conflicting authority (2 SSOTs for one fact) | 0 | resolved by `GOVERNANCE.md §2` matrix |
| Broken cross-references | 0 | ⚙ Living-doc strict-token references now guarded by `check_repository_consistency.ps1` (CI); full link-check (lychee, all files) still 👁 |
| Orphan documents (unreferenced by any index) | 0 | `repository-index.md` (auto) + `reports/README.md` index cover all |
| Stale documents (Living, >90d untouched, superseded fact) | **was 4, now 0** ⚙-partial | This row read "0" on the strength of a **2026-07-15** reconciliation and was never re-measured. On 2026-08-29 four Living docs were found restating superseded live state — certification (Primary at 90 migrations), health (this file), domain catalog, reports index — plus `MASTER_DEPENDENCY_GRAPH.md` claiming coverage of "every finding" while naming 46 of 135 (**GOV-5**, **AUD-07**). All corrected by removing the restatement rather than refreshing it. **Still 👁**: no guard measures document staleness, so this number is a review finding, not a measurement — treat it as accurate only as of the date above |
| Pending ADRs (proposed, unratified) | 10 | `MASTER_ARCHITECTURE_DECISIONS §B` — **owner action** |
| Pending findings (failed a pipeline stage) | 8 | `PENDING_ARCHITECTURE_FINDINGS.md` |
| Pending governance changes | 1 | GOVERNANCE §12 step 6 only (governance automation — config/CI, owner-gated); steps 1–5,7 DONE |
| Deprecated docs still present | 0 | codex.md + SYSTEM_PROMPT.md retired 2026-07-17 (documents-serve-ORVION clause; git preserves history) |

## 4. Growth / complexity trend
| Metric | Value | Watch |
|---|---|---|
| Reports folder size | 56 files | subfoldered into master/ (14) + evidence/ (5) + history/ (34) + 3 root — physical reorg DONE (session 9) |
| changes/ folder size | 121 files | flat SPEC-* history (119 SPEC + TEMPLATE + CHANGE_REQUEST, +SPEC-120/121 since last count); same subfolder-by-phase trigger reached (repo-eos-review §6) |
| Root conduct docs | **1 authority** (AGENTS) + 2 tombstones | PROTOCOL/global-rules RETIRED to pointers (session 10, full-diff verified zero unique authority) ✅ |
| Entry-point → GOVERNANCE pointer | present ✅ | added to README + AGENTS §4 + llms.txt (session 10) — navigation determinism closed |
| Stray artifacts | `backup/` gitignored ✅ | retired from version control (session 8) |
| AGENTS.md size | 114 lines | ✅ within the 150-line agent-efficiency budget (evidence: 2026 AGENTS.md research) |
| llms.txt (AI-convention entry) | present ✅ | thinned to a pure pointer 2026-07-15 (restates nothing → cannot drift); boot sequence lives only in `AGENTS.md §4` |
| Avg Living-doc age | <2 days | very fresh (governance built this month) |
| Governance version | 1.9 | changelog in GOVERNANCE.md (latest: Integration Catalog SSOT row; prior: documents-serve-ORVION, repository stewardship, Living-Documents-first) |
| Reports needing an index | 0 | `reports/README.md` covers all |

## 5. Top health actions (derived — not new findings)
1. **Extend governance automation** — the repository-consistency guard (`check_repository_consistency.ps1` + CI) now runs the **18** checks registered in §2b. Both items this action listed as remaining are **closed**: the report class-header lint is Check 4, and the **`Active CR`-cleared-after-Complete lint is Check 18** (COLD-3), delivered as a CI-gated guard rather than the workstation-local hook originally proposed, and validating the pointer against the CR's own Status rather than only against the literal string `None`. **Genuinely remaining: a full link-checker (lychee)** → would move the last 👁 indicators to ⚙.
2. **Ratify the 10 pending ADRs** — unblocks canon integration; drops "Pending ADRs" 10→0.
3. ~~pgTAP (DC-16)~~ — ✅ DONE (SPEC-113): schema invariants are now a measurable CI fitness function.

## How to read this
Every number here has a source: schema counts = migrations; findings = `MASTER_GAP_REGISTER.md`; pending = the evidence reports; governance = `GOVERNANCE.md`. This dashboard **references**, never restates. Regenerate at each review; when a 👁 indicator becomes ⚙ (tooling installed), update the method column.
