# ORVION reports/ — Index & Organization

Governed by `GOVERNANCE.md` (§7). This folder holds analysis, findings, evidence, and history — **never** authoritative business/schema canon (that is `_ORVION_CANONICAL/**`).

> **Latest session report:** `history/session-2026-09-05-engineering-awareness-layer.md` — **the §5b question-2 capability, built and proven; no migration, no schema change, no canon change.** `AGENTS.md §5b` has required *"which code CONSUMES, PARSES or DERIVES FROM the structure this package changed?"* since CUST-1, and nothing helped answer it — the answer was ad-hoc grep whose recall varied with the engineer. **`pg_depend` cannot close that gap**: PostgreSQL stores function bodies as unparsed text, so it records **no** edge from a function to the tables it touches, and it is equally blind to codes carried in trigger ARGUMENTS (MEAS-1) — measured here as **2** `pg_depend` rows for `moddatetime` against **51** triggers that pass it a column name. New **`scripts/impact.ps1`** reads what the catalog actually stores (`prosrc`/`prosqlbody`, `pg_get_expr` over `pg_policy`, `pg_get_triggerdef` **including arguments**, `pg_get_viewdef`, `pg_get_constraintdef`, FKs, indexes, grants) plus the repository (migrations, pgTAP suites, scripts, canon, Masters, ADRs), and labels **every** section with its evidence class. It is a **query, not a guard** — no CLEAN verdict, no CI wiring, because making a lead-generator into a gate is the overclaiming class `§6` exists to prevent. **Validated in both directions**: the known CUST-1 example replays mechanically (`-Target customers` returns `app.merge_customer_identity`); the must-flag counterexample finds all 51 trigger-argument consumers; the must-not-flag counterexample confirms `\mtenant\M` does **not** match `tenant_id`; an injected unapplied migration downgraded **every** evidence label to `LOCAL RUNTIME (STALE — NOT the repository)` and `-RequireFresh` exited **2** (PAR-1b, fail-closed); `-Target "drop; select"` was refused before reaching psql. Two defects in the script's own SQL were caught by running it, not by reading it. **The local database was found 6 migrations behind at session start** — by the new staleness check, before anything was read from it. **Deliberately no index, no embeddings, no vector store:** a freshly reset catalog is a better index of ORVION than any AST index of its migrations, which would index *history* and return dropped columns as live facts; the 2026 AST+embedding MCP family, GitHub Spec Kit, the generic Supabase agent skill and external knowledge graphs were all evaluated and **rejected** as duplicated authority or wrong-shaped evidence. Also added, outside ORVION: a `SessionStart` hook printing ahead/behind vs `origin` (the `§4` step-8 hole that RECOVER-1 paid for), plus `deny` rules making a schema-changing call to the **wrong Supabase project** impossible and `ask` rules on the five protected documents. Smoke **ALL CHECKS PASSED (77 tables)**, consistency **CLEAN 1–19**. **pgTAP, the HTTP suites and Primary were not run and are reported UNPROVEN** — nothing database-changing shipped. Next: use it as the first move of **GOV-20** rather than trusting it on a clean run alone.
>
> *Previously:* `history/session-2026-09-04-gov18-cust3-void1-ret1.md` — **all four owner-directed capabilities built, verified and deployed: GOV-18, CUST-3, VOID-1, RET-1.** Migrations **191 → 194**; suite **94/1347 → 97/1423**; HTTP **414 → 430**; consistency CLEAN 1–19; **Primary parity proven at every step, all three values read FROM Primary**. **GOV-18:** Check 2's open-detector saw only a bare `| OPEN |` cell — **20 of 84** open rows, blind to 76% of them, which is how GOV-17's five contradictions printed CLEAN. Each row's status cell is now found from **its own table header** (index 9 here, 2 and 5/6 in other Masters), and the vocabulary was **derived by enumerating every leading token**. New `scripts/test_status_contradiction_guard.ps1`: **25 assertions, both directions** — and its own first control caught a defect in the harness. It then flagged **ATTR-1** and **CAT-5**, stale GOV-19 splits, both reconciled on live evidence. **CUST-3** (`202607060300`): a nullable, tenant-supplied, **warning-only** customer receivable ceiling — no default invented, no dunning. Unlike the supplier ceiling it **CONVERTS** foreign-currency exposure (SUP-4c's rule; the first reader `exchange_rates` has ever had) and **REPORTS** any currency it cannot price rather than dropping it. **VOID-1** (`202607060400`): the internal invoice void, with **no day-count encoded anywhere** — canon 26 gained the Invoice State Machine it never had; `voided` is terminal; the money refusal is derived from `customer_balance`'s own arithmetic and asked of `payment_allocations` rather than the status word; and the ONE place the internal and external lifecycles touch is a refusal to void a document the authority has **accepted**. An externally recorded `cancelled` does **not** void the ORVION invoice, and a test pins that nothing invents that mapping. `journal_entries`' void columns are CLOSED BY DESIGN — a posted entry is reversed, never voided. **RET-1** (`202607060500`): retention became a **policy per `document_type_code`** — **zero rows seeded**, so nothing is destroyable; fail-closed is now the **shape of the query** (an INNER JOIN) rather than a WHERE clause; `retention_days >= 1` makes “destroy on sight” unstorable; withdrawal is **deactivation**, because `authenticated` holds DELETE on no public base table. It also **removed the PAR-2 hazard at its root**: retention was a FUNCTION, so four suites had to redefine it, and three earlier sessions chased the drift that caused. **THE GUARDS CAUGHT FOURTEEN REAL DEFECTS IN THIS SESSION'S OWN WORK** before anything shipped — among them a record field named in a shared trigger condition that broke **eight test files at once** (LIC-3/PP-4's class), a row-image comparison silently defeated by a **sibling trigger**, `numeric(14,2)` truncating a 3-dp currency (DC-1/R7), four RLS policies defaulting to `to public` (POL-1), and a credit control that never re-evaluated when the **ceiling** moved (**SUP-4d**, found by an HTTP probe after pgTAP was green). Assertion 22 of `83_actor_attribution_test` **demanded its own update** when `invoices.voided_by` became derived. Three findings were **recorded rather than fixed** in the wrong package: SUP-4d, CUST-4, CUST-5. **Zero open owner decisions.** One item waits on counsel and is not an engineering question — the retention period per document type, reconciling Egypt's PDPL (Decree 816/2025, in force) against tax minimums; it drops into `document_retention_policies` with no schema change. Manifest **6,813/7,000 — no budget raised**. **Batch 6 not started.**
>
> *Before that:* `history/session-2026-09-04-decision-debt-closure.md` — **eight "owner decisions" that were not, and the GitHub identity a re-clone silently undid.** The owner directed that no open decision sit unresolved merely because a document labels it one. Re-deriving each from the implementation rather than trusting its label closed **eight of the fifteen**, and **none of the eight needed the owner**. The largest: **FIN-7** said canon defines no Invoice State Machine and that inventing the transitions would be inventing policy — **the machine already exists in the RPCs**: `issue_invoice` raises `only a draft invoice can be issued`, and `record_payment` does not *choose* the next state, it **derives** it (`case when v_new_total >= total_amount then 'paid' else 'partially_paid' end`). What is missing is that `invoices` has **no `enforce_status_transition` trigger and zero `status_transitions` rows**, so those rules bind the RPC door and not the PostgREST door — BOOK-1/ADMIN-1/FIN-8/FIN-10/QUO-1's class, fixed five times before. Also closed: **VERIFY-1** (canon 07 makes verifying and approving **one act**, already implemented through `approval_requests`; the columns must never get a writer), **TRANS-1** (keep the guarded duplication — the cure needs a sparse or jsonb column, trading a machine-checked duplication for polymorphism no constraint can check), **LIC-1** (accept the residual: a 128-bit single-use token makes the attack the missing audit would detect infeasible, so an autonomous-transaction extension on the security path is a net loss), **DELIV-1 + PH8-2** (observability, not policy — the four states a health view would report already exist), **QUO-4** (canon 28's own scope column reads "assigned/**department**"), **DOC-LC-2** (a **version** supersedes; a **document** does not — the implementation is right and canon 26 is wrong). **RET-1 reclassified**: NULL means retain-forever and the scan's `WHERE` is unsatisfiable, so the safe answer is already in force and it is not blocking. **SUP-4c narrowed to one question** — `tenants.default_currency_code` and a tenant-maintained `exchange_rates` with `effective_at` already exist, so **no FX provider needs inventing**; only the conversion *instant* is a policy choice. **VOID-1 narrowed on external evidence**: Egypt's ETA permits cancellation within 7 days and requires credit notes thereafter, so the register's "void OR credit note" framing was a false dichotomy. Three new findings: **TAX-1** (`invoices.external_submission_status_code` is guarded against a catalog type that **does not exist**, so the column can only ever be NULL), **GOV-15** (TRANS-1 carried two blocks disagreeing on Status *and* Owner — invisible to Check 2 because both are open forms) and **GOV-16** (nothing binds the register's open rows to the manifest's line: a census found **~45 non-closed rows against the manifest's 15**, the whole DC-\* family and PH8-3…8 invisible at boot; **~30 remain untriaged and this is stated, not implied complete**). **GitHub identity fixed at its actual cause:** the 2026-08-26 session had already qualified `origin`, and `git reflog` shows a **re-clone on 2026-08-30** rebuilt `.git/config` and discarded it — git does not track `.git/config`, so the fix belonged in **`bootstrap.ps1`**, a tracked file that hardcoded the unqualified URL. Now qualified there, plus a mutation-tested `doctor.ps1` check; `git push --dry-run` authenticates as **PlatPlusHub non-interactively**. The stale machine-wide `Shehabhub` credential is **bypassed, not deleted** (deleting affects other repositories — one command given to the owner). **Four implementations scheduled and deliberately not built** (FIN-7's second door first — it needs no owner input). **No migration, no SQL, no database write; PRIMARY PARITY = UNPROVEN and reported as such.**
>
> *Previously:* `history/session-2026-09-04-python3-environment.md` — **a small read-only environment diagnostic; no engineering state changed.** `python3` was never a missing interpreter: it resolves on `PATH` to a **0-byte Microsoft Store App Execution Alias reparse point** whose backing Store package is not installed, so it prints an install advertisement and exits non-zero — while a healthy **Python 3.12.10** sits two PATH entries earlier, reachable as `python`. Windows CPython ships `python.exe`/`pythonw.exe` and **no `python3.exe`** (the POSIX spelling is served by `py -3`, which also works). The deciding measurement: **`python3` occurs zero times in this repository** — no `.py` file, no shebang, no `requirements.txt`/`pyproject.toml`, no venv, no CI step; all five live references use the bare name `python`, and none of them executes Python to do work. The `python3` spelling came from **the agent's POSIX habit, not from ORVION**. **No fix was applied and none is warranted** — an alias would be a global Windows change to satisfy a command no ORVION script issues. `doctor.ps1` runs all-green (git · node · npm · docker · **python 3.12.10** · claude · code, Docker Engine up, in sync with origin/main), so the `§6` protocol is executable and **Batch 6 is environment-ready — which is readiness, not authorization.** Latent tripwire recorded rather than fixed: `python` works *because* real Python (PATH 11) precedes `WindowsApps` (PATH 13); invert that order and `python` breaks too. **A SECOND environment defect was found by hitting it, and is deliberately NOT fixed because the fix is an owner choice:** `git push origin main` **hangs** non-interactively — the remote URL carries **no username**, so Git Credential Manager resolves `git:https://github.com` to the stored **`Shehabhub`** credential rather than the **`PlatPlusHub`** one this repository needs, then falls through to a prompt nobody can answer. Reads (`ls-remote`) work throughout, so it is credential *selection*, not connectivity. This **refines** commit `8c063ee`, which logged it as *"GCM has no non-interactive credential"* — a usable credential **does** exist, it is just unreachable through an unqualified URL. **The next agent should push with `git push https://PlatPlusHub@github.com/PlatPlusHub/CRM.git main`** (used here; fast-forward, no force, `.git/config` untouched) and must never read the hang as a reason to force, reset or abandon local work. Choosing which of two stored GitHub identities `origin` pushes as is the owner's call, stated exactly in the report's §8. **Primary and the local database were not touched; no test was re-run** (nothing testable changed). Written because **GOV-14** — minted by the previous session — makes a read-only session still a session; here it is applied for the first time by a different one. The engineering frontier is **unchanged**: HTTP-probe the RBAC-6 class (`supplier_balance` strongest), **a probe and not a migration.**
>
> *Previously:* `history/session-2026-09-04-owner-policy-package.md` — **SUP-4b decided by the owner and shipped; the other four targets held at their decision boundaries.** The package named five targets, and **exactly one arrived with an owner decision.** TRANS-1 (`BLOCKED — ARCHITECTURAL DECISION`), DELIV-1/PH8-2 (`OPEN, subsumed by PH8-2's decision`), PLAN-1 (three "Limited" ceilings canon never defines) and QUO-4 (`assigned` vs `department` scope) are owner decisions the owner did not make here, and each is isolated at its exact boundary rather than invented — the drift half of TRANS-1 is already fixed and guarded, and PLAN-1's counter was **not** built because three of the ceilings it would count against do not exist as numbers. **SUP-4b: the ceiling WARNS and never refuses (ADR-0028).** The answer to the owner's first question dissolved the other two — with nothing refused, no override permission is needed and **no concurrency serialisation exists to get wrong**, so no lock and no FIN-10-style deferred constraint were introduced. Delivered by `202607060100`: AFTER-row probes on the only two tables that can move exposure, comparison **in the ceiling's own currency**, two canon-27 event types, and one notification each to the **Company Owner and Finance Manager** resolved by ROLE — who is TOLD, not who is ALLOWED; **authorization is untouched and no permission was minted or widened.** Idempotency is the **event ledger**, not a new column or status vocabulary (the owner forbade a colour/status system): suppressed while the latest threshold event is already `exceeded`, with a `cleared` event so a later breach speaks again. `supplier_credit` now also returns `exposure_amount` and `threshold_exceeded` — one boolean a UI can render red. **No FX was invented** (`exchange_rates` has **zero readers**) → **SUP-4c**; **no customer rule** (`customers` has zero credit columns) → **CUST-3**. **No email is sent and nothing claims one was** — the obligation is recorded `pending` on the existing delivery ledger and canon 10 now states the boundary. Verified by execution: **93 files / 1326 assertions Pass A = Pass B**, **HTTP 414 / 0**, smoke passed, **deployed to Primary with the ledger row normalised** (GUARD-1), all three Primary values read live, both guards CLEAN.
>
> *Previously:* `history/session-2026-09-04-obs-remediation.md` — **the audit's three observations, two of which were wrong, and the two guards producing false green underneath them.** A post-merge forensic audit raised OBS-1/2/3; investigating them from the repository rather than accepting them changed two and found a third defect the audit missed entirely. **OBS-2 was not "three findings missing a summary row"** — 59 of 98 detail blocks have no row and that is the register's own convention. The real defect (**GOV-11**) is that Check 2's detail-block status reader required a bullet beginning `- **Status:**` while the register has *never* written that form, stating its verdict inline on a combined `- **Category:** … · **Status:** …` line: it matched **0 of 96** blocks from the day it was written, leaving 46 findings — **RECOVER-1 (Critical, CLOSED)**, SUP-2, SUP-3, SUP-4a, RBAC-5, RBAC-6 — with statuses no contradiction search could see. A second, deeper half was found only by **attacking the first fix**: detail-block statuses never reached the cross-Master table at all, so the mutation that should have flagged RECOVER-1 came back CLEAN even with the anchor repaired. **OBS-3 was not merely "documented and intentional"** (**GOV-12**): the audit missed that origin's `RBAC-3` table row and the local `RBAC-5` block are the **same finding**, so one finding sat under two ids while `RBAC-3` also headed two blocks for a different one; the row is now `RBAC-5`, applying a decision the register had already recorded, with migration/test prose deliberately left byte-identical to what Primary ran. **GOV-13 is new and was missed by the audit:** the merge deleted two pgTAP files and left **three live references** naming them — one a register row citing a deleted test as SUP-3's proof — because Check 1 resolved document tokens only; Check 1 is now widened to test files and `scripts/*.ps1`. **GOV-14** records that the audit itself left no repository artifact, and extends `AGENTS.md §6` so a read-only session is still a session. **CANON-31-1** adds `user_permission_grants`, `capability_group` and `action_kind` to canon 31 at intent level. Every guard change was **mutation-proven in both directions with byte-identical restore**. Full `§5a` re-verification executed green: 189 migrations, **92 files / 1303 assertions Pass A = Pass B**, **414 HTTP assertions / 0 failed** across six suites, smoke ALL CHECKS PASSED, and **all three Primary values read live** (`4029ecef…` · `c83114a8…`/257 · `7f327405…`/3442) with both guards CLEAN. Batch 6 reconnoitred discovery-only in `history/session-2026-09-04-batch6-discovery.md`; **SUP-4b untouched.**
>
> *Previously:* `history/session-2026-09-03-git-divergence-reconciliation.md` — **two legitimate histories, reconciled into one authoritative `main`.** Two sessions worked forward from `4b67d3f` on 2026-09-02 without fetching — one shipped COLD-2/COLD-3/SUP-2/SUP-3/RBAC-3/SUP-4a to `origin/main`, the other produced GOV-10 and the RECOVER-1 recovery and could not push. Analysed read-only first, then merged with `--no-ff`; **no rebase, no force, nothing discarded.** The analysis reframed RECOVER-1: the four migrations were **never orphaned from the repository**, only absent from that clone — `git rev-parse` proves the reconstructions are **byte-identical blobs** to the committed originals, so the recovery was exact while its causal story was wrong, and that story is corrected in the register, the manifest and the report. **The dangerous conflicts were the ones git did NOT flag:** `GOV-10 ≡ COLD-2` is one defect found twice, and a second copy of the comparison would have counted one divergence as two issues (a guard lying about magnitude); **ADR-0027** was auto-merged into two headings for one decision; and two independently-added `Check 18`s were separated into **Check 18** (COLD-3) and **Check 19** (RECOVER-1 ledger evidence) — where origin's block had also silently lost its closing braces to the conflict boundary and would not parse. Rebase was rejected on evidence, not taste: the ledger evidence binds to a commit a rebase would rewrite, so **Check 19 would have failed by design.** The duplicated test suites were resolved by keeping the originals and **porting the one assertion the reconstruction had that they lacked** — that each of the two credit guards independently defends the ceiling. 189 migrations, matching Primary exactly.
>
> *Previously:* `history/session-2026-09-03-recover-1-closure.md` — **RECOVER-1 CLOSED: the Primary ledger evidence guard.** The closure pass re-derived the root cause and **found the previous report's version of it wrong**: `check_database_parity.ps1` never failed open — run without Primary values it exits 2 and prints *"This is NOT a pass"*, reproduced live. The real hole was narrower and worse — **nothing in the repository recorded whether Primary had ever been read at this HEAD**, so the question was unanswerable from the repository, and an unanswerable question is indistinguishable from a satisfied one when nobody asks it. A live read was evaluated first and rejected on constraints, not taste: the `supabase-primary` MCP is an HTTP endpoint with no reusable local secret, every relevant credential env var is absent, and the CLI is not linked — so a guard credential would have to be committed, which `AGENTS.md §6` forbids. Closed instead by `reports/evidence/primary-ledger-evidence.json` (Primary's **whole** ledger, HEAD-attributed) enforced fail-closed as a new check of the guard (written as Check 18; **renumbered to Check 19** by the 2026-09-03 reconciliation, which found COLD-3 had independently taken 18 on `origin/main`) that actually runs on every commit and in CI. A pasted number is now worthless: count and fingerprint are **recomputed** from the ledger. **13 mutation assertions** prove it flags an extra migration on either side, a same-count/different-identity swap, a stale or foreign HEAD, missing evidence, hand-edited numbers, malformed JSON and a missing field — with controls in both directions — and the **integration** was proven separately by deleting the evidence and watching the parent guard exit 1. Residual stated rather than hidden: it verifies a *recorded* reading, not a live one (GUARD-1's class).
>
> *Previously:* `history/session-2026-09-03-orphaned-migration-recovery.md` — **four orphaned migrations, and the authorization refactor nobody could see.** An owner directive described work delivered in commit `1193643`, a finding `SUP-4`, and a `user_permission_grants` table — **none of which existed in this repository**, and `1193643` is not a valid object in it. Testing that rather than assuming the directive described a different repository found the defect: **Primary held 188 migrations while the repository held 184.** `202607059600`–`202607059900` had been applied to Primary on 2026-09-02 and never committed, so the repository could not rebuild the environment it deploys to, and every local proof was evidence about a different schema. Every guard reported CLEAN, because **no guard in ORVION reads Primary's migration count on its own initiative** — "I did not check" and "I checked and it matched" produce identical output (**RECOVER-1**, Critical). All four recovered byte-identical by md5 from Primary's ledger and replayed by a clean reset. Their tests, ADR and register rows were **not** recoverable — a database returns SQL, not tests — and are rewritten rather than restored: ADR-0027, seven register rows, two pgTAP files (31 assertions), 19 HTTP assertions. Reconstructing them surfaced **RBAC-6** (`app.effective_permissions` had no `public` wrapper, so the dashboard capability was unreachable), a swallowed-fixture class in the HTTP harness, a second independent enforcer found by a mutation that *failed*, and order-dependence caught by Pass B. SUP-4 driven to the end: currency, exposure, comparison rule and supplier scope all proved **DERIVABLE** and shipped; **`CREDIT LIMIT ENFORCED = NO`**, with one genuine owner decision left (**SUP-4b**). The guard RECOVER-1 earns is **not built** and is the next step. **SUPERSEDED 2026-09-03 (reconciliation):** the four were never orphaned from the REPOSITORY — they were committed and pushed as `b72caff`/`46a0cfc`/`1193643` while this clone worked forward without fetching, so `1193643` was absent from the CLONE, not from the repository. The recovery was exact — git blob hashes prove byte-identity with the committed originals — and the parity class the finding names still stands; only the account of where the commits went was wrong. See the latest report.
>
> *Previously:* `history/session-2026-09-02-handoff-pointer-guard.md` — **the cold-start handoff pointer, and Check 7's last unguarded field.** `generate-ai-map.ps1` extracts four `live_state` fields from the manifest; Check 7 is named "ai-map freshness vs manifest" and compared three. The uncompared one was `active_change_request` — the field `AGENTS.md §4` step 4 branches the entire boot sequence on, and the only sanctioned handoff channel between sessions, whose pointer-clear has already been forgotten twice (SPEC-024, SPEC-027). A stale non-`None` sends a fresh agent into a closed Change Request; a stale `None.` hides an open one. Root cause: coverage decided field-by-field, each field added the day its own drift shipped, so the check's coverage recorded which drifts had already been paid for rather than what its name claimed — GOV-4's class, sixth instance. Repaired and **proven to fail on an intentionally stale artifact before anything was regenerated**, then mutation-tested in both directions and at both values, because a comparison can be present and still inert at the value the field actually holds. Manifest headroom restored 33 → 652 characters by trimming narrative the field's own rules forbid, never by raising a budget (MF-1). The re-scan then found the register carrying `Status: OPEN` for **API-3** three days after the generated contract, the execution plan and the manifest all said CLOSED (**REG-3**), and the guard's own registry claiming both more and less than the guard measures. Repository-only: no migration, no database change.
>
> *Previously:* `history/session-2026-09-02-capability-authorization.md` — **RBAC-3 / ADR-0027: capability grants become per-user; SUP-4a fixes the ceiling's missing currency.** The owner relaxed the standing "do not redesign RBAC" constraint and authorised a full rebuild if the evidence supported one. **It did not.** `role_permissions` is the ONLY foreign key into `public.permissions`, so the whole gap to the owner's capability-level end-state was **one missing user→permission edge** — while `app.has_permission` is already resolved by **60 RLS policies, 61 triggers and 76 functions**. Rebuilding would rewrite 197 enforcement sites to reach identical behaviour, each a chance to drop a rule earned by a real defect. PRESERVE was rejected too: per-user override and deny were **unrepresentable**, so every “this user minus this capability” becomes a new role (OWASP's role explosion). Chosen: **refactor the grant model, preserve the enforcement plane** — not one policy, trigger, RPC or grant touched. `user_permission_grants` gives grant/deny per user with **deny-override > user grant > role grant, then the plan gate** (deny-override adopted from AWS IAM / Azure, not invented; the plan gate stays last so a tenant admin cannot grant past a commercial entitlement). `capability_group`/`action_kind` derived from canon 28 make the dashboard's grouping and View/Manage **data, not code**. **No privilege expanded** — pinned by a set-comparison assertion. Permissions deliberately NOT moved to JWT claims: ORVION resolves the actor from `public.users`, so claims would ADD a revocation-staleness window it does not have. `finance_manager` gains Supplier Management as a ROLE grant. **SUP-4a:** `suppliers` was the ONLY money column in ORVION without a currency beside it — canon 30's own standard unapplied to one column, and exactly what made the ceiling incomparable to `supplier_balance`'s per-currency payable; fixed. **SUP-4b stays OPEN** and narrowed: ADR-0020's `advance_booking` precedent settles the FORM (per-currency, never converted), leaving which operation the ceiling refuses, what overrides it, and whether it binds on all suppliers. Four class guards demanded justification before they could be edited and **one caught a real defect in the new table**. Tests 92/1301, HTTP 395, 76 tables; `202607059800`+`202607059900` deployed, parity re-proven from Primary.
>
> *Previously:* `history/session-2026-09-02-cold-start-handoff-guard.md` — **COLD-2: the handoff field no guard compared.** `generate-ai-map.ps1` extracts four live-state fields from the manifest and Check 7 compared three — the missing one, `active_change_request`, being the field `AGENTS.md §4` *branches* on and the whole of the agent handoff (`§6`, `CR_LIFECYCLE.md §9`). Proven blind in **both** directions before the fix: ai-map naming a SPEC that does not exist, and the manifest naming a SPEC ai-map did not carry, each printed CLEAN — a cold-start agent would have chased a Change Request that was never approved, or silently skipped the one in flight. Root cause: Check 7's coverage was a record of *past incidents* (each field guarded only after it had drifted) rather than a statement about the generator's contract — the GOV-4 / MEAS-1 class. Now compared by value on the identical contract as `Last Completed`, whose own comparison had been inert for forty commits until `4b67d3f` and was **never recorded until now**; both halves are registered as COLD-2. Attacked with 14 runs reporting each sub-comparison separately, so field isolation is measured, not assumed. The clear-on-Complete gap (SPEC-024/027) is explicitly **not** closed by this and is stated as a limit. Manifest headroom restored 33 → 747 characters by deleting chained history, **without raising any budget**. Repository-only: no migration, no database change.
>
> *Previously:* `history/session-2026-09-01-cold-start-guard.md` — **the cold-start contradiction, and the guard that reads canon against the decision list.** `32_execution_roadmap.md` told a fresh session that SEC-1 was an open owner decision blocking Phase 10, four days after the owner ratified it, and restated "71 RPC endpoints" inside a sentence declaring it never restates that count. Verifying the first found a third: the register's own SEC-1 row still said AWAITING OWNER RATIFICATION while its OWNER-1 row recorded the ratification — Check 2 could see neither, because a status cell opening `**EVALUATED …**` is neither its OPEN form nor its resolved form, and no guard had ever read canon against the decision list. Fixed by deletion rather than refreshment; **Checks 16 and 17** added, attacked with 13 cases including a second-direction test that mutates the authority instead of the consumer. A living-document re-scan found 74 further restated figures and confirmed every one is dated evidence, not a current claim. Repository-only: no migration, no database change.
>
> *Previously:* `history/session-2026-09-01-finance-periphery.md` — **PAY-1 / JE-1 / DEV-1, and the blind spot in my own detector.** The Batch-6 slice of tables `authenticated` can write that no test had ever been *about* — a ranking attacked twice before use, because counting triggers scores `moddatetime` as protection and counting test mentions scores `tenants` at 76 since every test builds a tenant fixture. `app.record_payment` refuses a draft, voided or archived invoice and `payment_allocations` carried neither rule, so 1,000 EGP sat allocated against a voided invoice while FIN-10's ceiling stayed green — it caps the amount and never reads the state. A journal line could post to a retired chart account. Two concurrent `record_trusted_device` calls produced two rows for one device, reproduced through the RPC alone. **And PARENT-1's own detector could not see the worst of them**, because it derived its population from `app.status_transitions` and `invoices` has no rows there (FIN-7): widened, counterexample-tested, residual stated rather than hidden. Two hypotheses were killed by measurement before any code was written. JE-2 (a journal entry balancing across two currencies) recorded and deliberately not fixed — DC-11 owns that model. Migration `202607059500`, deployed.
>
> *Previously:* `history/session-2026-09-01-parent-state.md` — **PARENT-1: the parent's state is a rule on every door.** The care/conversation slice was re-entered from live state and `conversation_messages` exposed a class: four RPCs refuse a write because of the PARENT row's state — an unaccepted quotation, a cancelled booking item, an archived document, a closed conversation — and not one of those rules existed on the table door `authenticated` reaches through PostgREST. The population was derived from `app.status_transitions` + `pg_proc` + `pg_trigger` rather than listed; twelve candidate pairs were reduced to four by reading each function instead of trusting the match. All four reproduced live with the RPC as positive control, and closed by one guard function plus four BEFORE INSERT triggers with the RPCs' messages copied verbatim. The HTTP suite already asserted both halves of the conversation case and missed it because they were never asked at the same moment. Also: a live instance of GOV-9 — QUO-4 had been an open owner decision the manifest never listed. Migration `202607059400`, deployed.
>
> `AGENTS.md §4` Stage A step 7 requires every session to read this before proposing work. **Whoever writes the next session report updates this row in the same commit** — an unlinked report is invisible to the boot sequence, which is the one job this pointer has.

**Physical structure (reorganized 2026-07-11, session 9):**
```
reports/
  README.md                      (this index)
  architecture-decision-records.md   (ratified ADR log — top-level authority)
  future-backlog.md                  (deferred work + triggers)
  master/     🟢 Living-Authoritative — findings, plan, blueprint, API contract
  evidence/   🔵 Living — decision-validation trail
  history/    🟠 Historical-Immutable — dated review/phase/session/process reports
```
*(File counts were removed 2026-08-29 — GOV-5. They had read 14 / 5 / 34 while the real figures were 15 / 5 / 78, and a count that must be hand-maintained on every commit is a stale number waiting to happen. `ls` answers it exactly; the current population is tracked in `MASTER_REPOSITORY_HEALTH.md §2`, which is reviewed rather than incidental.)*
**Reference convention:** reports are cited by **unique filename** (filenames are globally unique); the subfolder is an organizational detail. So `MASTER_GAP_REGISTER.md` resolves regardless of prose reference — moves never break citations.

> **Where do I write a new finding?** → `evidence/PENDING_ARCHITECTURE_FINDINGS.md` first, then (if validated) `master/MASTER_GAP_REGISTER.md`. See `GOVERNANCE.md §3`. Never edit a 🟠 `history/` file.

## reports/ root — stable authorities
| File | SSOT for |
|---|---|
| `architecture-decision-records.md` | **ratified ADRs** (ADR-0001…) |
| `future-backlog.md` | deferred work + triggers |
| `README.md` | this index |

## master/ 🟢 — Master documents
| File | SSOT for |
|---|---|
| `MASTER_GAP_REGISTER.md` | **all accepted findings** (others reference its IDs) |
| `MASTER_EXECUTION_PLAN.md` | finding batches (references roadmap phases in canon-32) |
| `MASTER_DEPENDENCY_GRAPH.md` | finding dependencies / ordering |
| `MASTER_RISK_REGISTER.md` | production/compliance risks |
| `MASTER_CERTIFICATION_STATUS.md` | certification state + gate |
| `MASTER_DESIGN_CHECKLIST.md` | design-integration checklist |
| `MASTER_ARCHITECTURE_DECISIONS.md` | proposed/amendment **overlay** (ratified log is at root) |
| `MASTER_DOMAIN_CATALOG.md` | domain index + completion % |
| `MASTER_ENTITY_RELATIONSHIP_MAP.md` | entity CRUD + references |
| `MASTER_DATA_FLOW.md` | end-to-end business flows |
| `MASTER_COVERAGE_SCORE.md` | design-completeness scorecard |
| `MASTER_HEAT_MAP.md` | architectural-importance ranking |
| `MASTER_REPOSITORY_HEALTH.md` | measurable repo/governance health |
| `MASTER_INTEGRATION_CATALOG.md` | external-integration contracts: registry, workflow specs, owner-setup checklists (seeded 2026-07-17, Phase-8 trigger) |
| `MASTER_API_CONTRACT.md` | the client-facing surface — RPC endpoints, tenant-reachable tables, reporting views, permissions, HTTP-evidence coverage. **AUTO-GENERATED (`scripts/generate-api-contract.ps1`); never hand-edit** — `check_database_parity.ps1` Check L3 regenerates and diffs it |

## evidence/ 🔵 — decision-validation trail
| File | Role |
|---|---|
| `VALIDATED_ARCHITECTURE_DECISIONS.md` | findings that passed the 9-stage pipeline |
| `PENDING_ARCHITECTURE_FINDINGS.md` | findings that failed a stage (+ triggers) |
| `REJECTED_ARCHITECTURE_DECISIONS.md` | rejected designs/sub-solutions (+ reasoning) |
| `INDUSTRY_REFERENCES.md` | external evidence library (cited by ref-id) |
| `ARCHITECTURE_PROOF_LOG.md` | every finding's path through the 9 stages |

## history/ 🟠 — immutable session/phase/process records (do not edit)
Design/review sessions: `engineering-audit-2026-07` · `business-stress-test-2026-07` · `design-evolution-plan-2026-07` · `complete-platform-design-baseline-2026-07` · `complete-platform-physical-design-2026-07` · `architecture-synthesis-2026-07` · `design-completion-certification-2026-07` · `design-review-2026-07-11` · `design-authority-2026-07-11` · `final-design-proof-2026-07-11` · `governance-eos-consolidation-2026-07-11` · `repository-eos-review-2026-07-11` · `repository-eos-validation-2026-07-11` · `execution-readiness-2026-07-11` · `repository-engineering-2026-07-11`
Phase & process: `phase-02-*` (2) · `phase-03-user-lifecycle-review` · `phase-04-crm-core-retrospective` · `phase-05-finance-gate-readiness` · `phase-2-*` (3) · `repository-communication-protocol` (+v0.2) · `repository-engineering-program` · `workflow-architecture-report` · `pre-phase8-readiness-audit-2026-07-13`
Discovery/verification checkpoints (read to continue from preserved engineering state, not to restart): `session-discovery-checkpoint-2026-07-14` — full state of the owner-directed review+research session (5-specialist verification pass, external compatibility research, approved P1–P7, UUIDv7/Self-Healing/Self-Learning/Airports-Airlines conclusions, consolidated synchronization register, pending owner decisions). · `repository-synchronization-integrity-audit-2026-07-15` — owner-directed final Repository Synchronization & Integrity Phase before Phase-8 implementation; re-verifies all prior findings against current file state (HIGH integrity, no forgotten architecture), confirms S-EVENT as the one live structural gap, and gives a priority-ordered Recovery Plan (awaiting owner approval). · `repository-reverification-log-2026-07-15` — deeper source-level re-verification pass (prior reports untrusted); independently confirms 76 migrations / 71 tables / 55 RPCs, resolves the RPC-count ambiguity (F11=55), re-confirms S-EVENT, and finds no new issue — the audit above remains the current register + Recovery Plan. · `repository-recovery-completion-2026-07-15` — **completion record** of the Repository Recovery phase: what was synchronized/annotated/cleaned, the permanent CI consistency guard installed, verification (guard CLEAN), remaining owner decisions, and the next-phase recommendation (Reporting/RC-4 before Phase 8). Governance advanced to v1.6. · **`pre-workflow-ground-truth-audit-2026-08-21`** — owner-directed **read-only** ground-truth audit before the first n8n Workflow: boot sequence re-run from scratch, then live introspection of Primary, the local stack, GitHub and n8n. Verdict `CLEAN WITH DOCUMENTATION DRIFT` — every executable axis reconciled (90 migrations agreeing by version *and* name across all three; smoke-test and both vocabulary guards executed live on Primary; SPEC-123's deployed function byte-identical to its certified md5), with three documentation-side defects found. Read it as **point-in-time evidence at HEAD `0cdcfd2`, not current state.** · **`remediation-and-hardening-pass-2026-08-21`** — the sequel that acted on every one of those findings and is the current-state record (HEAD `8b01de3`, 91 migrations). Its own discoveries matter more than the audit's: a **live-only privilege defect** (`anon` held full DML on all 72 public tables; `authenticated` held `DELETE`/`TRUNCATE`) inherited from Supabase's hosted default ACL and structurally invisible to `db reset` + the smoke-test — fixed by SPEC-124 and guarded — plus **SEC-1**, the open owner decision on whether `authenticated` should hold direct table writes at all, since RLS scopes rows and not permissions. Also carries the Google Data Manager API field contract re-verified against current official docs, and the WeWeb assessment (`CONDITIONALLY RECOMMENDED`).

## Reading order for a newcomer
`master/MASTER_CERTIFICATION_STATUS.md` (where we stand) → `master/MASTER_GAP_REGISTER.md` (what's open) → `master/MASTER_EXECUTION_PLAN.md` (what's next). History is context, read on demand.
