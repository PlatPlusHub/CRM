# ORVION reports/ — Index & Organization

Governed by `GOVERNANCE.md` (§7). This folder holds analysis, findings, evidence, and history — **never** authoritative business/schema canon (that is `_ORVION_CANONICAL/**`).

> **Latest session report:** `history/session-2026-08-31-batch6-attribution.md` — **Batch 6 table-by-table audit, first slice: the accounting core and the actor-attribution class.** The slice was picked by measuring guard and test coverage across the 54 writable tables, and the first ranking was WRONG and caught before use (it keyed on `guard_write_capability` and scored the finance tables as unguarded; FIN-3 had given them dedicated triggers under other names). Three clean results — SPEC-138 covers every identity/organization write, the accounting core requires its exact canon-28 permission, every FK into `exchange_rates` is tenant-qualified — and **four defects**. **FX-1:** `rate = -48.5` and `rate = 0` both returned `INSERT 0 1`. **FX-2:** the setter was accepted from the caller. **FX-3 (Medium):** the record of WHO GRANTED A ROLE was accepted from the caller — reproduced two-door in one transaction, where the RPC recorded the owner who called it and the direct INSERT recorded **the manager being promoted**. **FX-4:** found by the class assertion, not by a human pass; unreachable today and guarded anyway because canon 28 assigns the permission that would reach it. **The method failure is the lesson:** a hand-written actor-column list reported ONE gap and looked finished — `assigned_by` produced FX-3, `reviewed_by` produced FX-4. A detector's blind spot is indistinguishable from a clean result, so the detector became assertion 22, which asks `information_schema` with **no exemption list** and closes **GOV-8**. Pass A **83 files / 1,110**, HTTP **366/366**, Pass B **83/1,110** under residue, smoke PASS, guard CLEAN. **Nine migrations verified and NOT deployed; Primary stays at 169.** Predecessor `history/session-2026-08-31-spec-154b-and-decision-memory.md` — **SPEC-154-B decided by the owner (Option C) and implemented in `202607058700`.** A financial document was readable by anyone whose department could see the record it hung off. Reproduced with a discriminating control: two `employee`s in the SAME branch, department, role and permission set, differing only in whether they owned the booking, read the manager-uploaded invoice **identically** — the read came through the department axis, and the visible LINK was the whole test the policy applied. Closed in the `documents` RLS policy, the one layer both doors traverse; `VIEW_FINANCIAL_DOCUMENTS` was **not** granted to anyone (**ADR-0026** — scoped access is a predicate, never a coarser grant). `quotation` also left `app.is_financial_document_type`, because canon 07 omits it and canon 28 governs it under CRM. **Also found — TEST-3:** a pgTAP mutation assertion placed last is never counted (`rollback to savepoint` undoes the temp-table counter), so **test 80 had been shipping a `planned 18 but ran 17` warning under a green PASS**; both files also lacked PAR-4's closing move and now re-assert after the rollback. **Three ADRs ratified** (0024 two doors · 0025 enforcement layer and the session-less rule · 0026 scoped predicate), the ADR record shape gained *Alternatives rejected / Constraints on future work / Evidence basis / Revisit trigger*, and the undefined `LESSON 4`/`LESSON 6` ordinals were resolved. Pass A **82 files / 1,088**, HTTP **366/366**, Pass B **82/1,088** under residue, smoke PASS, guard CLEAN. **Seven migrations verified and NOT deployed; Primary stays at 169.** Predecessor `history/session-2026-08-30-api3-closure.md` — **API-3 is CLOSED: 71 of 71 endpoints carry HTTP EXECUTION evidence**, and the last three produced three defects. **TASK-1 (High):** `app.assign_task` charges ASSIGN_TASK (`branch_manager, ceo, department_manager, owner`) while `public.tasks` charged only **CREATE_TASK**, which `employee` and `senior_employee` also hold. **That difference is what makes it real** — when the same question was asked of `lead_assignments`, both permissions resolved to the identical role set and the finding was closed as NOT A DEFECT; here they diverge. Reproduced in one transaction: the RPC refused an employee with `permission denied: ASSIGN_TASK`, their direct `UPDATE` returned **UPDATE 1**, and `task_assigned` events numbered **0** — unauthorized *and* unaudited, because the event comes from the RPC. Closed with a trigger firing **only when `owner_user_id` actually changes**, chosen over widening `guard_write_capability` because that guard cannot see which column moved and would have stopped an employee completing their own task; a negative control proves they still can. **TASK-2 (Medium):** `assign_task` resolves the placement of an *arbitrary* user, so neither `current_placement()` nor `eligible_lead_handlers` fits — and it inlined `ends_at is null`, a **third and narrowest** definition of "current placement". Reproduced with a wrong value: a CAIRO task handed to GIZA staff whose transfer is scheduled 30 days out silently kept **Cairo**, the previous owner's branch. Fixed with PLACE-1's strictly-additive shape. **SUP-1 (Low):** provider-pair coherence, the derived requester and the item lifecycle were all absent from `internal_supplier_links`' table door — each reproduced as an ASSIGN_SUPPLIER holder whose RPC call had just been refused. **Severity is Low for a measured reason: that table has NO consumer** — nothing reads it but its own RPC and the capability guard — so corrupt rows misstate history and change no behaviour today; fixed because the history *is* the point of an append-only log. **`financial_documents` produced EVIDENCE, not a fix.** Measured with a discriminating control: a `finance_manager` reads the financial documents, an `employee` is refused **42501** by the endpoint — yet that employee reads a **non-confidential** invoice document through the table, while a **confidential** one is correctly hidden. So the confidentiality boundary IS enforced, and the breadth beyond it is **SPEC-154-B**, which canon 28 frames on both sides at once (*"assigned employee may view financial documents directly related to their lead/booking"* AND *"financial documents require stricter visibility"*). Forcing `is_confidential` on financial types would satisfy the second by destroying the first, so it was **pinned by assertion rather than decided** — recorded as FIN-DOC-1, evidence attached to the existing decision, creating no new one. **Six things were established as NOT defects rather than assumed**, including that `user_branch_assignments` RLS is tenant-wide — checked precisely because the tests ran as `postgres`, which bypasses RLS and would otherwise have made the TASK results unsafe to trust. **175 migrations, Pass A = Pass B = 81 files / 1,066 assertions, HTTP 349 → 360, API-3 6 → 3 → 0.** **Six migrations (`202607058100`–`202607058600`) are verified but NOT deployed — Primary stays at 169 and the drift is intended, pending owner approval.** Next: SPEC-154-B, now decidable. Predecessor `history/session-2026-08-30-subscription-licensing-audit.md` — **API-3's subscription/licensing family: a single-use code that was not, and a recovery path that was shut.** **LIC-2 (High):** `redeem_license_token` checked `consumed_at is null` and then updated `where id = ...` — check-then-act with no guard on the act, so under READ COMMITTED the second redeemer's UPDATE re-evaluates only the id and still matches. **The repository asserts the opposite twice** — the function's own comment (*"replay is closed by consumed_at"*) and `43_license_activation_test` assertion 11 (*"the SAME code cannot be used twice"*) — and both are true only IN SEQUENCE, which is LESSON 4 exactly. Reproduced with **two live psql sessions**: both returned success, `security_events` recorded **2** `license_token_redeemed` rows for ONE token, and the subscription activated twice — evidence that is not merely incomplete but **internally inconsistent**, the row saying consumed once while the spine says twice. Closed with a compare-and-swap raising the SAME generic message, because "you lost the race" would be as much of an oracle as "already used"; the identical experiment afterwards produced **one** redemption. **The function is the complete enforcement layer here, and that was measured rather than copied:** `authenticated` holds NO grant on `tenant_license_activations` and its RLS is `platform_only`, so unlike BOOK-1/ASGN-1/CM-2 there is no second door and a trigger would guard one nobody can open. **LIC-3 (High):** `UPLOAD_DOCUMENT` and `CREATE_DOCUMENT_VERSION` both carry `required_feature_code = documents`, which `feature_entitlements` sets **false** for `starter` — so the entry-level tenant could not file the payment proof canon 09/28 call *the only way back from read_only*. WP-04-B narrowed the SUBSCRIPTION gate for exactly this case; the PLAN gate was never considered. Proven by a discriminating experiment: two tenants identical but for the plan, same owner role, same aal2 claim — professional succeeds, starter is refused, `MANAGE_TENANT_SETTINGS` true on both. **PP-4 (Medium):** an `employee` holding no MANAGE_TENANT_SETTINGS planted a confidential `payment_proof` document by direct DML — SPP-2's shape one table over. Both closed by charging **MANAGE_TENANT_SETTINGS** for payment-proof documents: the permission the RPC already charges, and the only one not gated on the entitlement the tenant is trying to restore — **paying for your plan cannot itself be a plan feature.** The `starter` entitlement was deliberately NOT changed, because plan contents are a commercial decision belonging to the owner. **Six things were established as NOT defects rather than assumed**, including that `tenant_capabilities` being ungated is deliberate (canon 09 restricts no capability visibility, canon 28 scopes VIEW_SUBSCRIPTION_STATUS to *status*, it exposes no commercial data and has no internal consumer) and that **LIC-1 and SPP-3 were read, not reopened**. **CAP-1 recorded, not reproducible:** `tenant_capabilities` is the only one of eight subscription readers without "latest row wins", but `provision_tenant` is the sole inserter and no role holds MANAGE_SUBSCRIPTION, so a second row is unreachable. **Two guards caught regressions in this session's own work** — the whole suite refused a first draft that named a record field inside a CASE shared by 21 tables, and test 35 failed correctly because its payment-proof block used an employee. **174 migrations, Pass A = Pass B = 80 files / 1,046 assertions, HTTP 339 → 349, API-3 6 → 3.** **Five migrations (`202607058100`–`202607058500`) are verified but NOT deployed — Primary stays at 169 and the drift is intended, pending owner approval.** Next: the final three API-3 endpoints. Predecessor `history/session-2026-08-30-customer-data-audit.md` — **API-3's customer-data family, and every defect was already written down somewhere in this repository.** **PLACE-1 (High):** `app.current_placement()` matched `ends_at is null` only, so an employee with a **scheduled transfer** — which canon 03 explicitly provides for — returned NOTHING. Its **five** consumers (`create_customer`, `create_quotation`, `create_complaint`, `create_service_request`, `start_conversation`) all read it with `SELECT … INTO`, so empty is not an error but a silent NULL. Reproduced end to end: an owner holding MANAGE_USERS set `ends_at = now() + 30 days` through the door RLS already permits, the function returned **0 rows**, and the next customer that employee registered was stored with **`first_registered_branch_id` NULL** — re-creating precisely what `create_customer`'s own comment says it had fixed (*"Leaving it null was how canon 03's requirement quietly went unrecorded"*). The repository already held the better answer: `eligible_lead_handlers` (LEAD-3) tests the full window and returns 1 row for the same fixture. **The fix is strictly additive** — the ordering prefers the open-ended row, so every answer the old predicate gave is returned unchanged and only the empty case is filled; `user_branch_assignments_one_primary_idx` guarantees at most one such row and **structurally cannot be widened**, because a partial-index predicate may not call `now()`. **CM-2 (High) is LESSON 4 landing exactly:** `202607052100` asserts *"the index is what makes that check hold under concurrency and on the direct path"* — and **that sentence is false**, because `customer_contact_methods_unique_value_idx` covers the **raw** value, so a denormalized string is simply a different string. Reproduced: the RPC stored `mona@example.com`, a direct INSERT of `'  MONA@example.com  '` **succeeded**, leaving **two rows for one logical address** on one customer and one channel — while the identical value was **REFUSED** on `customers.primary_email`. `merge_customer_identity` picks which methods to delete by comparing `t.value = s.value`, so the twin is invisible to it (CUST-1's family). Closed with a CHECK that **reuses** `normalize_email`/`normalize_phone` — both verified IMMUTABLE, which is what makes them legal in a constraint — rather than writing a third copy of the rule. **CM-1 (Medium):** the RPC demoted "primary" across ALL channels while the index encodes one primary **per** channel, so adding a primary email silently unset the primary phone. **Five things were established as NOT defects rather than assumed:** `find_customer_duplicates` is INVOKER so RLS bounds it (proven — a rival tenant's customer with the *same phone* is not returned), it matches through normalization, it reads `customers`/`customer_identity_signals` and not contact methods, `current_placement`'s `limit 1` was deterministic under the old predicate, and its SECURITY DEFINER is safe because the row is selected by `app.current_user_id()`. **PLACE-2 recorded UNPROVEN** — `current_placement` still omits a `starts_at` test; no harm was reproduced and adding it would change behaviour in an unproven case, which is why the fix stayed additive. **172 migrations, Pass A = Pass B = 79 files / 1,028 assertions, HTTP 328 → 339, API-3 9 → 6.** **`202607058100`, `202607058200` and `202607058300` are verified but NOT deployed — Primary stays at 169 and the three-migration drift is intended, pending owner approval.** Next: the subscription/licensing group. Predecessor `history/session-2026-08-30-marketing-conversion-audit.md` — **API-3's marketing-campaign family, and its defects sit on the only path where money leaves ORVION.** `app.claim_conversion_deliveries` returns `conversion_value` and `currency_code` **verbatim** into the payload n8n hands the Google Data Manager API, filtering only on platform, delivery status and attempt count — so a value that should never have existed is not caught downstream, it is **delivered**. **CONV-4 (High):** `record_offline_conversion` refuses a negative value and nothing else did; reproduced as an `owner` with `aal2` over the real `authenticated` role, in the same transaction the RPC had just refused, a direct INSERT stored **-5000.0000 EGP**. **CONV-5 (High):** an amount with **no currency**, same door, **7777.0000 / NULL** — unusable rather than imprecise, and interpreted by an external platform against whatever default it picks. **CAMP-1 (Medium):** `enforce_status_transition` is BEFORE **UPDATE**, so a campaign could be INSERTed with no status — permanently unadvanceable while `advance_marketing_campaign` reported *"campaign not found in your tenant"* about a row that plainly exists. All three closed with **row-level CHECK/NOT NULL rather than triggers**, because each is decidable from the single row and a constraint cannot be reached around by any door or session-less path — and **every legal writer was proven compatible before the constraint was written**: `payments_amount_nonneg_check` plus a NOT NULL currency make the session-less `map_outcomes_to_conversions` structurally incapable of violating either money rule. **Two hypotheses were disproven rather than filed:** `marketing_campaigns` already carries `enforce_status_transition` and six `status_transitions` rows, and an illegal transition by direct DML was **refused** (the RPC's inline list was compared row-by-row and agrees exactly); and the four unvalidated id parameters on `record_offline_conversion` are covered by **composite tenant FKs**, so cross-tenant reference is structurally impossible. **CAMP-2 recorded UNPROVEN** — INSERT does not constrain which state a row opens in, but no harm was reproduced and the question spans every status-bearing table, not this family. **A guard caught a regression in this session's own work:** `64_acquisition_lineage_test`'s positive control set an amount with no currency — a row the RPC has always refused — and passed only because nothing enforced the pair (**TEST-64**, corrected). **171 migrations, Pass A = Pass B = 78 files / 1,009 assertions, HTTP 315 → 328, API-3 12 → 9.** **`202607058100` and `202607058200` are verified but NOT deployed — Primary stays at 169 and the two-migration parity drift is intended, pending owner approval.** Next: the customer-data group. Predecessor `history/session-2026-08-30-lead-routing-audit.md` — **API-3's lead-routing family, and two of its four endpoints had no coverage but a name in a list.** `assign_lead_round_robin` and `lead_booking_readiness` appeared in the suite only inside `53_api_surface_test`'s endpoint inventory — the CUST-2 shape — and all four had **zero HTTP evidence**. Four defects, each reproduced before it was fixed. **LEAD-6 (High):** canon 04 says round-robin routes "among **eligible employees**", and LEAD-3 already resolved what eligible means by reading ORVION's own permission matrix — a candidate must hold `CLOSE_LEAD`. That answer reached `process_lead_sla` **and nothing else**. Round-robin still selected on PLACEMENT, the definition LEAD-3 explicitly rejected. Reproduced: a department holding one manager and one **trainee**; `eligible_lead_handlers` returned one candidate, round-robin's predicate returned two, and round-robin chose the **trainee** — who was then refused `permission denied: CLOSE_LEAD` on the lead they now owned. Fixed by CALLING the existing authority, not restating it: `user_role_assignments` carries `scope_read` RLS, so the permission join inside this INVOKER function would have been **row-filtered** and silently wrong (the BOOK-1 lesson), which is why the resolution must stay in the DEFINER helper. Granting that helper to `authenticated` as it stood would have opened a **cross-tenant staff-enumeration oracle** — it is DEFINER and takes `p_tenant_id` — so the grant ships with a tenant guard. **ASGN-1 (High):** "one lead has one current handler" lived in one function; a manager's direct INSERT produced **two current rows**, desynchronised `leads.assigned_user_id` and emitted **no event**. Closed with a partial unique index rather than a trigger, after checking every legal writer first. **ASGN-2 (High):** `assigned_by` was caller-supplied, so the manager's own act recorded as **Employee A's** — ATTR-1's class in a column ATTR-1's twenty tables never reached; the composite FK proves a user exists, never whose act it was (ADMIN-1). **ASGN-3:** a lead legally advanced to `lost` was refused by the RPC and accepted by the table door in the **same transaction**. **Both new guards were mutation-tested** (drop the enforcer, prove the violation succeeds, roll back, prove it is refused again), and **two existing guards caught real regressions in this session's own work**: `10_grant_model_test` on a new function inheriting `EXECUTE TO PUBLIC`, and **Pass B** on a test using the bare slug `rival-travel` that `verify_api_end_to_end` commits — Pass A green, Pass B losing all 22 assertions, the TEST-2 class found by the step that exists to find it. **No owner decision was created**; canon, the LEAD-3 precedent and the live permission matrix answered every question. **170 migrations, Pass A = Pass B = 77 files / 990 assertions, HTTP 298 → 315, API-3 16 → 12.** **`202607058100` is verified but NOT deployed — Primary stays at 169 and the one-migration parity drift is intended, pending owner approval.** Next: the marketing-campaign family. Predecessor `history/session-2026-08-30-handoff-readiness.md` — **an owner-directed continuity pass, and every defect it found is in the layer that RECORDS the work rather than in the product.** No migration, no test change, no roadmap change, no new package. **BOOT-1 (High):** the verification protocol this programme has actually executed on every package since 2026-08-27 — `db reset → Pass A → six HTTP suites → Pass B under the suites' own residue → smoke → three Primary values read FROM Primary → parity guard → regenerate → repository guard` — existed **only in `reports/history/**`, which is Historical-Immutable**. Measured, not assumed: `grep "Pass A"` resolved to no Living document at all. So a fresh session following the boot sequence *correctly* would run reset + smoke + the repository guard, see CLEAN twice and report "verified" — having never executed an HTTP suite, never run Pass B and never contacted Primary, which is UNPROVEN narrowed to PROVEN *by following the instructions*. Now `AGENTS.md §5a`, nine numbered steps each carrying the finding ID that earned it, plus the **seven evidence classes** as a table whose load-bearing column is *what this class can never prove*. **REG-1:** this register's only **Critical** row — IDENT-1, the account takeover — opened with a backslash-**escaped** pipe, the single such line in the repository; it rendered one column to the left, and because Check 2 extracts a row's subject with `^\|\s*(id)`, **IDENT-1 was structurally invisible to the cross-Master status comparison** — "no contradiction over 23 open ids" was computed over a set that silently excluded the most severe finding in the file. A latent false-green, not a live one (no contradiction existed, verified). Closed by **Check 13**, attacked in both directions before acceptance: re-escaping the row fails the guard by file and line, restoring it passes. **REG-2 and ROAD-1** are the **GOV-5** class twice more and were fixed the same way — by *deleting the restatement, not refreshing it*: the register's API-3 block said "30 of 71" while the generated contract it was summarising said 55 covered / 16 uncovered, and canon 32 presented "160 migrations … 230 functions" as present tense under *What is NOT blocking* while the live figures were 169 / 236. The plan's per-package narratives (`33 → 30`, `20 → 16`) were deliberately left alone — dated history of what a package moved is what AUD-03 explicitly permits. **MF-1:** `manifest.md` sat at **6,995 of 7,000** characters — five characters from failing Check 5 on whoever recorded the next capability; trimmed, never widened, for the sixth time. **And a measurement defect that was mine:** every artifact was stamped one day into the future by inferring the date from the shape of the session rather than reading the clock, and **Check 12 rejected it 16 times** — the second occurrence of AUD-01 and the first since the guard existed, caught on an author who knew the rule. Governance gained two blocks, both by ID rather than restatement: `§4` step 13 **"Never assume"** (an HTTP-covered endpoint is not safe; a passing test does not prove the intended enforcer; a CLEAN guard proves nothing outside its evidence class; a comment can be false; **IDENT-1 and ADMIN-1 are one family that landed in opposite enforcement layers**, each chosen by counting) and `§6` **"Measurement is not evidence until the measurement itself has been attacked"**. Four candidate guards were **rejected on measurement** — a register column-count check (16 legitimate historic rows already differ), a hard-coded API-3 count check (deleting the duplicate is strictly stronger than measuring it), a prose "next step" comparison, and any new aggregate health script. **Verified: 169 migrations repo = local = Primary, all three hashes read independently from both sides, Pass A 76 files / 968 assertions, smoke ALL CHECKS PASSED, parity CLEAN across ten surfaces at 3,348 objects, repository guard CLEAN at 13 checks.** HTTP and Pass B were **not** re-run and this is stated rather than implied — no SQL or suite changed; their last full evidence is `846feaf` at 298/298. Next: API-3's lead-routing family, unchanged. Predecessor `history/session-2026-08-30-tenant-administration.md` — **API-3’s tenant-administration family, the group that creates the memberships IDENT-1 exploited.** **ADMIN-1 (High):** `create_tenant_user` accepted `p_auth_user_id` from the caller and validated it against **nothing** — `users_auth_user_id_fkey` proves an identity EXISTS and nothing about WHOSE it is. Canon 34 settles the rule rather than inventing it: the Human Identity “owns everything that proves who the person is: credentials, verified email/phone”, while a membership owns only “what the person may DO”. Reproduced — Agency A’s owner bound a membership named ‘Alice Smith’ to **Bob’s identity** from unrelated Agency B, and three harms followed from one pasted UUID: every action Bob takes in Agency A is **attributed to Alice**; the real Alice, confirmed, claimed **0 memberships** and was **permanently locked out**; and Bob’s `my_memberships()` returned **both agencies**, a tenant he never joined. **The enforcement layer was chosen by measurement and is deliberately the opposite of IDENT-1’s** — IDENT-1 went in the function because 49 test files create `auth.users` without `email_confirmed_at` and its alternate paths were already closed; ADMIN-1 goes in a trigger because 120 linked rows carry **zero** divergent emails and `users.scope_update` lets any MANAGE_USERS holder rebind by direct DML, so a function-only check would have closed one door and left the other. Copying the previous package’s answer would have been wrong in both directions. **Proven NOT defects, each by experiment rather than inspection:** tenant lockout is unreachable (ADMIN-2) — established by a discriminating test showing that with TWO owner grants the first revoke succeeds and the last is refused, because `emit_role_change` is an AFTER trigger re-checking MANAGE_USERS once the granting row is already inactive; the protection is real but **accidental**, so test 76 pins it against a future refactor, and its misleading error message is recorded rather than papered over. 42 unproduced event codes (ADMIN-3) are Fundamental Domain Structure, not dead code. `one_primary_idx`, the department-name uniqueness index and the transfer/role-change events all work as claimed. **Two measurement defects, both mine, both caught before they became findings:** my “event codes with no producer” detector over-reported by **7** because generic emitters carry the code in TRIGGER ARGUMENTS rather than function source — exposed by contradicting a test I had just run — and my static pre-check for divergent fixtures reported “zero” while **ten** rows in `65_eligible_lead_handler_test.sql` modelled impossible humans, which only the full suite caught. Test 75 assertion 11 also turned **false red** (a BEFORE trigger now answers before RLS) and was rewritten so it isolates RLS and proves what it claims. **No owner decision was needed from this family** — canon, schema and experiment answered every question. **169 migrations, 76 files / 968 assertions (Pass A = Pass B), HTTP 282 → 298, parity CLEAN across ten surfaces at 3,348 objects, API-3 20 → 16.** Next: the lead-routing family. Predecessor `history/session-2026-08-30-identity-family.md` — **API-3’s canon-34 Human Identity family, and IDENT-1 is an account takeover.** `app.activate_membership()` is how a pre-provisioned membership is CLAIMED — `create_tenant_user` may leave `auth_user_id` NULL (it logs `has_auth_link:false`, so unlinked rows are an intended flow) and the invitee links their identity later. The entire authorization for that claim was a string comparison between `auth.users.email` and `public.users.email`, justified in the function’s own comment: *“the caller’s auth.users row exists only after Supabase verified this email, so the match is an authorization proof.”* **That sentence is false against this repository’s own configuration.** `supabase/config.toml` sets `enable_confirmations = false`, so a GoTrue signup creates the row IMMEDIATELY with `email_confirmed_at` NULL — and that column appeared **nowhere in any migration, test or script**. Reproduced on a clean reset: an attacker signs up as the pre-provisioned CEO’s address, never confirms, calls the RPC — and **claims the CEO membership, then holds APPROVE_FINANCE, VIEW_FINANCIAL_DOCUMENTS and MANAGE_USERS**, with `current_tenant_id()` resolving to the victim tenant. Full takeover from a signup form. **Fixed in the FUNCTION, not a trigger, and the layer was chosen on measurement:** the alternate paths were proven closed first — before claiming, the attacker has no membership, so RLS hides every row and a direct `UPDATE users SET auth_user_id` affected **0 rows** while a direct INSERT was refused **42501** — and **49 test files create `auth.users` without ever setting `email_confirmed_at`**, so a trigger would break all 49 and would impose confirmation on the ADMIN provisioning path too, which rests on a different trust basis. Banned and soft-deleted identities are refused in the same breath, because **a JWT issued before a ban outlives it**. **IDENT-4, found by not stopping when the first fix looked correct:** the claim matches case-INSENSITIVELY while `users_tenant_email_key` is case-SENSITIVE, so the same comment’s “bounded to one row per tenant” was ALSO false — a tenant holding `ceo@x` and `CEO@x` makes the claim target both rows and `users_tenant_auth_key` refuses with a raw 23505, leaving that human **permanently unable to onboard**. Fixed at the CONSTRAINT layer with a case-insensitive unique index, because the real fault was that two rows for one human existed at all; the failure now lands at provisioning time, where it is actionable. One of the two false comments was **in this session’s own new migration**, inherited verbatim before it was tested. **IDENT-2 and IDENT-3 recorded rather than invented into rules:** the claim emits no event, blocked on the same side-effect question as FIN-9/FIN-11 with a specific obstacle (`record_event` validates the tenant against `current_tenant_id()`, which is `limit 1` with no ORDER BY, so a multi-tenant claim would audit one tenant arbitrarily); and `otp_challenges`/`totp_enrollments` have **zero consumers** because MFA runs off the JWT `aal2` claim, making them Fundamental Domain Structure rather than dead code. Also corrected: SEC-1’s “ungoverned” label — `trusted_devices` does carry three event triggers; what it lacks is a CAPABILITY trigger, and its `owner_only` policy is genuinely sound, now proven behaviourally instead of quoted from `pg_policies`. **The family’s entire prior coverage was a name-existence list**, the CUST-2 shape, with a takeover sitting behind it; test 75 (24 assertions) is its first behavioural test. **168 migrations, 75 files / 945 assertions (Pass A = Pass B), HTTP 267 → 282, parity CLEAN across ten surfaces at 3,346 objects, API-3 25 → 20.** Next: the tenant-administration family, which creates the unlinked memberships IDENT-1 exploited. Predecessor `history/session-2026-08-30-book1-booking-lifecycle.md` — **API-3's booking/passenger family, and it begins with a correction:** the previous report named this group as the next of API-3's 25 uncovered endpoints, and that was wrong — `create_booking_item`, `link_passenger_to_booking_item` and `advance_booking_item` **already had HTTP evidence** and none is among the 25. The audit ran anyway, because a 200 is not evidence of a capability, and it found a High-severity money defect behind endpoints that had been “covered” for weeks. **BOOK-1:** both writer RPCs refuse to attach to an archived, completed or cancelled booking, and each rule lived in exactly one function — `booking_items` carries nine triggers and not one is a capability guard. Reproduced as an ordinary **employee holding CREATE_BOOKING_ITEM, ENTER_COST and ENTER_SELLING_PRICE**, the same permissions the RPC charges: against a booking cancelled through the legal path the RPC refused and a direct INSERT **succeeded** — selling 5000, cost 3000, gross 2000, and `commission_rate` **0.10 derived automatically**. `gross_profit = selling - cost` and `commission = max(gross,0) x 10%` are ratified rules and nothing derives the BOOKING'S state into them, so a trip that never happened produced 200 of commission while the booking beside it still read `cancelled` — and since `booking_item_created` comes from the RPC, the path was **unaudited** as well. Why the guards missed it: the RLS `WITH CHECK` requires **the parent to be visible, not the actor permitted**, and `guard_booking_item_financials` charges its permissions only when the amounts are non-zero, checking scope against `new.sales_owner_user_id` — a value the caller supplies, so an employee naming themselves is in scope by construction. Closed with BEFORE INSERT/UPDATE triggers carrying the RPCs' own rules verbatim, and three things recorded as decisions rather than defaults: **plain BEFORE rather than deferred**, because unlike FIN-8/FIN-10 this invariant holds at every instant and deferring it would break the legal order (add items, THEN cancel); **no trigger on `bookings`**, deliberately asymmetric with FIN-10, because cancelling a booking that already carries items is correct business behaviour; and **SECURITY DEFINER with a mandatory REVOKE**, because under INVOKER the check's own SELECT would be RLS-filtered and the guard would be weakest against exactly the caller it must stop. **No session-less exemption** — integrity, not authorization. **BOOK-1 is NOT the aggregate-across-rows subclass**, and that is the point worth carrying: SEC-1's clause-3 filter would not have found it, because the rule spans TABLES rather than rows. That filter is a lead, not a sieve. Two failures are recorded because both are the lesson: `10_grant_model_test.sql` caught the first draft leaving EXECUTE to PUBLIC on a DEFINER function — an existing guard catching a real regression introduced by a security fix — and my own HTTP “positive control” **was not positive**, linking a passenger to the baseline item that the refund branch had already cancelled; the companion `links=0` assertion is what caught it. **BOOK-2** recorded and deliberately NOT fixed: the per-passenger amount overrides are written by the RPC and read by nothing, so no invariant is derivable and inventing a sum rule would be manufacturing policy to produce a finding. **166 migrations, 74 files / 921 assertions (Pass A = Pass B), HTTP 259 → 267, parity CLEAN across all ten surfaces at 3,345 objects. API-3 stays at 25**, honestly. Next by evidence: the canon-34 identity family, SEC-1's only ungoverned writable tables. Predecessor `history/session-2026-08-30-verification-methodology.md` — **an adversarial review of ORVION's own verification system, proven by controlled mutation rather than by reading the scripts.** **PAR-3 is the finding: the parity guard compared 233 objects out of 3,341** — the migration ledger and the FUNCTION surface — and was blind to triggers, RLS policies, RLS *enablement*, grants, constraints, columns, views, indexes and the `app.status_transitions` rows. Reproduced on a clean local reset by dropping `payment_allocations_within_invoice_total`, **FIN-10's financial ceiling shipped the day before**: the repository guard printed CLEAN exit 0, the parity guard printed **CLEAN exit 0**, the API contract said “matches the live surface” and the smoke test said ALL CHECKS PASSED. **Only `72_invoice_allocation_ceiling_test.sql` noticed — and pgTAP runs against local only**, so nothing in the repository could have detected the same drift on Primary, which is the environment that will hold real money. Closed with **Check L4/P4** over ten surfaces, the SQL held in **`scripts/parity_surface.sql` which BOTH SIDES RUN** — not two hand-copied queries, because PAR-1a was two databases agreeing while genuinely differing over a regex. `-PrimaryStructureHash` now joins the other two in what a CLEAN verdict *requires*, since AUD-05's rule is that an unmeasured value must never quietly count as a passed one. **Then the fix was attacked and found incomplete:** `pg_policies` lists a policy whether or not row security is ON, so `DISABLE ROW LEVEL SECURITY` on `invoices` removed the entire tenant boundary while leaving the combined hash **byte-identical** — `relrowsecurity`/`relforcerowsecurity` added as a tenth surface, and `ordinal_position` added to columns because **CUST-1 was a positional assumption**. Proven both ways: mutated → exit 1 naming the drifted surfaces, `db reset` → exit 0. Local = Primary at `4bca45c73e058581190505ac878688fc`, **3,341 objects**, read independently from both. **PAR-4**, its direct consequence: since pgTAP was the only layer that noticed, it is worth knowing which assertion does the noticing — tests **70/72/73** now drop their named trigger inside a savepoint, assert the violation **succeeds**, roll back, and assert it is **refused again**. Two initial failures are recorded rather than tidied away, because both were the lesson: each probe ran under the session a *previous* assertion had left behind and died on 23503 / 42501 — the exact “denied” vs “never attempted” confusion `AGENTS.md §6` warns about, met in tests written the day before by the author of that rule. Suite **899 → 905**. **VER-1**: the contract's `http` column claimed execution and measured source text — audited first (no current false positive), comment lines now excluded, and the column restated as a **repository fact**. **VER-2 is the rejection that matters most**: a mechanical vacuous-test detector was designed, measured (**31 hits across 15 of 73 files**), and **rejected on evidence** — its one tightened hit was a false positive, because what distinguishes a vacuous test is whether the actor can *see* the rows it selects, which is semantic and not statically decidable. Mutation observes behaviour instead of matching shapes, which is why PAR-4 works where a detector would only cry wolf. Also rejected: full mutation tooling, property-based testing, a contract broker, a runtime coverage file, a second handoff document — and **raising the manifest character budget**, which Check 5 tripped on this session's own additions; the manifest was trimmed instead. **The roadmap is unchanged and no migration was written: API-3 remains next, with the booking/passenger family.** Predecessor `history/session-2026-08-29-sec1-and-the-set-level-class.md` — **SEC-1 evaluated against the live system rather than judged by opinion: ACCEPT WITH REFINEMENT, awaiting owner ratification.** The headline is that the proposed model is **not a change of direction** — it describes what ORVION has already converged on. Measured writable surface: **54 tables**, of which 35 carry a capability trigger, 15 are permission-gated in RLS `WITH CHECK`, one has DOC-3's integrity trigger, and the residual **3** are the canon-34 Human Identity tables (intentional). Option A (revoke `authenticated` writes) is **not recommended**: it needs 56 INVOKER functions converted to DEFINER, replacing RLS with 56 hand-written tenant checks — the second authorization system canon 35 forbids. **Refinement 1:** clause 3 needs a *decision procedure*, because “has no business invariant” is not decidable by inspection — FIN-8, FIN-10 and QUO-1 all looked fine. The mechanizable test that found all three: **a rule comparing an aggregate ACROSS ROWS cannot be a CHECK constraint, so it lives in exactly one function unless deliberately extracted** (with its known blind spot stated: `create_journal_entry` accumulates in a loop variable and the filter cannot see it). **Refinement 2:** authorization guards exempt session-less platform paths; **integrity constraints must not** — reached independently three times, now pinned by assertion. Applying it immediately found two more: **FIN-10** — `record_payment` refuses to over-allocate and takes an advisory lock doing it, yet a `finance_manager` put **1300 against a 1000 invoice** by direct DML, still reporting `partially_paid`, with `customer_outstanding` deriving the customer's balance from it; guarded on **both** sides, since the total can also shrink beneath the allocations. **QUO-1** — `quotations.total_amount` is *defined* as the sum of its items and maintained on one path, so an ordinary employee left a quotation reading **1000 while its items summed to 6000**: the price offered to a customer, and the number `advance_quotation` carries into the booking. **Recomputed rather than refused**, because a derived value with no independent source can simply be kept right. **GOV-7** makes CUST-1's consumer-impact rule durable without a new governance document: `AGENTS.md §3 5b` gains a second question — *which code consumes, parses or derives from the structure you changed?* — guarded narrowly by pinning the set of catalog-driven functions, of which ORVION has exactly one. **FIN-11** recorded rather than patched, because FIN-9 asks the identical question and the two want one answer. Predecessor `history/session-2026-08-29-api3-ledger-and-identity.md` — **API-3's two named endpoints, and BOTH hid a defect** — neither findable by calling the endpoint and checking the status code, because both RPCs work perfectly. **FIN-8**: the double-entry invariant lived in *one function*. `authenticated` holds INSERT/UPDATE on the journal tables and the RLS policies charge the same permission the RPC charges, so a `finance_manager` INSERTed a single 1,000,000-debit line with no credit at all — in the same transaction the RPC refused it, emitting no event. `debit_xor_credit` is a PER-ROW constraint and structurally cannot say whether an ENTRY balances, because that is a statement about a SET of rows. Closed with **deferred** constraint triggers (the invariant is only true *between* statements) carrying the RPC's own rules, and **no session-less exemption** — integrity is not authorization, so the platform does not escape it either. **CUST-1**, worse: `merge_customer_identity` archived the source, wrote its audit row, emitted a **CRITICAL** `customer_identity_merged` event, returned the target id — and **re-pointed nothing**. Silent since 2026-08-21, because **TENANT-1's composite FKs turned the loop's first key column into `tenant_id`**: *a previous fix broke it, and the failure mode was silence.* Fixed by pairing `conkey` with `confkey`, plus the contact-method collision the correct loop immediately exposes (two duplicate customers nearly always both have a primary email — resolved by deleting an exact duplicate and **demoting, not discarding**, a colliding primary, since the target is the identity that survives). **CUST-2** records why nothing caught it: the only guards checked the function's event VOCABULARY and its endpoint's EXISTENCE, and neither can see a no-op. **FIN-9** recorded rather than papered over. **API-3 30 → 25.** Predecessor `history/session-2026-08-29-document-lifecycle.md` — **DOC-LC-1**: canon 26's Document Lifecycle State Machine, defined since Phase 1 and **never wired**. `app.status_transitions` held **zero** rows for `documents`, and the guard that *is* attached — `enforce_archive_authority` — returns early unless `is_archived` changes: **it watches the boolean**, so `lifecycle_status_code` was governed only by a catalog check that asks whether a code exists and never whether the move is legal. Reproduced as a `trainee` holding `ARCHIVE_DOCUMENT = false`, proven to SEE the document first, refused by the RPC and **succeeding by direct DML in the same transaction** — FIN-6's shape one domain over. **Not cosmetic:** both document write paths read `archived` as a refusal condition, so any tenant user could **permanently freeze a colleague's passport** — unversionable *and* un-archivable — while `is_archived=false` kept it looking untouched. Fixed with the two transitions ORVION actually performs; no role gained or lost authority, and the assertion that matters most proves a manager can still archive. **Two things deliberately not decided:** **DOC-LC-2** — canon defines `active → superseded` and *nothing produces it*; the `document_superseded` **event** comes from a trigger on `current_version_id`, so supersede is an event about the version pointer, never a document status (registering it would have invented a capability to satisfy a document); **DOC-LC-3** — `is_archived` and the status column are two representations of one concept and an authorized holder can still split them, blocked on a genuine canonical contradiction and *pinned* rather than guessed. Also repaired `54_transition_permission_parity_test`'s hardcoded `lead_status_code`, and gave `archive_document` its first HTTP evidence (**API-3 30 → 29**) including `PATCH /rest/v1/documents`, the real attack shape. Predecessor `history/session-2026-08-29-verification-integrity.md` — the independent Codex audit **AUD-01…AUD-07 reproduced rather than accepted**: five verified defects, one duplicate, one false positive. **PAR-2 is the result that matters** — `verify_storage_end_to_end.ps1` rewrote `app.document_retention_days` on **every run** and "restored" it by retyping an equivalent instead of the migration's `$fn$` body, so every HTTP run left local permanently unequal to the repository. That is **the root cause PAR-1, PAR-1a and PAR-1b were all circling**: three sessions diagnosed drift in one function, all three looked at the database, and the cause was in the test suite — one of them then pushed the polluted body to Primary. Parity now survives a full HTTP run for the first time. **AUD-05**: the parity guard printed `CLEAN` and **exit 0** while Primary was never contacted, contradicting its own header contract — three outcomes now (0 CLEAN / 1 DRIFT / **2 UNPROVEN**). **AUD-01, and it was mine**: the previous session was stamped **one day ahead** in 43 places across 13 files while its own commit was authored on the real date — the date had been *assumed* from the shape of the session rather than read from the clock; all corrected, **Check 12** added with **no exemption list** (it flagged its own comment first, and the answer was to stop quoting the date, not to carve out an exception). **AUD-04**: "conflicting status across Masters = 0" was published while Check 2 only ever compared a file to itself — now genuinely measured across all Masters. **AUD-07** splits: the dependency graph claimed to cover "every finding" while naming 46 of 135 (scope corrected), `repository-index.md` is a false positive. AUD-02 duplicate of GOV-5; AUD-03 INTENTIONAL with the harmful half guarded; AUD-06 structurally closed by AUD-05. Predecessor `history/session-2026-08-29-program-reconciliation.md` — the pre-Phase-10 **program reconciliation**: roadmap, plan, canon, register, reports index and guards checked against live systems rather than against each other. **Phase 10 determined NOT READY**, and the reason isolated to one thing — Phase 8's only remaining deliverable is an n8n workflow, the instance holds **zero**, and the build is gated behind the Foundation Completion Programme; Phase 10's own first output *is* "n8n workflows", so starting it would rename the work without moving the dependency. Phase 9 acceptance re-proven live (all six outputs). Its through-line: **no product defect was found anywhere in the sweep, and six defects were found in the layer that records and governs the product** — **GOV-2** (the API contract was Living-Authoritative with no SSOT row at all, so the one property making it trustworthy — that it is GENERATED — was undocumented), **GOV-3** (seven findings were *defined* in Masters with no row in the register, five of them ids the manifest raises while promising the register defines them, so the boot sequence pointed at a dead end), **GOV-4** (Check 2's ID vocabulary was frozen at the 2026-07 prefixes and matched **none** of the ids minted since — and widening it exposed two further defects inside the same check, including that the row-vs-detail contradiction it was *built* for was invisible in the form the register actually writes), **GOV-5** (four Living docs restated live state and all four went stale — `MASTER_CERTIFICATION_STATUS.md`, the newcomer's **first** document, said Primary was at 90 migrations when it was at 160; fixed by **removing the restatement, not refreshing it**), **GOV-6** (two Batch-6 items were already closed, one contradicting its own document). Guards gained **Check 11**: every open-decision id the manifest raises must resolve in the register. Predecessor `history/session-2026-08-29-contract-to-finance.md` — the whole 2026-08-29 session in one place, **across a usage-limit interruption**: the owner's ten recommendations evaluated (9 ACCEPT, 1 ACCEPT-with-finding, none changing the execution order), the **API Capability Contract** delivered as a GENERATED document guarded by a regenerate-and-diff check, **SEC-2 resolved** — it was never one question — and **FIN-6**, the invoice an employee could declare PAID with no payment. Its through-line: **three of the session's findings were defects in the things that MEASURE, not the things measured**, and two of the three were mine. Also records what was in flight when the limit hit (a probe failing on an invalid UUID) and that the failure on resuming was **Docker being down, not SQL** — because those look identical in a transcript and only one is a finding. Carries three corrections: **PAR-1a** (the previous session's "all 228 functions byte-identical" used a POSIX bracket expression that does not mean what it looks like), **PAR-1b** (I twice read "the repository's text" out of a hand-modified local database and pushed it to Primary while reporting the opposite — RET-1 was never at risk on either environment), and a broken `\b` regex that would have supported the false conclusion "ORVION has no update RPCs at all". Per-package detail: `sec2-resolved-and-the-invoice-that-paid-itself-2026-08-29.md` and `the-api-capability-contract-2026-08-29.md`. Predecessor `history/the-care-journeys-and-the-ceiling-that-counted-wrong-2026-08-29.md` — complaints and conversations walked end to end over HTTP, and **SEC-1b**: the SEC-1 ceiling asked whether a table had a trigger MENTIONING `app.authorize` and never asked WHEN it fires, so thirteen tables were credited with INSERT-path protection they did not have; corrected residue **3 → 15**, reproduced by a `trainee` inserting a complaint AND a conversation by direct DML in the same transaction the RPC refused them. Also ATTR-4, CONV-2, COMP-1, and **TEST-2** — Pass B DIED where Pass A was green, on an `auth.users.email` collision, the one identifier the slug-collision rule never covered. Predecessor `history/scheduled-execution-and-the-guard-that-never-looked-2026-08-29.md` — all six background paths traced; **CONV-1** was real loss (a lapsed tenant's conversions were DESTROYED, not deferred, and restoring the tenant recovered nothing), **SCHED-2** gave two cron jobs the per-item isolation their sibling `reconcile_document_storage` already had, and **PAR-1** found that parity had never compared anything beyond the ledger and the functions each package touched. Predecessor `history/acquisition-lineage-and-the-eligible-handler-2026-08-29.md` — **ATTR-3**: acquisition lineage was rewritable by any employee (§8 item J answered and then exceeded), and **LEAD-3** resolved by the permission matrix, which exposed the real defect beneath it — SLA reassignment chose by PROXIMITY, handing an overdue lead to a **trainee** who can neither quote nor close. Also **GUARD-1**: the parity guard reported "primary proven" for a fingerprint supplied to it. Predecessor `history/sla-1-the-escalation-that-never-fired-2026-08-29.md` — canon 04 and canon 10 both require the employee's **manager** to be notified when a lead breaches its 15-minute SLA, and canon 10 lists it among the notifications a user **cannot mute**. It had never fired. Earlier predecessors are chained from that report.
>
> `AGENTS.md §4` Stage A step 7 requires every session to read this before proposing work. **Whoever writes the next session report updates this row in the same commit** — an unlinked report is invisible to the boot sequence, which is the one job this pointer has.

**Physical structure (reorganized 2026-07-11, session 9):**
```
reports/
  README.md                      (this index)
  architecture-decision-records.md   (ratified ADR log — top-level authority)
  future-backlog.md                  (deferred work + triggers)
  master/     🟢 Living-Authoritative — findings, plan, blueprint, API contract
  evidence/   🔵 Living — decision-validation trail
  history/    🟠 Historical-Immutable — dated review/phase/session/process reports
```
*(File counts were removed 2026-08-29 — GOV-5. They had read 14 / 5 / 34 while the real figures were 15 / 5 / 78, and a count that must be hand-maintained on every commit is a stale number waiting to happen. `ls` answers it exactly; the current population is tracked in `MASTER_REPOSITORY_HEALTH.md §2`, which is reviewed rather than incidental.)*
**Reference convention:** reports are cited by **unique filename** (filenames are globally unique); the subfolder is an organizational detail. So `MASTER_GAP_REGISTER.md` resolves regardless of prose reference — moves never break citations.

> **Where do I write a new finding?** → `evidence/PENDING_ARCHITECTURE_FINDINGS.md` first, then (if validated) `master/MASTER_GAP_REGISTER.md`. See `GOVERNANCE.md §3`. Never edit a 🟠 `history/` file.

## reports/ root — stable authorities
| File | SSOT for |
|---|---|
| `architecture-decision-records.md` | **ratified ADRs** (ADR-0001…) |
| `future-backlog.md` | deferred work + triggers |
| `README.md` | this index |

## master/ 🟢 — Master documents
| File | SSOT for |
|---|---|
| `MASTER_GAP_REGISTER.md` | **all accepted findings** (others reference its IDs) |
| `MASTER_EXECUTION_PLAN.md` | finding batches (references roadmap phases in canon-32) |
| `MASTER_DEPENDENCY_GRAPH.md` | finding dependencies / ordering |
| `MASTER_RISK_REGISTER.md` | production/compliance risks |
| `MASTER_CERTIFICATION_STATUS.md` | certification state + gate |
| `MASTER_DESIGN_CHECKLIST.md` | design-integration checklist |
| `MASTER_ARCHITECTURE_DECISIONS.md` | proposed/amendment **overlay** (ratified log is at root) |
| `MASTER_DOMAIN_CATALOG.md` | domain index + completion % |
| `MASTER_ENTITY_RELATIONSHIP_MAP.md` | entity CRUD + references |
| `MASTER_DATA_FLOW.md` | end-to-end business flows |
| `MASTER_COVERAGE_SCORE.md` | design-completeness scorecard |
| `MASTER_HEAT_MAP.md` | architectural-importance ranking |
| `MASTER_REPOSITORY_HEALTH.md` | measurable repo/governance health |
| `MASTER_INTEGRATION_CATALOG.md` | external-integration contracts: registry, workflow specs, owner-setup checklists (seeded 2026-07-17, Phase-8 trigger) |
| `MASTER_API_CONTRACT.md` | the client-facing surface — RPC endpoints, tenant-reachable tables, reporting views, permissions, HTTP-evidence coverage. **AUTO-GENERATED (`scripts/generate-api-contract.ps1`); never hand-edit** — `check_database_parity.ps1` Check L3 regenerates and diffs it |

## evidence/ 🔵 — decision-validation trail
| File | Role |
|---|---|
| `VALIDATED_ARCHITECTURE_DECISIONS.md` | findings that passed the 9-stage pipeline |
| `PENDING_ARCHITECTURE_FINDINGS.md` | findings that failed a stage (+ triggers) |
| `REJECTED_ARCHITECTURE_DECISIONS.md` | rejected designs/sub-solutions (+ reasoning) |
| `INDUSTRY_REFERENCES.md` | external evidence library (cited by ref-id) |
| `ARCHITECTURE_PROOF_LOG.md` | every finding's path through the 9 stages |

## history/ 🟠 — immutable session/phase/process records (do not edit)
Design/review sessions: `engineering-audit-2026-07` · `business-stress-test-2026-07` · `design-evolution-plan-2026-07` · `complete-platform-design-baseline-2026-07` · `complete-platform-physical-design-2026-07` · `architecture-synthesis-2026-07` · `design-completion-certification-2026-07` · `design-review-2026-07-11` · `design-authority-2026-07-11` · `final-design-proof-2026-07-11` · `governance-eos-consolidation-2026-07-11` · `repository-eos-review-2026-07-11` · `repository-eos-validation-2026-07-11` · `execution-readiness-2026-07-11` · `repository-engineering-2026-07-11`
Phase & process: `phase-02-*` (2) · `phase-03-user-lifecycle-review` · `phase-04-crm-core-retrospective` · `phase-05-finance-gate-readiness` · `phase-2-*` (3) · `repository-communication-protocol` (+v0.2) · `repository-engineering-program` · `workflow-architecture-report` · `pre-phase8-readiness-audit-2026-07-13`
Discovery/verification checkpoints (read to continue from preserved engineering state, not to restart): `session-discovery-checkpoint-2026-07-14` — full state of the owner-directed review+research session (5-specialist verification pass, external compatibility research, approved P1–P7, UUIDv7/Self-Healing/Self-Learning/Airports-Airlines conclusions, consolidated synchronization register, pending owner decisions). · `repository-synchronization-integrity-audit-2026-07-15` — owner-directed final Repository Synchronization & Integrity Phase before Phase-8 implementation; re-verifies all prior findings against current file state (HIGH integrity, no forgotten architecture), confirms S-EVENT as the one live structural gap, and gives a priority-ordered Recovery Plan (awaiting owner approval). · `repository-reverification-log-2026-07-15` — deeper source-level re-verification pass (prior reports untrusted); independently confirms 76 migrations / 71 tables / 55 RPCs, resolves the RPC-count ambiguity (F11=55), re-confirms S-EVENT, and finds no new issue — the audit above remains the current register + Recovery Plan. · `repository-recovery-completion-2026-07-15` — **completion record** of the Repository Recovery phase: what was synchronized/annotated/cleaned, the permanent CI consistency guard installed, verification (guard CLEAN), remaining owner decisions, and the next-phase recommendation (Reporting/RC-4 before Phase 8). Governance advanced to v1.6. · **`pre-workflow-ground-truth-audit-2026-08-21`** — owner-directed **read-only** ground-truth audit before the first n8n Workflow: boot sequence re-run from scratch, then live introspection of Primary, the local stack, GitHub and n8n. Verdict `CLEAN WITH DOCUMENTATION DRIFT` — every executable axis reconciled (90 migrations agreeing by version *and* name across all three; smoke-test and both vocabulary guards executed live on Primary; SPEC-123's deployed function byte-identical to its certified md5), with three documentation-side defects found. Read it as **point-in-time evidence at HEAD `0cdcfd2`, not current state.** · **`remediation-and-hardening-pass-2026-08-21`** — the sequel that acted on every one of those findings and is the current-state record (HEAD `8b01de3`, 91 migrations). Its own discoveries matter more than the audit's: a **live-only privilege defect** (`anon` held full DML on all 72 public tables; `authenticated` held `DELETE`/`TRUNCATE`) inherited from Supabase's hosted default ACL and structurally invisible to `db reset` + the smoke-test — fixed by SPEC-124 and guarded — plus **SEC-1**, the open owner decision on whether `authenticated` should hold direct table writes at all, since RLS scopes rows and not permissions. Also carries the Google Data Manager API field contract re-verified against current official docs, and the WeWeb assessment (`CONDITIONALLY RECOMMENDED`).

## Reading order for a newcomer
`master/MASTER_CERTIFICATION_STATUS.md` (where we stand) → `master/MASTER_GAP_REGISTER.md` (what's open) → `master/MASTER_EXECUTION_PLAN.md` (what's next). History is context, read on demand.
